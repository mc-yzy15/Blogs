<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <author>
    <name>mc-yzy15</name>
  </author>
  <generator uri="https://hexo.io/">Hexo</generator>
  <icon>https://blogs.yzy15.dpdns.org/img/favicon.ico</icon>
  <id>https://blogs.yzy15.dpdns.org/</id>
  <link href="https://blogs.yzy15.dpdns.org/" rel="alternate"/>
  <link href="https://blogs.yzy15.dpdns.org/atom.xml" rel="self"/>
  <rights>All rights reserved 2026, mc-yzy15</rights>
  <subtitle>技术教程 | 编程经验 | 学习笔记</subtitle>
  <title>Yzy15's Blog - 技术分享与生活记录</title>
  <updated>2026-09-12T17:33:38.964Z</updated>
  <entry>
    <author>
      <name>mc-yzy15</name>
    </author>
    <category term="云计算" scheme="https://blogs.yzy15.dpdns.org/categories/%E4%BA%91%E8%AE%A1%E7%AE%97/"/>
    <category term="Monorepo" scheme="https://blogs.yzy15.dpdns.org/tags/Monorepo/"/>
    <category term="依赖治理" scheme="https://blogs.yzy15.dpdns.org/tags/%E4%BE%9D%E8%B5%96%E6%B2%BB%E7%90%86/"/>
    <category term="架构约束" scheme="https://blogs.yzy15.dpdns.org/tags/%E6%9E%B6%E6%9E%84%E7%BA%A6%E6%9D%9F/"/>
    <category term="包管理" scheme="https://blogs.yzy15.dpdns.org/tags/%E5%8C%85%E7%AE%A1%E7%90%86/"/>
    <content>
      <![CDATA[<h1 id="Monorepo-包边界治理：依赖方向、公开-API-与循环依赖的工程约束"><a href="#Monorepo-包边界治理：依赖方向、公开-API-与循环依赖的工程约束" class="headerlink" title="Monorepo 包边界治理：依赖方向、公开 API 与循环依赖的工程约束"></a>Monorepo 包边界治理：依赖方向、公开 API 与循环依赖的工程约束</h1><p>把代码放进一个仓库不等于拥有了 monorepo 的收益。真正的收益来自「包」这个单位：独立的版本、独立的构建、独立的边界。如果边界可以随意穿透，monorepo 就退化成一个巨大的单包，只是多了一层目录。</p><h2 id="边界的第一性问题：依赖方向"><a href="#边界的第一性问题：依赖方向" class="headerlink" title="边界的第一性问题：依赖方向"></a>边界的第一性问题：依赖方向</h2><p>健康的依赖图是一张有向无环图，且方向单一。常见的分层是：</p><ul><li>基础设施层：类型、常量、工具函数，<strong>不依赖任何业务包</strong></li><li>领域层：领域模型与业务逻辑，只依赖基础设施层</li><li>应用层：页面、路由、API 入口，依赖上面两层</li><li>应用之间<strong>互不依赖</strong></li></ul><p>一旦出现反向依赖或同层互赖，任务图退化成链，缓存复用率骤降，语义也会混乱——比如工具包里开始出现业务判断。</p><p>判断一个包是否放错位置，有个简单测试：<strong>删掉它，其他包会不会因为缺少业务知识而无法编译？</strong> 如果会，说明业务知识泄漏进了基础层。</p><h2 id="公开-API-收敛：包只能从门口进出"><a href="#公开-API-收敛：包只能从门口进出" class="headerlink" title="公开 API 收敛：包只能从门口进出"></a>公开 API 收敛：包只能从门口进出</h2><p>边界不仅是「谁能依赖谁」，还包括「通过什么依赖」。常见问题是应用直接从 <code>packages/ui/src/components/button/internal-impl.tsx</code> 深链导入，绕过包的设计意图。</p><p>解决办法是强制入口收敛：</p><ul><li>每个包通过 <code>exports</code> 字段声明唯一入口</li><li>禁止 <code>@org/ui/src/*</code> 这类深链</li><li>内部文件放在约定目录（如 <code>src/internal/</code>），并用 lint 规则标记为私有</li></ul><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;@org/ui&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;exports&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;.&quot;</span><span class="punctuation">:</span> <span class="string">&quot;./src/index.ts&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;./styles&quot;</span><span class="punctuation">:</span> <span class="string">&quot;./src/styles/index.css&quot;</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;types&quot;</span><span class="punctuation">:</span> <span class="string">&quot;./src/index.ts&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p><code>exports</code> 一旦声明，Node 与现代打包器就会拒绝未列出的子路径，等于在运行时和构建期同时上了锁。这比只靠 code review 靠谱得多。</p><h2 id="用自动化检查守住边界"><a href="#用自动化检查守住边界" class="headerlink" title="用自动化检查守住边界"></a>用自动化检查守住边界</h2><p>人工守边界一定失守。可落地的检查有三类：</p><table><thead><tr><th>检查类型</th><th>工具方向</th><th>拦截时机</th><th>维护成本</th></tr></thead><tbody><tr><td>依赖方向白名单</td><td>ESLint 规则 &#x2F; dependency-cruiser</td><td>本地 + CI</td><td>低，规则集中维护</td></tr><tr><td>深链导入检测</td><td>ESLint <code>no-restricted-imports</code></td><td>本地 + CI</td><td>低</td></tr><tr><td>循环依赖检测</td><td>madge &#x2F; dependency-cruiser</td><td>CI 门禁</td><td>中</td></tr><tr><td>包体积与产物审计</td><td>自定义脚本</td><td>CI 门禁</td><td>中</td></tr><tr><td>依赖声明一致性</td><td>syncpack &#x2F; knip</td><td>CI 门禁</td><td>中</td></tr></tbody></table><p>经验上，前两类是必做的，投入很小但能挡住绝大多数越界。第三类在包数量超过 20 个后价值凸显。</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;rules&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;no-restricted-imports&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;error&quot;</span><span class="punctuation">,</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;patterns&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span></span><br><span class="line">        <span class="punctuation">&#123;</span> <span class="attr">&quot;group&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;@org/ui/src/*&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span> <span class="attr">&quot;message&quot;</span><span class="punctuation">:</span> <span class="string">&quot;请从包入口导入，不要深链 src&quot;</span> <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">        <span class="punctuation">&#123;</span> <span class="attr">&quot;group&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;@org/app-*&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span> <span class="attr">&quot;message&quot;</span><span class="punctuation">:</span> <span class="string">&quot;应用之间不允许互相依赖&quot;</span> <span class="punctuation">&#125;</span></span><br><span class="line">      <span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">]</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><h2 id="循环依赖：为什么必须零容忍"><a href="#循环依赖：为什么必须零容忍" class="headerlink" title="循环依赖：为什么必须零容忍"></a>循环依赖：为什么必须零容忍</h2><p>循环依赖在单包里可能只是警告，在 monorepo 里后果被放大：</p><ol><li>构建顺序无法确定，Turborepo 可能直接报错或行为不可预期</li><li>缓存键互相引用，任何一侧变动都使另一侧失效，缓存价值归零</li><li>拆包与发布时无法独立进行</li></ol><p>打破循环的常见手法，按代价从低到高：</p><ol><li>把共享类型抽到独立的 <code>types</code> 包（无运行时依赖，最安全）</li><li>用依赖注入替代直接引用，由应用层负责组装</li><li>定义接口包，双方依赖接口而非实现</li><li>重新划分领域，把一个包拆成两个方向严格单向的包</li></ol><p>第 1 种在大多数场景下就够用了。类型包没有副作用，不参与构建产物，进图也不会引入运行时耦合。</p><h2 id="取舍：包应该切多细"><a href="#取舍：包应该切多细" class="headerlink" title="取舍：包应该切多细"></a>取舍：包应该切多细</h2><p>切得过粗，缓存粒度大、改动影响面广；切得过细，包数量膨胀、跨包跳转成本高、配置维护量上升。</p><p>笔者的经验阈值：</p><ul><li>少于 3 个使用者，先不单独建包</li><li>一个包被改动后若影响超过一半的应用，考虑它是否过度通用</li><li>单个包的源码超过约千行且职责不单一，考虑拆分</li><li>包内出现两个以上明显独立的领域概念，应当拆</li></ul><p>还有一个常被忽略的成本：每个包都要维护 <code>package.json</code>、tsconfig、构建配置、lint 配置。能用约定和共享配置消除的部分，尽量用 <code>extends</code> 复用，避免配置文件数量随包数线性膨胀。</p><blockquote><p><strong>说明：</strong> 本文为原创技术分析，涉及的工具与配置写法为社区主流实践，具体规则与字段以各工具官方最新文档为准；文中的阈值类建议为笔者基于常见团队规模的主观经验判断，非硬性标准。</p></blockquote>]]>
    </content>
    <id>https://blogs.yzy15.dpdns.org/vc-monorepo-package-boundaries/</id>
    <link href="https://blogs.yzy15.dpdns.org/vc-monorepo-package-boundaries/"/>
    <published>2026-09-13T12:04:00.000Z</published>
    <summary>包切分容易，守住边界难。本文讨论如何用依赖方向规则、公开 API 收敛与自动化检查，把 monorepo 的包边界变成可执行的工程约束。</summary>
    <title>Monorepo 包边界治理：依赖方向、公开 API 与循环依赖的工程约束</title>
    <updated>2026-09-12T17:33:38.964Z</updated>
  </entry>
  <entry>
    <author>
      <name>mc-yzy15</name>
    </author>
    <category term="云计算" scheme="https://blogs.yzy15.dpdns.org/categories/%E4%BA%91%E8%AE%A1%E7%AE%97/"/>
    <category term="远程缓存" scheme="https://blogs.yzy15.dpdns.org/tags/%E8%BF%9C%E7%A8%8B%E7%BC%93%E5%AD%98/"/>
    <category term="Turborepo" scheme="https://blogs.yzy15.dpdns.org/tags/Turborepo/"/>
    <category term="CI 优化" scheme="https://blogs.yzy15.dpdns.org/tags/CI-%E4%BC%98%E5%8C%96/"/>
    <category term="工程效能" scheme="https://blogs.yzy15.dpdns.org/tags/%E5%B7%A5%E7%A8%8B%E6%95%88%E8%83%BD/"/>
    <content>
      <![CDATA[<h1 id="Turborepo-远程缓存落地的三个硬问题：隔离、命中率与命中失败"><a href="#Turborepo-远程缓存落地的三个硬问题：隔离、命中率与命中失败" class="headerlink" title="Turborepo 远程缓存落地的三个硬问题：隔离、命中率与命中失败"></a>Turborepo 远程缓存落地的三个硬问题：隔离、命中率与命中失败</h1><p>本地缓存只能加速你重复构建自己的分支。真正把 CI 时长压下来的是远程缓存：一个开发者或一条流水线算过的结果，整支团队都能复用。但远程缓存不是打开开关就完事，落地阶段总会撞上三个具体问题。</p><h2 id="问题一：缓存隔离，别让-feature-分支污染主干"><a href="#问题一：缓存隔离，别让-feature-分支污染主干" class="headerlink" title="问题一：缓存隔离，别让 feature 分支污染主干"></a>问题一：缓存隔离，别让 feature 分支污染主干</h2><p>远程缓存默认按「哈希 → 产物」存储，键里不包含分支信息。这意味着如果 A 分支构建了一个哈希为 <code>abc123</code> 的产物，B 分支算出同样的哈希，就会直接复用 A 的产物。</p><p>正常情况下这是好事：内容相同，产物自然相同。风险出现在两类场景：</p><ul><li><strong>产物含构建时环境差异</strong>：例如不同分支注入了不同的 API 地址，但环境变量没进 <code>env</code> 声明，哈希相同而产物应当不同。</li><li><strong>本地脏构建上传</strong>：开发者本地带着未提交改动构建并上传，恰好哈希与 CI 一致时，会把错误产物喂给流水线。</li></ul><p>防护手段有三层：</p><ol><li>把影响产物的所有环境变量写进 <code>env</code> &#x2F; <code>globalEnv</code>，这是根本。</li><li>在 CI 上统一使用 <code>--remote-cache-read-only</code> 以外的写入策略，禁止非受信环境上传。</li><li>对关键应用开启签名校验（如 <code>remoteCache.signature</code>），防止缓存被篡改。</li></ol><h2 id="问题二：命中率为什么上不去"><a href="#问题二：命中率为什么上不去" class="headerlink" title="问题二：命中率为什么上不去"></a>问题二：命中率为什么上不去</h2><p>打开远程缓存后发现命中率只有个位数，通常不是缓存坏了，而是哈希输入被无意放大了。</p><table><thead><tr><th>常见诱因</th><th>现象</th><th>修复方向</th></tr></thead><tbody><tr><td>未声明的环境变量</td><td>每次 CI 哈希都变</td><td>补进 env &#x2F; globalEnv</td></tr><tr><td>时间戳或随机数写进产物</td><td>命中率恒为 0</td><td>让构建产物确定性化</td></tr><tr><td>outputs 写成 <code>**</code> 或 <code>dist</code> 不带子路径</td><td>缓存体积巨大、上传慢</td><td>精确声明产物路径</td></tr><tr><td>lockfile 频繁变动</td><td>全局哈希变化</td><td>检查是否被无关依赖抖动</td></tr><tr><td>源码里引用绝对路径</td><td>本地与 CI 哈希不同</td><td>用相对路径与 rootDir 变量</td></tr></tbody></table><p>一个实用技巧是用 <code>--dry=json</code> 或 <code>--summarize</code> 看每个任务的哈希输入，对比两次运行差异。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 输出详细的任务摘要，用于定位哈希抖动</span></span><br><span class="line">turbo run build --dry=json &gt; run.json</span><br><span class="line"></span><br><span class="line"><span class="comment"># 对比两次运行的哈希，肉眼找出变化的任务</span></span><br><span class="line">node -e <span class="string">&quot;const a=require(&#x27;./run-a.json&#x27;),b=require(&#x27;./run-b.json&#x27;);\</span></span><br><span class="line"><span class="string">console.log(a.tasks.filter(t=&gt;!b.tasks.find(x=&gt;x.taskId===t.taskId&amp;&amp;x.hash===t.hash)).map(t=&gt;t.taskId))&quot;</span></span><br></pre></td></tr></table></figure><h2 id="问题三：缓存命中却构建失败"><a href="#问题三：缓存命中却构建失败" class="headerlink" title="问题三：缓存命中却构建失败"></a>问题三：缓存命中却构建失败</h2><p>这是最让人头疼的一类：日志显示 <code>cache hit</code>，但后续步骤缺文件或报错。绝大多数情况指向 <code>outputs</code> 与 <code>inputs</code> 声明不匹配。</p><p>Turborepo 的缓存只保存 <code>outputs</code> 里声明的东西。如果构建实际产出 <code>dist/client</code>、<code>dist/server</code>，而你只声明了 <code>dist</code>，恢复时不会丢文件（因为 <code>dist</code> 是前缀），但如果你声明了 <code>dist</code> 而实际产出在 <code>.next</code>，就会命中一个空缓存。</p><p>反向问题同样存在：<code>inputs</code> 没排除测试文件，导致改一个 <code>.test.ts</code> 就失效整条链。</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;tasks&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;build&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;inputs&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;src/**&quot;</span><span class="punctuation">,</span> <span class="string">&quot;package.json&quot;</span><span class="punctuation">,</span> <span class="string">&quot;tsconfig.json&quot;</span><span class="punctuation">,</span> <span class="string">&quot;!src/**/*.test.ts&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;outputs&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;dist/**&quot;</span><span class="punctuation">,</span> <span class="string">&quot;.next/**&quot;</span><span class="punctuation">,</span> <span class="string">&quot;!.next/cache/**&quot;</span><span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p><code>.next/cache</code> 本身是 Next.js 自己的增量缓存，放进 Turborepo 的 outputs 会让缓存包体积翻好几倍，收益却不明显，通常应排除。</p><h2 id="自建缓存服务还是用托管方案"><a href="#自建缓存服务还是用托管方案" class="headerlink" title="自建缓存服务还是用托管方案"></a>自建缓存服务还是用托管方案</h2><table><thead><tr><th>维度</th><th>Vercel 托管远程缓存</th><th>自建（S3 &#x2F; Redis 后端）</th></tr></thead><tbody><tr><td>接入成本</td><td>低，环境变量即用</td><td>需部署服务、配鉴权</td></tr><tr><td>跨团队共享</td><td>天然支持</td><td>需自行设计权限模型</td></tr><tr><td>数据合规</td><td>产物在第三方</td><td>完全自控</td></tr><tr><td>成本</td><td>随使用量与套餐计</td><td>存储与流量自付</td></tr><tr><td>可观测性</td><td>平台提供命中率指标</td><td>需自建监控</td></tr></tbody></table><p>选择的核心不是价格，而是「产物是否包含敏感数据」。如果构建产物里含有服务端密钥或客户数据，自建更稳妥；纯前端静态资源放托管通常更省心。</p><h2 id="维护远程缓存的三条日常纪律"><a href="#维护远程缓存的三条日常纪律" class="headerlink" title="维护远程缓存的三条日常纪律"></a>维护远程缓存的三条日常纪律</h2><ul><li><strong>定期轮换缓存</strong>：schema 大改或依赖大版本升级后，主动失效一次，避免旧产物污染新结构。</li><li><strong>监控缓存体积增长</strong>：缓存只增不减会推高存储与恢复耗时，按周看增长曲线。</li><li><strong>把命中率当度量指标</strong>：在 CI 里记录 <code>cache hit / miss</code> 比例，命中率突然下跌通常是哈希抖动的早期信号。</li></ul><blockquote><p><strong>说明：</strong> 本文为原创技术分析，配置项名称与行为基于 Turborepo 公开文档与常见实践归纳，具体参数以官方最新文档为准；文中数值均为量级描述，非精确基准测试结果。结论含主观判断成分。</p></blockquote>]]>
    </content>
    <id>https://blogs.yzy15.dpdns.org/vc-monorepo-remote-cache-ops/</id>
    <link href="https://blogs.yzy15.dpdns.org/vc-monorepo-remote-cache-ops/"/>
    <published>2026-09-13T12:02:00.000Z</published>
    <summary>远程缓存能显著缩短 CI 时长，但落地时会遇到缓存隔离、命中率低、缓存污染三类硬问题。本文给出可操作的排查路径与配置要点。</summary>
    <title>Turborepo 远程缓存落地的三个硬问题：隔离、命中率与命中失败</title>
    <updated>2026-09-12T17:33:38.964Z</updated>
  </entry>
  <entry>
    <author>
      <name>mc-yzy15</name>
    </author>
    <category term="云计算" scheme="https://blogs.yzy15.dpdns.org/categories/%E4%BA%91%E8%AE%A1%E7%AE%97/"/>
    <category term="Monorepo" scheme="https://blogs.yzy15.dpdns.org/tags/Monorepo/"/>
    <category term="Turborepo" scheme="https://blogs.yzy15.dpdns.org/tags/Turborepo/"/>
    <category term="工程效能" scheme="https://blogs.yzy15.dpdns.org/tags/%E5%B7%A5%E7%A8%8B%E6%95%88%E8%83%BD/"/>
    <category term="构建系统" scheme="https://blogs.yzy15.dpdns.org/tags/%E6%9E%84%E5%BB%BA%E7%B3%BB%E7%BB%9F/"/>
    <content>
      <![CDATA[<h1 id="Turborepo-任务图怎么设计：从-workspace-拓扑到增量构建"><a href="#Turborepo-任务图怎么设计：从-workspace-拓扑到增量构建" class="headerlink" title="Turborepo 任务图怎么设计：从 workspace 拓扑到增量构建"></a>Turborepo 任务图怎么设计：从 workspace 拓扑到增量构建</h1><p>在 monorepo 里跑构建，最费时间的往往不是编译本身，而是「不知道该跑什么、该跑什么顺序」。Turborepo 的核心价值就是把这件事从人工脚本里抽出来，交给一个基于依赖关系的任务图（Task Graph）来自动决策。理解这张图是怎么被推导出来的，比记住几个命令重要得多。</p><h2 id="任务图从哪里来：workspace-拓扑是地基"><a href="#任务图从哪里来：workspace-拓扑是地基" class="headerlink" title="任务图从哪里来：workspace 拓扑是地基"></a>任务图从哪里来：workspace 拓扑是地基</h2><p>Turborepo 并不会猜测你的结构。它读取两样东西：<code>pnpm-workspace.yaml</code>（或 package.json 的 workspaces）声明的包列表，以及每个包 <code>package.json</code> 里的 <code>dependencies</code> &#x2F; <code>devDependencies</code>。</p><p>当 <code>apps/web</code> 依赖 <code>packages/ui</code>，Turborepo 就知道：跑 <code>web</code> 的 build 之前，<code>ui</code> 的 build 必须完成。这种「包级别」的依赖构成了图的骨架，通常称为拓扑（topology）。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看 Turborepo 推导出的完整任务图</span></span><br><span class="line">turbo run build --graph</span><br><span class="line"></span><br><span class="line"><span class="comment"># 只看某个包及其依赖子图</span></span><br><span class="line">turbo run build --graph=web.dot --filter=web</span><br></pre></td></tr></table></figure><p><code>--graph</code> 会导出一份 Graphviz dot 文件，把图渲染出来是排查「为什么某个包被重复构建」最直接的手段。很多团队觉得 CI 慢，实际是拓扑里藏了一条不该存在的依赖边。</p><h2 id="pipeline-与-dependsOn：把语义告诉构建器"><a href="#pipeline-与-dependsOn：把语义告诉构建器" class="headerlink" title="pipeline 与 dependsOn：把语义告诉构建器"></a>pipeline 与 dependsOn：把语义告诉构建器</h2><p><code>turbo.json</code> 的 <code>pipeline</code>（新版本里叫 <code>tasks</code>）字段用来声明「一个任务内部还要依赖哪些任务」。这层信息无法从 package.json 自动推导，必须手动声明。</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;$schema&quot;</span><span class="punctuation">:</span> <span class="string">&quot;https://turbo.build/schema.json&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;tasks&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;build&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;dependsOn&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;^build&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;outputs&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;dist/**&quot;</span><span class="punctuation">,</span> <span class="string">&quot;.next/**&quot;</span><span class="punctuation">,</span> <span class="string">&quot;!.next/cache/**&quot;</span><span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;test&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;dependsOn&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="string">&quot;build&quot;</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;outputs&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">]</span></span><br><span class="line">    <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;lint&quot;</span><span class="punctuation">:</span> <span class="punctuation">&#123;</span></span><br><span class="line">      <span class="attr">&quot;dependsOn&quot;</span><span class="punctuation">:</span> <span class="punctuation">[</span><span class="punctuation">]</span><span class="punctuation">,</span></span><br><span class="line">      <span class="attr">&quot;cache&quot;</span><span class="punctuation">:</span> <span class="literal"><span class="keyword">true</span></span></span><br><span class="line">    <span class="punctuation">&#125;</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>这里有两个细节最容易出错：</p><ul><li><code>&quot;^build&quot;</code> 中的 <code>^</code> 表示<strong>上游依赖包的 build</strong>，是拓扑方向；写成 <code>&quot;build&quot;</code> 则表示<strong>同一个包内的 build</strong>，是先后置任务。</li><li><code>outputs</code> 决定了缓存要保存什么。建得不对，缓存命中后目录是空的，构建照样重跑。</li></ul><p>一句话经验：<code>^</code> 管跨包，无 <code>^</code> 管包内。</p><h2 id="增量构建与缓存命中：图是静态的，缓存是动态的"><a href="#增量构建与缓存命中：图是静态的，缓存是动态的" class="headerlink" title="增量构建与缓存命中：图是静态的，缓存是动态的"></a>增量构建与缓存命中：图是静态的，缓存是动态的</h2><p>任务图在一次运行内是相对静态的，但每个节点是否真正执行，取决于内容哈希。Turborepo 会为每个任务计算一个哈希，输入包括：任务命令、环境变量（<code>env</code> 声明的）、相关源文件内容、以及上游包的哈希。</p><table><thead><tr><th>变更类型</th><th>是否影响下游哈希</th><th>典型后果</th></tr></thead><tbody><tr><td>修改 packages&#x2F;ui 源码</td><td>是</td><td>ui 与所有依赖它的应用全部重跑</td></tr><tr><td>只改 apps&#x2F;web 的一个页面</td><td>否</td><td>仅 web 本身重跑</td></tr><tr><td>新增一个未声明的环境变量</td><td>否（除非列入 env）</td><td>缓存命中但行为错误，属于隐性 bug</td></tr><tr><td>改动 turbo.json 中 outputs</td><td>是</td><td>全量失效，需要重建</td></tr><tr><td>升级共享的 tsconfig</td><td>是</td><td>视是否被读取为输入而定</td></tr></tbody></table><p>第三行是最危险的：环境变量如果不写进 <code>env</code> 或 <code>globalEnv</code>，本地构建出来的产物和 CI 上可能完全不同，而缓存会把这个错误稳定复现。</p><h2 id="并行度与并发控制：图越宽越快，但别把机器打满"><a href="#并行度与并发控制：图越宽越快，但别把机器打满" class="headerlink" title="并行度与并发控制：图越宽越快，但别把机器打满"></a>并行度与并发控制：图越宽越快，但别把机器打满</h2><p>任务图的执行是并行化的：同层无依赖的节点可以同时跑。Turborepo 默认按 CPU 核数决定并发度，也可以用 <code>--concurrency</code> 手动指定。</p><p>需要注意 IO 密集与 CPU 密集任务混跑的问题。Next.js 构建会大量占用 CPU 和内存，同时跑五个应用级 build 往往让单机 swap，反而比串行更慢。更稳的做法是分组：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 先并行跑所有库的构建（快、无服务依赖）</span></span><br><span class="line">turbo run build --filter=<span class="string">&quot;./packages/*&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 再按应用逐个跑，控制内存峰值</span></span><br><span class="line">turbo run build --filter=<span class="string">&quot;./apps/*&quot;</span> --concurrency=2</span><br></pre></td></tr></table></figure><p>在 Vercel 上，构建机规格由所选套餐决定，并发过高会导致 OOM 被杀。把并发当作需要观测的调参项，而不是越大越好。</p><h2 id="常见图设计反模式"><a href="#常见图设计反模式" class="headerlink" title="常见图设计反模式"></a>常见图设计反模式</h2><p>以下几种结构几乎一定会在规模上来后拖慢流水线：</p><ol><li><strong>万能 shared 包</strong>：把工具函数、类型、常量、组件全塞进一个 <code>packages/shared</code>，导致任何小改动都让整张图重跑。应按领域切分。</li><li><strong>应用之间互相依赖</strong>：<code>apps/a</code> 依赖 <code>apps/b</code>，拓扑立刻退化成链，并行度归零。应用之间应通过接口而非源码依赖。</li><li><strong>循环依赖</strong>：pnpm 会报警但未必阻断，Turborepo 可能报无法排序。必须在 CI 加检测。</li><li><strong>devDependencies 里放运行时依赖</strong>：构建顺序看起来对，但裁剪产物时会缺依赖。</li><li><strong>只在根 package.json 里放脚本</strong>：任务粒度太粗，缓存复用率直线下降。</li></ol><h2 id="取舍：任务图越精细越好吗"><a href="#取舍：任务图越精细越好吗" class="headerlink" title="取舍：任务图越精细越好吗"></a>取舍：任务图越精细越好吗</h2><p>精细化能提高缓存命中率，但会带来维护成本。包切得越碎，<code>turbo.json</code> 越复杂，开发者越难判断「我改了这个文件到底会影响什么」。</p><p>笔者的建议是分层：稳定的基础设施（lint、typecheck）保持全局单任务；业务包按领域划分为 5~15 个；应用层作为图的出口，不反向被依赖。这个粒度在几十人团队里通常够用，再往上就该考虑远程缓存与分布式执行了。</p><blockquote><p><strong>说明：</strong> 本文为原创技术分析，所述 Turborepo 行为基于其公开文档与常见实践归纳，具体字段名（如 <code>pipeline</code> 与 <code>tasks</code>）随版本演进以官方最新文档为准；文中性能表述为量级描述，非精确基准数据。观点部分为主观判断，不代表 Vercel 官方立场。</p></blockquote>]]>
    </content>
    <id>https://blogs.yzy15.dpdns.org/vc-monorepo-task-graph-design/</id>
    <link href="https://blogs.yzy15.dpdns.org/vc-monorepo-task-graph-design/"/>
    <published>2026-09-13T12:00:00.000Z</published>
    <summary>拆解 Turborepo 如何根据 workspace 依赖自动推导任务图，讲清 dependsOn、拓扑依赖与增量构建的协作机制，并给出在 Vercel 上合理设计任务图的实践建议。</summary>
    <title>Turborepo 任务图怎么设计：从 workspace 拓扑到增量构建</title>
    <updated>2026-09-12T17:33:38.964Z</updated>
  </entry>
  <entry>
    <author>
      <name>mc-yzy15</name>
    </author>
    <category term="云计算" scheme="https://blogs.yzy15.dpdns.org/categories/%E4%BA%91%E8%AE%A1%E7%AE%97/"/>
    <category term="边缘计算" scheme="https://blogs.yzy15.dpdns.org/tags/%E8%BE%B9%E7%BC%98%E8%AE%A1%E7%AE%97/"/>
    <category term="Vercel" scheme="https://blogs.yzy15.dpdns.org/tags/Vercel/"/>
    <category term="对象存储" scheme="https://blogs.yzy15.dpdns.org/tags/%E5%AF%B9%E8%B1%A1%E5%AD%98%E5%82%A8/"/>
    <category term="KV存储" scheme="https://blogs.yzy15.dpdns.org/tags/KV%E5%AD%98%E5%82%A8/"/>
    <content>
      <![CDATA[<h1 id="Blob、KV-与-Edge-Config-的选型对比：三种”边缘存储”到底该用在哪儿"><a href="#Blob、KV-与-Edge-Config-的选型对比：三种”边缘存储”到底该用在哪儿" class="headerlink" title="Blob、KV 与 Edge Config 的选型对比：三种”边缘存储”到底该用在哪儿"></a>Blob、KV 与 Edge Config 的选型对比：三种”边缘存储”到底该用在哪儿</h1><p>很多团队在 Vercel 上做第一个”真正有状态”的功能时，都会遇到同一个困惑：Blob、KV、Edge Config 看起来都是”托管的存储服务”，都能在 Serverless 函数里 <code>import</code> 一下就用了，那为什么不干脆选一个用到底？</p><p>答案藏在它们的存储模型里。这三种服务在设计之初就对应了完全不同的数据形态和访问模式，如果混用，轻则成本翻倍，重则写出”看着能跑、上线就出错”的一致性问题。本文不罗列 API，而是从数据建模的角度把它们拆开讲清楚。</p><h2 id="先分清三种数据形态"><a href="#先分清三种数据形态" class="headerlink" title="先分清三种数据形态"></a>先分清三种数据形态</h2><p>理解这三者的最快方式，是问自己一个问题：<strong>你的数据是”文件”、”键值对”还是”配置”？</strong></p><ul><li><strong>Blob</strong> 面向的是<strong>对象</strong>：图片、视频、PDF、用户上传的任意二进制文件。它的心智模型接近 S3 桶——写入后得到一个不可变的 URL，删除是显式的。</li><li><strong>KV</strong> 面向的是<strong>可变的小值</strong>：会话、计数器、限流状态、功能开关的运行时快照。它是 Redis 语义的托管子集，强调低延迟读写。</li><li><strong>Edge Config</strong> 面向的是<strong>几乎不变的配置</strong>：路由规则、A&#x2F;B 实验分组表、上游服务地址名单、白名单。它的读延迟极低，但写入是”发布式”的，而不是”事务式”的。</li></ul><p>把这三句话记住，后面所有的取舍推导都会自然很多。反过来说，绝大多数踩坑都可以归结为把配置塞进了 KV，或者把会话塞进了 Edge Config。</p><h2 id="Blob：为不可变对象而生的分发层"><a href="#Blob：为不可变对象而生的分发层" class="headerlink" title="Blob：为不可变对象而生的分发层"></a>Blob：为不可变对象而生的分发层</h2><p>Blob 的核心设计取向是”写一次、读无数次”。当你通过 <code>put()</code> 上传一个文件时，系统返回的是带随机后缀的 URL，这个 URL 在对象被删除前保持稳定。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; put, list, del &#125; <span class="keyword">from</span> <span class="string">&#x27;@vercel/blob&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 上传：让服务端生成随机文件名，避免用户可控路径</span></span><br><span class="line"><span class="keyword">const</span> blob = <span class="keyword">await</span> <span class="title function_">put</span>(<span class="string">`uploads/<span class="subst">$&#123;crypto.randomUUID()&#125;</span>.png`</span>, file, &#123;</span><br><span class="line">  <span class="attr">access</span>: <span class="string">&#x27;public&#x27;</span>,</span><br><span class="line">  <span class="attr">addRandomSuffix</span>: <span class="literal">true</span>,</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 列举某个前缀下的对象</span></span><br><span class="line"><span class="keyword">const</span> &#123; blobs &#125; = <span class="keyword">await</span> <span class="title function_">list</span>(&#123; <span class="attr">prefix</span>: <span class="string">&#x27;uploads/&#x27;</span> &#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 显式删除</span></span><br><span class="line"><span class="keyword">await</span> <span class="title function_">del</span>(blob.<span class="property">url</span>);</span><br></pre></td></tr></table></figure><p>几个工程上必须注意的点：</p><ol><li><strong>不要把用户输入直接拼进路径</strong>。路径穿越和覆盖攻击在对象存储上同样成立，用 <code>crypto.randomUUID()</code> 生成文件名，原始文件名只存在数据库里。</li><li><strong>不要在上传路径里做鉴权之外的判断</strong>。Blob 本身的访问控制粒度有限，敏感文件应该走私有访问 + 服务端签名。</li><li><strong>删除和列举是最终一致的</strong>。刚 <code>del()</code> 掉的对象可能在 <code>list()</code> 里短暂出现，别把 <code>list()</code> 当数据库查询用。</li></ol><p>Blob 的计费通常拆成三块：存储容量、读写操作次数、以及出网流量。前两者量级很小，真正贵的是<strong>流量</strong>。如果你的图片没有经过优化就直接分发原图，账单会以你意想不到的速度增长——务必配合 <code>next/image</code> 之类的优化层使用。</p><h2 id="KV：低延迟可变状态，但一致性有边界"><a href="#KV：低延迟可变状态，但一致性有边界" class="headerlink" title="KV：低延迟可变状态，但一致性有边界"></a>KV：低延迟可变状态，但一致性有边界</h2><p>KV 的价值在于延迟。把会话或限流计数放在 KV 里，函数在冷启动之后的读取通常是个位数毫秒级的，这对边缘运行时尤其重要——你不可能为了读一个 session 去跨区域回源到某个单区域数据库。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; kv &#125; <span class="keyword">from</span> <span class="string">&#x27;@vercel/kv&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">rateLimit</span>(<span class="params"><span class="attr">ip</span>: <span class="built_in">string</span>, limit = <span class="number">60</span></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> key = <span class="string">`rl:<span class="subst">$&#123;ip&#125;</span>`</span>;</span><br><span class="line">  <span class="comment">// 原子的自增 + 过期，避免&quot;读-判断-写&quot;的竞态</span></span><br><span class="line">  <span class="keyword">const</span> count = <span class="keyword">await</span> kv.<span class="title function_">incr</span>(key);</span><br><span class="line">  <span class="keyword">if</span> (count === <span class="number">1</span>) &#123;</span><br><span class="line">    <span class="keyword">await</span> kv.<span class="title function_">expire</span>(key, <span class="number">60</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> count &lt;= limit;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>上面这段代码的写法很关键：<strong>用 <code>INCR</code> 而不是 <code>GET</code> 再 <code>SET</code></strong>。后者看起来更直观，但在并发场景下会丢计数，这是限流最经典的 bug。</p><p>KV 的一致性模型需要特别说明。它是<strong>最终一致</strong>的：一次写操作在源区域确认之后，其他边缘节点的读取可能会有短暂的陈旧窗口。这个窗口通常是毫秒到秒的量级，具体以实现和官方文档为准。由此推导出两条实践规则：</p><ul><li><strong>不要用 KV 做需要读己之写（read-your-writes）的场景</strong>。用户刚提交的表单立刻在同一次交互里查，可能查不到。</li><li><strong>不要在 KV 上做需要跨键原子性的业务</strong>。虽然没有事务，但单键的 <code>INCR</code>、<code>SETNX</code> 这类操作是原子的，够用的场景其实很多——关键是设计时别指望多键一致。</li></ul><h2 id="Edge-Config：读得极快，但写入是一等公民之外的少数派"><a href="#Edge-Config：读得极快，但写入是一等公民之外的少数派" class="headerlink" title="Edge Config：读得极快，但写入是一等公民之外的少数派"></a>Edge Config：读得极快，但写入是一等公民之外的少数派</h2><p>Edge Config 的定位非常明确：<strong>极小、极少变、读极多</strong>的数据。它的读取被内联到边缘网络的配置分发路径上，延迟可以低到几乎没有网络往返的感觉——通常在个位数毫秒甚至更低，具体以官方文档为准。</p><p>典型场景是功能开关和实验分组：</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; createClient &#125; <span class="keyword">from</span> <span class="string">&#x27;@vercel/edge-config&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> edgeConfig = <span class="title function_">createClient</span>(process.<span class="property">env</span>.<span class="property">EDGE_CONFIG</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">middleware</span>(<span class="params"><span class="attr">req</span>: <span class="title class_">Request</span></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> flags = <span class="keyword">await</span> edgeConfig.<span class="property">get</span>&lt;<span class="title class_">Record</span>&lt;<span class="built_in">string</span>, <span class="built_in">boolean</span>&gt;&gt;(<span class="string">&#x27;flags&#x27;</span>);</span><br><span class="line">  <span class="keyword">if</span> (flags?.<span class="property">newCheckout</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Response</span>(<span class="string">&#x27;checkout-v2&#x27;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Response</span>(<span class="string">&#x27;checkout-v1&#x27;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>但请注意它的三个硬约束，这些往往是踩坑的根源：</p><table><thead><tr><th>维度</th><th>Edge Config</th><th>KV</th><th>Blob</th></tr></thead><tbody><tr><td>数据形态</td><td>小型配置项</td><td>键值对 &#x2F; 计数器</td><td>二进制对象</td></tr><tr><td>单值大小上限</td><td>很小（KB 量级）</td><td>较大（MB 量级）</td><td>很大（GB 量级）</td></tr><tr><td>读延迟</td><td>极低</td><td>低</td><td>一次 HTTPS 往返</td></tr><tr><td>写延迟</td><td>秒级生效</td><td>毫秒级</td><td>立即</td></tr><tr><td>一致性</td><td>发布式，有传播延迟</td><td>最终一致</td><td>强一致（单对象）</td></tr><tr><td>适用写入频率</td><td>每天几次</td><td>每秒数千次</td><td>任意</td></tr><tr><td>典型用途</td><td>功能开关、路由表</td><td>会话、限流</td><td>图片、附件</td></tr></tbody></table><p>Edge Config 的写操作不是”改一个值”这么轻，它更像一次<strong>配置发布</strong>。如果你打算每秒写十几次，选错服务了——那是 KV 的活。反过来说，如果你的功能开关几秒钟才生效，业务上完全无感，但换来了中间件里几乎零成本的读取，这个交换非常划算。</p><h2 id="决策路径：从数据特征反推服务"><a href="#决策路径：从数据特征反推服务" class="headerlink" title="决策路径：从数据特征反推服务"></a>决策路径：从数据特征反推服务</h2><p>与其记服务名，不如记住这条判断链：</p><ol><li><strong>是二进制文件吗？</strong> 是 → Blob。文件大小、是否公开、是否需要 CDN 缓存决定了你的访问模式。</li><li><strong>是可变的运行时状态吗？</strong> 是 → KV。但要确认你能接受最终一致，并且不需要跨键事务。</li><li><strong>是全局共享、变化极少的读重度数据吗？</strong> 是 → Edge Config。</li><li><strong>以上都不是？</strong> 那么你大概率需要的是一个真正的数据库（Postgres &#x2F; SQLite 等），而不是这三者中的任何一个。</li></ol><p>第 4 条值得单独强调。很多团队用 KV 硬扛关系型数据，用 <code>SCAN</code> 模拟查询，最后在数据量上来之后陷入困境。这三者是<strong>补充层</strong>，不是数据库的替代品。</p><blockquote><p><strong>经验法则：</strong> 把 Blob 当”文件柜”、KV 当”便签纸”、Edge Config 当”公告板”。便签纸可以随时撕掉重写，公告板贴上去要等一会儿大家才看到，而文件柜里的东西一旦放进去了就最好别乱动。</p></blockquote><h2 id="组合使用才是常态"><a href="#组合使用才是常态" class="headerlink" title="组合使用才是常态"></a>组合使用才是常态</h2><p>真实的架构几乎从不会只用一种。一个典型的电商站点可能是这样分工的：</p><ul><li>商品图与用户头像 → <strong>Blob</strong></li><li>购物车与会话 → <strong>KV</strong></li><li>灰度开关与推荐算法参数 → <strong>Edge Config</strong></li><li>订单与库存 → <strong>托管 Postgres</strong></li></ul><p>关键在于让每一层承担它最擅长的事，并明确它在一致性上的承诺。当你在设计评审里被问到”这个数据为什么放在 KV 而不是 Edge Config”时，你能答出”因为它每秒写几十次，且需要毫秒级读己之写”，那这次选型就是站得住的。</p><p>反过来，如果答案是”因为当时 KV 先建好了”，那就值得在下一个迭代里重新审视一遍。存储选型的成本，往往不在于第一次写代码，而在于三年后你想改的时候发现到处都在依赖那个错误的一致性假设。</p><blockquote><p><strong>说明：</strong> 本文涉及的产品能力、配额与计费口径以 Vercel 官方最新文档为准；文中的延迟与一致性描述为架构层面的量级判断，实际数值会随区域、套餐与实现演进变化。代码示例为示意性写法，生产环境请结合项目实际的鉴权与错误处理策略调整。</p></blockquote>]]>
    </content>
    <id>https://blogs.yzy15.dpdns.org/vc-blob-kv-edge-config-selection/</id>
    <link href="https://blogs.yzy15.dpdns.org/vc-blob-kv-edge-config-selection/"/>
    <published>2026-09-13T10:00:00.000Z</published>
    <summary>用一张决策表把 Vercel Blob、KV 与 Edge Config 的存储模型、读写语义、成本结构与失效边界讲清楚，帮助你在真实项目里选对那一层存储，而不是把所有数据都塞进同一种服务。</summary>
    <title>Blob、KV 与 Edge Config 的选型对比：三种&quot;边缘存储&quot;到底该用在哪儿</title>
    <updated>2026-09-12T17:33:38.963Z</updated>
  </entry>
  <entry>
    <author>
      <name>mc-yzy15</name>
    </author>
    <category term="云计算" scheme="https://blogs.yzy15.dpdns.org/categories/%E4%BA%91%E8%AE%A1%E7%AE%97/"/>
    <category term="Cloudflare" scheme="https://blogs.yzy15.dpdns.org/tags/Cloudflare/"/>
    <category term="迁移" scheme="https://blogs.yzy15.dpdns.org/tags/%E8%BF%81%E7%A7%BB/"/>
    <category term="Netlify" scheme="https://blogs.yzy15.dpdns.org/tags/Netlify/"/>
    <category term="静态站点" scheme="https://blogs.yzy15.dpdns.org/tags/%E9%9D%99%E6%80%81%E7%AB%99%E7%82%B9/"/>
    <content>
      <![CDATA[<h1 id="从-Netlify-迁移到-Cloudflare：静态站与函数的分手清单"><a href="#从-Netlify-迁移到-Cloudflare：静态站与函数的分手清单" class="headerlink" title="从 Netlify 迁移到 Cloudflare：静态站与函数的分手清单"></a>从 Netlify 迁移到 Cloudflare：静态站与函数的分手清单</h1><p>如果你的站点是 Astro、Hugo、Jekyll 或其他 SSG 产物，挂在 Netlify 上，迁移到 Cloudflare 的难度远低于从 Vercel 迁移。原因很简单：静态站的抽象层最薄，平台差异主要体现在构建配置和几个附加服务的命名上。真正会卡住人的，往往是那些”Netlify 替你默默做了”的隐形能力。</p><h2 id="迁移前的能力盘点"><a href="#迁移前的能力盘点" class="headerlink" title="迁移前的能力盘点"></a>迁移前的能力盘点</h2><p>Netlify 把构建、部署、重定向、表单、身份、边缘函数打包成了一个整体体验。Cloudflare 把对应能力拆成了 Pages、Workers、Access、Turnstile 等独立产品。拆开之后你可以只用需要的部分，但也意味着要多做几次配置。</p><table><thead><tr><th>Netlify 能力</th><th>Cloudflare 对应</th><th>需要额外做的事</th></tr></thead><tbody><tr><td>Build &amp; Deploy</td><td>Pages 构建流水线</td><td>指定构建镜像与 Node 版本</td></tr><tr><td><code>_redirects</code> 文件</td><td><code>_redirects</code> 兼容支持</td><td>优先级与通配略有差异</td></tr><tr><td>Netlify Functions</td><td>Pages Functions &#x2F; Workers</td><td>目录约定不同</td></tr><tr><td>Netlify Identity</td><td>Cloudflare Access</td><td>定价模型与用户体系需重建</td></tr><tr><td>Netlify Forms</td><td>Workers + 邮件&#x2F;存储服务</td><td>需要自己写接收端点</td></tr><tr><td>Netlify Edge Handlers</td><td>Workers</td><td>需改写为 fetch 处理器</td></tr><tr><td>Deploy Previews</td><td>Pages 预览部署</td><td>几乎等价，成本更低</td></tr></tbody></table><p>其中表单和身份是最容易低估的两项。Netlify Forms 是”零代码”能力，换成 Cloudflare 后你必须自己拿一个 Worker 接住 POST、做校验、落库或发信。</p><h2 id="构建配置的逐项对齐"><a href="#构建配置的逐项对齐" class="headerlink" title="构建配置的逐项对齐"></a>构建配置的逐项对齐</h2><p>Netlify 用 <code>netlify.toml</code> 描述构建；Pages 用 <code>wrangler.toml</code> 加面板或 Git 集成。把这两者对齐是迁移的第一步。</p><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># wrangler.toml —— Pages 项目的最小配置</span></span><br><span class="line"><span class="attr">name</span> = <span class="string">&quot;blog-static-site&quot;</span></span><br><span class="line"><span class="attr">pages_build_output_dir</span> = <span class="string">&quot;dist&quot;</span></span><br><span class="line"><span class="attr">compatibility_date</span> = <span class="string">&quot;2026-09-01&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="section">[vars]</span></span><br><span class="line"><span class="attr">SITE_ENV</span> = <span class="string">&quot;production&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 本地开发时的绑定</span></span><br><span class="line"><span class="section">[[kv_namespaces]]</span></span><br><span class="line"><span class="attr">binding</span> = <span class="string">&quot;CACHE_KV&quot;</span></span><br><span class="line"><span class="attr">id</span> = <span class="string">&quot;REPLACE_WITH_REAL_ID&quot;</span></span><br></pre></td></tr></table></figure><p>对应地，构建命令要在 Pages 面板里显式声明，例如 <code>pnpm build</code>，并在环境变量里锁定 <code>NODE_VERSION</code>。这一步看起来琐碎，但它是”构建在 CI 上失败、本地却成功”类问题的最大来源。</p><p>重定向规则的差异值得单独提。Netlify 的 <code>_redirects</code> 支持 <code>/*</code> 通配和 <code>200</code> 强制改写；Pages 对 <code>_redirects</code> 的支持覆盖了常见语法，但通配符的贪婪匹配顺序仍按行序，遇到多条规则命中时要按”更具体在前”的原则排列。</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"># public/_redirects</span><br><span class="line">/old-blog/*    /posts/:splat    301</span><br><span class="line">/api/legacy/*  /api/v2/:splat   200</span><br></pre></td></tr></table></figure><h2 id="边缘函数与表单的替代实现"><a href="#边缘函数与表单的替代实现" class="headerlink" title="边缘函数与表单的替代实现"></a>边缘函数与表单的替代实现</h2><p>Netlify Functions 的目录约定是 <code>netlify/functions/</code>，Pages Functions 是 <code>functions/</code>，且通过文件路径映射路由。迁移时不要试图做”一对一自动转换”，而是按路由重新设计。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// functions/api/contact.ts —— 替代 Netlify Forms 的接收端点</span></span><br><span class="line"><span class="keyword">interface</span> <span class="title class_">Env</span> &#123;</span><br><span class="line">  <span class="attr">CONTACT_QUEUE</span>: <span class="title class_">Queue</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">const</span> <span class="attr">onRequestPost</span>: <span class="title class_">PagesFunction</span>&lt;<span class="title class_">Env</span>&gt; = <span class="title function_">async</span> (context) =&gt; &#123;</span><br><span class="line">  <span class="keyword">const</span> form = <span class="keyword">await</span> context.<span class="property">request</span>.<span class="title function_">formData</span>();</span><br><span class="line">  <span class="keyword">const</span> email = <span class="title class_">String</span>(form.<span class="title function_">get</span>(<span class="string">&quot;email&quot;</span>) ?? <span class="string">&quot;&quot;</span>).<span class="title function_">trim</span>();</span><br><span class="line">  <span class="keyword">const</span> message = <span class="title class_">String</span>(form.<span class="title function_">get</span>(<span class="string">&quot;message&quot;</span>) ?? <span class="string">&quot;&quot;</span>).<span class="title function_">trim</span>();</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 基础校验：拒绝空值与超长输入</span></span><br><span class="line">  <span class="keyword">if</span> (!email || email.<span class="property">length</span> &gt; <span class="number">254</span> || message.<span class="property">length</span> &gt; <span class="number">5000</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="title class_">Response</span>.<span class="title function_">json</span>(&#123; <span class="attr">ok</span>: <span class="literal">false</span>, <span class="attr">error</span>: <span class="string">&quot;invalid_input&quot;</span> &#125;, &#123; <span class="attr">status</span>: <span class="number">400</span> &#125;);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">await</span> context.<span class="property">env</span>.<span class="property">CONTACT_QUEUE</span>.<span class="title function_">send</span>(&#123;</span><br><span class="line">    email,</span><br><span class="line">    message,</span><br><span class="line">    <span class="attr">receivedAt</span>: <span class="keyword">new</span> <span class="title class_">Date</span>().<span class="title function_">toISOString</span>(),</span><br><span class="line">  &#125;);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="title class_">Response</span>.<span class="title function_">json</span>(&#123; <span class="attr">ok</span>: <span class="literal">true</span> &#125;, &#123; <span class="attr">status</span>: <span class="number">202</span> &#125;);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>要点有三个：一是入参必须做长度与格式校验，因为边缘端点直接暴露在公网；二是把耗时逻辑丢进队列而不是在请求里同步处理，避免 CPU 时间超限；三是返回结构化错误码，方便前端做差异化提示。</p><p>如果你原本依赖 Netlify Identity 做登录，迁移到 Cloudflare Access 意味着用户体系要重建。Access 更偏向”企业内部应用的门禁”，而非面向消费者的注册登录。面向公众的站点如果用它，通常需要配合自己的用户表，把 Access 当作一层边缘鉴权，而不是完整的身份系统。</p><h2 id="迁移顺序与验收标准"><a href="#迁移顺序与验收标准" class="headerlink" title="迁移顺序与验收标准"></a>迁移顺序与验收标准</h2><p>推荐按下面顺序推进，每一步都有明确的验收项：</p><ol><li><strong>静态产物上线</strong>：Pages 预览部署能正常渲染，验收项是首屏 LCP 与 Netlify 版本差异在可接受范围内。</li><li><strong>重定向等价性</strong>：抓取站点全部内链与已知外链，逐条比对状态码，验收项是零意外的 404。</li><li><strong>函数端点切换</strong>：先切只读接口，验收项是错误率与 P95 延迟不劣化。</li><li><strong>写路径与表单</strong>：最后切，验收项是端到端提交链路可用且有告警。</li><li><strong>DNS 切换</strong>：降低 TTL 后再切，验收项是解析全球生效。</li><li><strong>旧平台观察期</strong>：保留一个发布周期，验收项是无流量后下线。</li></ol><blockquote><p>提醒：Netlify 的 Deploy Previews 与 Pages 预览部署在体验上很接近，但预览环境的环境变量隔离策略不同。别在预览环境里放生产机密，否则迁移过程本身就是一次泄露风险。</p></blockquote><h2 id="常见坑与应对"><a href="#常见坑与应对" class="headerlink" title="常见坑与应对"></a>常见坑与应对</h2><ul><li><strong>构建缓存路径不同</strong>：依赖缓存的命中率下降会导致构建变慢，必要时在 CI 里显式缓存 <code>node_modules</code>。</li><li><strong>Node 版本漂移</strong>：Netlify 会自动选版本，Pages 需要显式声明，建议在 <code>package.json</code> 的 <code>engines</code> 与面板变量里双重锁定。</li><li><strong>大文件与 LFS</strong>：静态资源超过单文件限制时要改用 R2 直链，而不是硬塞进构建产物。</li><li><strong>SEO 与 sitemap 域名</strong>：切换后 sitemap 与 canonical 必须同步更新，否则会在搜索侧产生重复内容。</li><li><strong>Webhook 与回调地址</strong>：任何注册在 Netlify 域名下的第三方回调都要重新登记。</li></ul><p>整体而言，静态站迁移 Netlify 到 Cloudflare 的性价比很高：风险面窄、回退容易（DNS 层即可），成本通常更低。真正需要投入的是表单、身份与边缘逻辑这三块，它们不是”配置差异”，而是需要重新实现的业务能力。</p><blockquote><p><strong>说明：</strong> 本文为个人技术分析，涉及的产品能力与配置项基于公开文档整理，可能随版本更新而变化。文中代码为示意性实现，未包含生产所需的鉴权、限流与审计逻辑，请以 Cloudflare 与 Netlify 官方最新文档为准。</p></blockquote>]]>
    </content>
    <id>https://blogs.yzy15.dpdns.org/cf-netlify-to-cloudflare-migration/</id>
    <link href="https://blogs.yzy15.dpdns.org/cf-netlify-to-cloudflare-migration/"/>
    <published>2026-09-13T07:02:00.000Z</published>
    <summary>针对以静态站点和 Netlify Functions 为主的项目，梳理迁移到 Cloudflare Pages 与 Workers 的具体清单：构建命令、重定向规则、表单、边缘函数与身份鉴权的逐项替换方案，并给出迁移顺序与验收标准。</summary>
    <title>从 Netlify 迁移到 Cloudflare：静态站与函数的分手清单</title>
    <updated>2026-09-12T17:33:38.958Z</updated>
  </entry>
  <entry>
    <author>
      <name>mc-yzy15</name>
    </author>
    <category term="云计算" scheme="https://blogs.yzy15.dpdns.org/categories/%E4%BA%91%E8%AE%A1%E7%AE%97/"/>
    <category term="边缘计算" scheme="https://blogs.yzy15.dpdns.org/tags/%E8%BE%B9%E7%BC%98%E8%AE%A1%E7%AE%97/"/>
    <category term="Cloudflare" scheme="https://blogs.yzy15.dpdns.org/tags/Cloudflare/"/>
    <category term="迁移" scheme="https://blogs.yzy15.dpdns.org/tags/%E8%BF%81%E7%A7%BB/"/>
    <category term="Vercel" scheme="https://blogs.yzy15.dpdns.org/tags/Vercel/"/>
    <content>
      <![CDATA[<h1 id="从-Vercel-迁移到-Cloudflare：一条可回退的完整路径"><a href="#从-Vercel-迁移到-Cloudflare：一条可回退的完整路径" class="headerlink" title="从 Vercel 迁移到 Cloudflare：一条可回退的完整路径"></a>从 Vercel 迁移到 Cloudflare：一条可回退的完整路径</h1><p>Vercel 和 Cloudflare 都是”前端优先”的平台，但它们的底层假设完全不同：Vercel 假设你的应用是一个构建产物 + Serverless Function 的集合，Cloudflare 假设你的应用是一段跑在边缘的 JavaScript&#x2F;Wasm 程序。这个差异决定了迁移不是”换个 Git 远程”，而是一次架构上的重新归档。</p><p>本文只谈路径与取舍，不谈营销。目标读者是已经把 Next.js 应用跑在 Vercel 上、正在评估成本与可控性的团队。</p><h2 id="为什么要迁移：先把动机说清楚"><a href="#为什么要迁移：先把动机说清楚" class="headerlink" title="为什么要迁移：先把动机说清楚"></a>为什么要迁移：先把动机说清楚</h2><p>迁移的动机通常只有三类，且必须至少命中两类才值得动手。</p><p>第一类是<strong>成本结构</strong>。Vercel 的计费颗粒度偏向”函数调用 + 边缘请求 + 构建时长”，流量上量后账单曲线较陡。Cloudflare 的 Workers 模型以请求数和 CPU 时间为计量核心，静态资源与出网流量在多数套餐里更宽松。量级上，中高流量站的月度差额可能到数倍，但具体以两家官方定价页为准。</p><p>第二类是<strong>控制力</strong>。Cloudflare 允许你在请求进入应用之前插入自定义路由、缓存规则、WAF 与 Bot 管理，且这些能力在同一套配置体系里。Vercel 的中间件虽然能改写请求，但它在平台抽象层之内，遇到”我想在边缘直接操作 TLS 或做 IP 级策略”时会撞到天花板。</p><p>第三类是<strong>架构收敛</strong>。如果你已经用了 R2、D1、KV、Durable Objects，把计算放在 Cloudflare 上能显著减少跨云往返延迟，这是纯粹的性能收益。</p><blockquote><p>经验法则：如果迁移动机里只有”便宜”，先别迁。成本差异往往会被重写成本和运维复杂度吃掉。</p></blockquote><h2 id="能力差异矩阵：哪些能平移，哪些要重写"><a href="#能力差异矩阵：哪些能平移，哪些要重写" class="headerlink" title="能力差异矩阵：哪些能平移，哪些要重写"></a>能力差异矩阵：哪些能平移，哪些要重写</h2><table><thead><tr><th>能力维度</th><th>Vercel 上的形态</th><th>Cloudflare 上的对应物</th><th>迁移难度</th></tr></thead><tbody><tr><td>静态托管</td><td>构建产物自动 CDN 分发</td><td>Pages 或 Workers Static Assets</td><td>低</td></tr><tr><td>SSR &#x2F; API</td><td>Node.js Serverless Function</td><td>Workers（V8 isolate，非 Node）</td><td>中高</td></tr><tr><td>中间件</td><td><code>middleware.ts</code></td><td>Workers 本身的请求处理链</td><td>中</td></tr><tr><td>增量缓存</td><td>ISR &#x2F; Data Cache</td><td>Cache API + KV 手动实现</td><td>高</td></tr><tr><td>定时任务</td><td>Cron Jobs</td><td>Cron Triggers</td><td>低</td></tr><tr><td>对象存储</td><td>Blob</td><td>R2</td><td>低（API 需适配）</td></tr><tr><td>数据库</td><td>Postgres &#x2F; KV 市场集成</td><td>D1 &#x2F; Hyperdrive &#x2F; 外部库</td><td>中高</td></tr><tr><td>长连接</td><td>受限</td><td>Durable Objects &#x2F; WebSocket</td><td>中</td></tr></tbody></table><p>最需要警惕的是运行时的差异。Workers 用的是 V8 isolate，不是完整 Node.js：没有 <code>fs</code>、没有原生 <code>net</code>、<code>process</code> 是受限 shim。任何依赖原生模块（图片处理、PDF 生成、加密库绑定）的路径都要重新实现或改为调用外部服务。</p><h2 id="代码改造的三个高频点"><a href="#代码改造的三个高频点" class="headerlink" title="代码改造的三个高频点"></a>代码改造的三个高频点</h2><p><strong>第一，运行时检测与垫片。</strong> 很多库在加载时会探测 <code>typeof window</code>、<code>process.version</code>。在 Workers 里这类探测会误判，需要显式指定边缘目标。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// next.config.ts —— 明确告知构建器运行环境</span></span><br><span class="line"><span class="keyword">import</span> <span class="keyword">type</span> &#123; <span class="title class_">NextConfig</span> &#125; <span class="keyword">from</span> <span class="string">&quot;next&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="attr">nextConfig</span>: <span class="title class_">NextConfig</span> = &#123;</span><br><span class="line">  <span class="comment">// 让依赖按 edge runtime 解析，避免被打包成 Node 目标</span></span><br><span class="line">  <span class="attr">experimental</span>: &#123;</span><br><span class="line">    <span class="attr">serverComponentsExternalPackages</span>: [],</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">env</span>: &#123;</span><br><span class="line">    <span class="attr">RUNTIME_FLAVOR</span>: <span class="string">&quot;workers&quot;</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> nextConfig;</span><br></pre></td></tr></table></figure><p><strong>第二，缓存语义重写。</strong> Vercel 的 <code>revalidate</code> 是平台替你管理的；Workers 里你得自己决定写哪一层缓存。</p><figure class="highlight ts"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  <span class="keyword">async</span> <span class="title function_">fetch</span>(<span class="params"><span class="attr">request</span>: <span class="title class_">Request</span>, <span class="attr">env</span>: <span class="title class_">Env</span>, <span class="attr">ctx</span>: <span class="title class_">ExecutionContext</span></span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> cache = caches.<span class="property">default</span>;</span><br><span class="line">    <span class="keyword">const</span> hit = <span class="keyword">await</span> cache.<span class="title function_">match</span>(request);</span><br><span class="line">    <span class="keyword">if</span> (hit) <span class="keyword">return</span> hit;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> res = <span class="keyword">await</span> <span class="title function_">fetch</span>(request);</span><br><span class="line">    <span class="keyword">const</span> cached = <span class="keyword">new</span> <span class="title class_">Response</span>(res.<span class="property">body</span>, res);</span><br><span class="line">    <span class="comment">// 只在 GET 且响应可缓存时才写入</span></span><br><span class="line">    <span class="keyword">if</span> (request.<span class="property">method</span> === <span class="string">&quot;GET&quot;</span> &amp;&amp; res.<span class="property">status</span> === <span class="number">200</span>) &#123;</span><br><span class="line">      cached.<span class="property">headers</span>.<span class="title function_">set</span>(<span class="string">&quot;Cache-Control&quot;</span>, <span class="string">&quot;public, max-age=60&quot;</span>);</span><br><span class="line">      ctx.<span class="title function_">waitUntil</span>(cache.<span class="title function_">put</span>(request, cached.<span class="title function_">clone</span>()));</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> cached;</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p><strong>第三，环境变量的读取方式。</strong> Vercel 的 <code>process.env</code> 在构建期与运行期都可读；Workers 用 <code>env</code> 绑定对象，且机密是运行时注入，不会进入 bundle。这实际上是好事，但也意味着任何”构建期读取机密”的写法都要改掉。</p><h2 id="分阶段切换：别想着一次切完"><a href="#分阶段切换：别想着一次切完" class="headerlink" title="分阶段切换：别想着一次切完"></a>分阶段切换：别想着一次切完</h2><p>推荐的四阶段节奏：</p><ol><li><strong>旁路验证</strong>：把 Cloudflare 版本部署到独立子域，用生产流量的影子副本压测，观察错误率与 P95 延迟差异。</li><li><strong>静态优先切换</strong>：先把静态资源与图片托管切过去，DNS 用低 TTL 记录，验证 CDN 命中率。</li><li><strong>读接口切换</strong>：API 的只读部分切到边缘，写路径仍回源到旧平台，此时两套并存。</li><li><strong>全量切换</strong>：写路径切换，旧平台降级为冷备，保留至少一个发布周期再下线。</li></ol><p>关键设计是<strong>一个可控的回退开关</strong>。最可靠的做法不是”再改一次 DNS”，而是在 Cloudflare 侧配置一条路由规则，按请求头把流量打回旧源站：</p><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># wrangler.toml 片段</span></span><br><span class="line"><span class="section">[[routes]]</span></span><br><span class="line"><span class="attr">pattern</span> = <span class="string">&quot;api.example.com/*&quot;</span></span><br><span class="line"><span class="attr">zone_name</span> = <span class="string">&quot;example.com&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="section">[vars]</span></span><br><span class="line"><span class="attr">FALLBACK_ORIGIN</span> = <span class="string">&quot;https://legacy.example.com&quot;</span></span><br><span class="line"><span class="attr">ROLLBACK_HEADER</span> = <span class="string">&quot;x-force-legacy&quot;</span></span><br></pre></td></tr></table></figure><p>当 P95 延迟劣化或错误率超过阈值时，只需在网关层统一注入 <code>x-force-legacy: 1</code>，即可秒级把流量导回旧平台，而不用等待 DNS 传播。</p><h2 id="风险清单：迁移前必须逐条确认"><a href="#风险清单：迁移前必须逐条确认" class="headerlink" title="风险清单：迁移前必须逐条确认"></a>风险清单：迁移前必须逐条确认</h2><ul><li><strong>冷启动与并发模型</strong>：Workers 的 isolate 冷启动通常在毫秒到数十毫秒量级，远优于容器，但首次请求仍可能触发模块初始化。</li><li><strong>子请求上限</strong>：单次请求内的 <code>fetch</code> 数量有上限，扇出型聚合接口需要改造为批量或串行。</li><li><strong>CPU 时间配额</strong>：计费基于 CPU 时间而非墙上时间，CPU 密集任务（如图片转码）必须外移。</li><li><strong>本地开发一致性</strong>：<code>wrangler dev</code> 的模拟与生产仍有差异，尤其是 D1 与队列的边界行为。</li><li><strong>可观测性迁移</strong>：日志、Trace、告警要重新接线，别等出事才发现没有 dashboard。</li><li><strong>回退窗口</strong>：旧平台的数据写入在切换期间如何处理，必须有明确的”单一写入方”规则，否则会出现双写冲突。</li></ul><h2 id="结论与决策建议"><a href="#结论与决策建议" class="headerlink" title="结论与决策建议"></a>结论与决策建议</h2><p>从 Vercel 迁移到 Cloudflare，本质上不是”换供应商”，而是从”平台托管应用”切换到”你自己编排边缘程序”。如果团队里有能读懂 isolate 限制、愿意自己实现缓存和增量逻辑的工程师，迁移是划算的；如果团队追求的是”零运维”，那留在原平台、只把重流量路径（图片、静态、防护）外包给 Cloudflare，往往是更好的折中。</p><blockquote><p><strong>说明：</strong> 本文为个人技术分析，基于两家平台公开的产品模型与笔者实践经验整理。文中提到的配额、延迟与成本量级均为示意，具体数值随套餐与版本变化，请以 Cloudflare 与 Vercel 官方最新定价及文档为准。迁移决策请结合自身流量特征与团队能力评估。</p></blockquote>]]>
    </content>
    <id>https://blogs.yzy15.dpdns.org/cf-vercel-to-cloudflare-migration-path/</id>
    <link href="https://blogs.yzy15.dpdns.org/cf-vercel-to-cloudflare-migration-path/"/>
    <published>2026-09-13T07:00:00.000Z</published>
    <summary>面向已经跑在 Vercel 上的 Next.js 团队，拆解迁移到 Cloudflare Workers 的完整路径：为什么迁移、能力差异矩阵、代码改造清单、分阶段切换节奏与回退开关。读完你能拿到一份可执行的迁移检查表，而不是一句&quot;换平台就行&quot;。</summary>
    <title>从 Vercel 迁移到 Cloudflare：一条可回退的完整路径</title>
    <updated>2026-09-12T17:33:38.958Z</updated>
  </entry>
  <entry>
    <author>
      <name>mc-yzy15</name>
    </author>
    <category term="云计算" scheme="https://blogs.yzy15.dpdns.org/categories/%E4%BA%91%E8%AE%A1%E7%AE%97/"/>
    <category term="架构设计" scheme="https://blogs.yzy15.dpdns.org/tags/%E6%9E%B6%E6%9E%84%E8%AE%BE%E8%AE%A1/"/>
    <category term="Vectorize" scheme="https://blogs.yzy15.dpdns.org/tags/Vectorize/"/>
    <category term="RAG" scheme="https://blogs.yzy15.dpdns.org/tags/RAG/"/>
    <category term="Workers" scheme="https://blogs.yzy15.dpdns.org/tags/Workers/"/>
    <category term="向量检索" scheme="https://blogs.yzy15.dpdns.org/tags/%E5%90%91%E9%87%8F%E6%A3%80%E7%B4%A2/"/>
    <content>
      <![CDATA[<h1 id="Cloudflare-Vectorize-做-RAG-的完整架构：从索引构建到召回链路"><a href="#Cloudflare-Vectorize-做-RAG-的完整架构：从索引构建到召回链路" class="headerlink" title="Cloudflare Vectorize 做 RAG 的完整架构：从索引构建到召回链路"></a>Cloudflare Vectorize 做 RAG 的完整架构：从索引构建到召回链路</h1><p>把 RAG 拆开看，它其实只有两件事：<strong>把知识变成可检索的向量</strong>，以及<strong>把检索结果塞进模型的上下文</strong>。听起来简单，但真正落地时，绝大多数工程问题都出在”变成向量”和”塞进上下文”这两步之间的缝隙里。本文以 Cloudflare 的 Vectorize 为核心，讲清楚一条最小可用、又能持续演进的 RAG 链路。</p><h2 id="为什么把-RAG-放在边缘而不是自建向量库"><a href="#为什么把-RAG-放在边缘而不是自建向量库" class="headerlink" title="为什么把 RAG 放在边缘而不是自建向量库"></a>为什么把 RAG 放在边缘而不是自建向量库</h2><p>自建方案（例如外部托管的向量数据库 + 一台常年运行的 API 服务）的诱因是”可控”：参数可调、算子可换、索引结构任选。代价则是三件长期负担：运维、网络往返、以及向量库与业务服务之间的数据同步。</p><p>边缘栈的价值恰恰在于把这三件事一起消解：</p><ul><li><strong>就近执行</strong>：Workers 在全球节点分布运行，检索与拼接 prompt 的往返不再是”用户 → 你的机房 → 用户”的跨洋链路。</li><li><strong>免运维</strong>：Vectorize 不暴露分片、副本、内存参数，把调优空间换成了确定性的运维成本。</li><li><strong>同栈数据</strong>：D1 存结构化元数据、R2 存原始文档、KV 存热配置、Vectorize 存向量，四者之间不需要自建同步管道。</li></ul><p>反过来说，如果你的场景要求自定义量化、自定义距离度量、或者需要几十亿量级向量的单索引，边缘方案当前未必是首选。选型的前提是承认：<strong>RAG 的瓶颈通常不在检索算法，而在数据质量与请求链路。</strong></p><h2 id="一条最小可用的-RAG-链路"><a href="#一条最小可用的-RAG-链路" class="headerlink" title="一条最小可用的 RAG 链路"></a>一条最小可用的 RAG 链路</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">用户提问</span><br><span class="line">  → Worker 接收请求</span><br><span class="line">  → Workers AI 生成 query embedding</span><br><span class="line">  → Vectorize query(topK)</span><br><span class="line">  → 用返回的 id 回查 D1 / R2 取原文</span><br><span class="line">  → 拼装 prompt → 送 LLM</span><br><span class="line">  → 流式返回并写入日志</span><br></pre></td></tr></table></figure><p>这条链路的每一个箭头，都是一次潜在的失败点。设计时要明确：<strong>哪一步失败可以降级，哪一步失败必须报错。</strong> 例如向量库暂时不可用时，可以降级成”只用关键词检索”；而 LLM 调用失败时，通常只能如实返回错误。</p><h2 id="索引构建：切分策略比模型选型更重要"><a href="#索引构建：切分策略比模型选型更重要" class="headerlink" title="索引构建：切分策略比模型选型更重要"></a>索引构建：切分策略比模型选型更重要</h2><p>很多人把精力花在”选哪个 embedding 模型”上，但实践中影响召回质量最大的是<strong>切分</strong>。RAG 的检索单位是 chunk，chunk 的边界决定了语义是否完整。</p><p>几条经验法则：</p><ol><li><strong>按语义结构切，不按固定字数切</strong>。Markdown 按标题层级切、代码按函数切、FAQ 按问答对切，效果通常好于机械的 512 字符滑窗。</li><li><strong>块间保留重叠</strong>。相邻块重叠约 10%~20% 的内容，可以缓解”答案跨边界”的丢失问题。</li><li><strong>块要带元数据</strong>。每个块至少要记住：所属文档 id、章节路径、更新时间、权限标签。后两者在过滤与安全上几乎是必需的。</li><li><strong>块不宜过碎</strong>。过小的块会让 topK 召回一堆碎片，反而稀释了上下文信噪比。</li></ol><p>下面是一个在 Worker 中生成并写入向量的简化示例，注意向量与元数据是<strong>分开存储</strong>的：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  <span class="keyword">async</span> <span class="title function_">fetch</span>(<span class="params">request, env</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> &#123; docId, chunks &#125; = <span class="keyword">await</span> request.<span class="title function_">json</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1. 批量生成 embedding</span></span><br><span class="line">    <span class="keyword">const</span> &#123; data &#125; = <span class="keyword">await</span> env.<span class="property">AI</span>.<span class="title function_">run</span>(<span class="string">&quot;@cf/baai/bge-base-en-v1.5&quot;</span>, &#123;</span><br><span class="line">      <span class="attr">text</span>: chunks.<span class="title function_">map</span>(<span class="function">(<span class="params">c</span>) =&gt;</span> c.<span class="property">text</span>),</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. 向量入 Vectorize，元数据入 D1，原文入 R2</span></span><br><span class="line">    <span class="keyword">await</span> env.<span class="property">VECTORIZE</span>.<span class="title function_">upsert</span>(</span><br><span class="line">      chunks.<span class="title function_">map</span>(<span class="function">(<span class="params">c, i</span>) =&gt;</span> (&#123;</span><br><span class="line">        <span class="attr">id</span>: <span class="string">`<span class="subst">$&#123;docId&#125;</span>#<span class="subst">$&#123;i&#125;</span>`</span>,</span><br><span class="line">        <span class="attr">values</span>: data[i],</span><br><span class="line">        <span class="attr">metadata</span>: &#123; docId, <span class="attr">section</span>: c.<span class="property">section</span>, <span class="attr">updatedAt</span>: <span class="title class_">Date</span>.<span class="title function_">now</span>() &#125;,</span><br><span class="line">      &#125;))</span><br><span class="line">    );</span><br><span class="line"></span><br><span class="line">    <span class="keyword">await</span> env.<span class="property">DB</span>.<span class="title function_">batch</span>(</span><br><span class="line">      chunks.<span class="title function_">map</span>(<span class="function">(<span class="params">c, i</span>) =&gt;</span></span><br><span class="line">        env.<span class="property">DB</span>.<span class="title function_">prepare</span>(</span><br><span class="line">          <span class="string">&quot;INSERT OR REPLACE INTO chunks (id, doc_id, section, text) VALUES (?, ?, ?, ?)&quot;</span></span><br><span class="line">        ).<span class="title function_">bind</span>(<span class="string">`<span class="subst">$&#123;docId&#125;</span>#<span class="subst">$&#123;i&#125;</span>`</span>, docId, c.<span class="property">section</span>, c.<span class="property">text</span>)</span><br><span class="line">      )</span><br><span class="line">    );</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="title class_">Response</span>.<span class="title function_">json</span>(&#123; <span class="attr">ok</span>: <span class="literal">true</span>, <span class="attr">count</span>: chunks.<span class="property">length</span> &#125;);</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>这段代码里有个容易踩的坑：<code>metadata</code> 是<strong>用来过滤的</strong>，不是用来存正文的。Vectorize 的 metadata 有大小限制，塞长文本会让写入失败或挤占可用字段。正确的做法是向量只存”指路牌”，正文回落到 D1 或 R2。</p><h2 id="召回链路：topK、阈值与重排的取舍"><a href="#召回链路：topK、阈值与重排的取舍" class="headerlink" title="召回链路：topK、阈值与重排的取舍"></a>召回链路：topK、阈值与重排的取舍</h2><p>检索质量由三个参数共同决定，它们彼此耦合：</p><table><thead><tr><th>参数</th><th>作用</th><th>调大</th><th>调小</th></tr></thead><tbody><tr><td>topK</td><td>召回候选数量</td><td>召回更全，但噪声变多、token 成本上升</td><td>上下文干净，但可能漏掉关键块</td></tr><tr><td>score 阈值</td><td>过滤低相关结果</td><td>更严格，可能召回为空</td><td>更宽松，噪声增多</td></tr><tr><td>重排（rerank）</td><td>对候选二次排序</td><td>显著提升精度，增加一次模型调用</td><td>省成本，但排序粗糙</td></tr></tbody></table><p>一个常见的误判是”topK 越大越好”。实际上当 topK 超过某个点后，<strong>新增的上下文带来的干扰会抵消召回收益</strong>，模型反而更容易被无关内容带偏。更稳妥的流水线是：<strong>先用较大的 topK 召回（例如 20<del>30），再用 rerank 截断到 3</del>5 条</strong>送入 LLM。</p><p>重排这一步值得多花点预算，因为它是”检索”与”生成”之间的质量闸门。若不想引入额外模型，也可以用一个轻量的策略：按元数据（如章节权重、更新时间）做加权，或者对同一文档的碎片做合并去重。</p><h2 id="与-D1-R2-Workers-AI-的组合方式"><a href="#与-D1-R2-Workers-AI-的组合方式" class="headerlink" title="与 D1 &#x2F; R2 &#x2F; Workers AI 的组合方式"></a>与 D1 &#x2F; R2 &#x2F; Workers AI 的组合方式</h2><p>四件套的职责边界建议这样划分：</p><ul><li><strong>R2</strong>：原始文档（PDF、Markdown、HTML）。它是唯一的事实源，任何时候都能重建向量。</li><li><strong>Vectorize</strong>：向量与轻量 metadata。用于检索阶段定位 chunk。</li><li><strong>D1</strong>：chunk 正文与结构化元数据。用于检索后取回内容，也用于管理后台做统计。</li><li><strong>Workers AI</strong>：embedding 与生成模型。属于无状态计算，可替换。</li></ul><p>这条分工的关键是：<strong>向量索引要能随时重建</strong>。如果你把原文只存在向量库里，一旦切分策略变更或模型升级，就没有办法无损重建。把 R2 当作事实源，Vectorize 当作可丢弃的派生视图，架构的演进成本会低一个量级。</p><h2 id="何时该放弃边缘-RAG"><a href="#何时该放弃边缘-RAG" class="headerlink" title="何时该放弃边缘 RAG"></a>何时该放弃边缘 RAG</h2><p>不是所有场景都适合。以下情况建议重新评估：</p><ul><li>需要毫秒级返回且索引在十亿向量以上，边缘节点的冷启动与索引规模可能成为瓶颈。</li><li>需要强一致的实时写入 + 复杂聚合查询（类似全文检索的布尔组合），关系型或专用检索引擎更合适。</li><li>合规要求数据必须留在特定地域的单一数据中心内，全球分布反而是负担。</li></ul><blockquote><p><strong>说明：</strong> 本文为基于 Cloudflare 边缘产品形态的原创技术分析，所述架构为通用设计参考，不代表官方推荐实现。产品能力、配额与 API 签名会持续演进，具体以 Cloudflare 官方最新文档为准。文中代码为示意性写法，直接用于生产前需补充错误处理、重试与鉴权。</p></blockquote>]]>
    </content>
    <id>https://blogs.yzy15.dpdns.org/cf-vectorize-rag-architecture-basics/</id>
    <link href="https://blogs.yzy15.dpdns.org/cf-vectorize-rag-architecture-basics/"/>
    <published>2026-09-13T04:00:00.000Z</published>
    <summary>面向要在 Cloudflare 边缘栈上落地检索增强生成（RAG）的开发者，梳理从文本切分、Embedding 生成、Vectorize 索引构建到 Workers 侧检索与拼 prompt 的完整链路，解释各环节的职责边界与常见误区，帮助读者建立一套可运行、可观测、可演进的基础架构。</summary>
    <title>Cloudflare Vectorize 做 RAG 的完整架构：从索引构建到召回链路</title>
    <updated>2026-09-12T17:33:38.958Z</updated>
  </entry>
  <entry>
    <author>
      <name>mc-yzy15</name>
    </author>
    <category term="云计算" scheme="https://blogs.yzy15.dpdns.org/categories/%E4%BA%91%E8%AE%A1%E7%AE%97/"/>
    <category term="边缘计算" scheme="https://blogs.yzy15.dpdns.org/tags/%E8%BE%B9%E7%BC%98%E8%AE%A1%E7%AE%97/"/>
    <category term="Pages Functions" scheme="https://blogs.yzy15.dpdns.org/tags/Pages-Functions/"/>
    <category term="绑定管理" scheme="https://blogs.yzy15.dpdns.org/tags/%E7%BB%91%E5%AE%9A%E7%AE%A1%E7%90%86/"/>
    <content>
      <![CDATA[<h1 id="Pages-Functions-实战：在静态站点里安全地嵌入动态能力"><a href="#Pages-Functions-实战：在静态站点里安全地嵌入动态能力" class="headerlink" title="Pages Functions 实战：在静态站点里安全地嵌入动态能力"></a>Pages Functions 实战：在静态站点里安全地嵌入动态能力</h1><p>静态站点的最大优点是简单：没有运行时、没有状态、没有失败模式。引入 Pages Functions 的那一刻，这三样东西全部回来了。于是问题变成：如何只取走动态能力，而不把后端的复杂度一并接管。</p><p>答案在于克制。Functions 适合做”薄”的事情——鉴权、代理、重写、聚合，而不适合承载业务流程。本文讨论这条线该怎么画，以及画好之后代码该怎么写。</p><h2 id="绑定：能力与约束的同一枚硬币"><a href="#绑定：能力与约束的同一枚硬币" class="headerlink" title="绑定：能力与约束的同一枚硬币"></a>绑定：能力与约束的同一枚硬币</h2><p>Functions 通过绑定访问外部资源：KV 做缓存、D1 做关系数据、R2 做对象存储、队列做异步。绑定是平台给能力的方式，同时也是约束的来源：</p><ul><li><strong>绑定是环境级的</strong>。预览与生产可以使用不同的绑定，这是做数据隔离的基础。</li><li><strong>绑定是声明式的</strong>。你需要显式声明用到哪些资源，未声明的资源无法访问——这其实是好事，它让依赖关系可见。</li><li><strong>绑定有配额</strong>。读写次数、存储量、请求数都有上限，静态站点突然”变贵”往往是因为某段代码在高频路径上访问了付费资源。</li></ul><p>一个常见反模式是：在 Functions 里用 KV 做”每次请求都读一次”的配置存储。请求量上来后，KV 读次数会以请求量级增长。正确做法是在模块作用域内做一次性缓存，或者干脆把配置内联。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 模块作用域缓存：同一个 isolate 内只解析一次</span></span><br><span class="line"><span class="keyword">let</span> cachedConfig = <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">getConfig</span>(<span class="params">env</span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (cachedConfig) <span class="keyword">return</span> cachedConfig;</span><br><span class="line">  <span class="keyword">const</span> raw = <span class="keyword">await</span> env.<span class="property">CONFIG_KV</span>.<span class="title function_">get</span>(<span class="string">&quot;site-config&quot;</span>, <span class="string">&quot;json&quot;</span>);</span><br><span class="line">  cachedConfig = raw ?? &#123; <span class="attr">featureFlags</span>: &#123;&#125; &#125;;</span><br><span class="line">  <span class="keyword">return</span> cachedConfig;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">onRequestGet</span>(<span class="params">&#123; env, request &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> cfg = <span class="keyword">await</span> <span class="title function_">getConfig</span>(env);</span><br><span class="line">  <span class="keyword">if</span> (!cfg.<span class="property">featureFlags</span>.<span class="property">beta</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Response</span>(<span class="string">&quot;not found&quot;</span>, &#123; <span class="attr">status</span>: <span class="number">404</span> &#125;);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> env.<span class="property">ASSETS</span>.<span class="title function_">fetch</span>(request);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>注意 <code>cachedConfig</code> 的生命周期：它存在多久取决于 isolate 是否被回收，因此<strong>只能用于”最终一致即可”的配置</strong>，不能用于权限判断这类强一致场景。</p><h2 id="敏感信息：运行期注入，永不进产物"><a href="#敏感信息：运行期注入，永不进产物" class="headerlink" title="敏感信息：运行期注入，永不进产物"></a>敏感信息：运行期注入，永不进产物</h2><p>这是安全边界上最容易犯错的地方。需要记住一个硬规则：</p><blockquote><p><strong>任何进入构建产物的值都应当被视为公开值。</strong> 构建后的静态文件会被分发到边缘节点、会被缓存、会被下载，没有任何”前端私有变量”这种东西。</p></blockquote><p>因此：</p><ul><li>对外可暴露的配置（站点名、公开 API 基址）可以走构建期变量。</li><li>凭据、密钥、上游 token 一律走运行期变量（secret），只在 Functions 中读取。</li><li>不要在 Functions 里把 secret 拼进响应体，哪怕是为了调试。</li><li>不要用 secret 作为缓存键的一部分。</li></ul><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 正确：只在服务端使用凭据，并且不把它写进日志</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">onRequestPost</span>(<span class="params">&#123; request, env &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> token = env.<span class="property">UPSTREAM_TOKEN</span>;</span><br><span class="line">  <span class="keyword">if</span> (!token) &#123;</span><br><span class="line">    <span class="comment">// 失败要 fail fast，而不是回退到某个默认值</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Response</span>(<span class="string">&quot;server misconfigured&quot;</span>, &#123; <span class="attr">status</span>: <span class="number">500</span> &#125;);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> body = <span class="keyword">await</span> request.<span class="title function_">text</span>();</span><br><span class="line">  <span class="keyword">const</span> res = <span class="keyword">await</span> <span class="title function_">fetch</span>(<span class="string">&quot;https://upstream.example.com/ingest&quot;</span>, &#123;</span><br><span class="line">    <span class="attr">method</span>: <span class="string">&quot;POST&quot;</span>,</span><br><span class="line">    <span class="attr">headers</span>: &#123;</span><br><span class="line">      <span class="string">&quot;content-type&quot;</span>: <span class="string">&quot;application/json&quot;</span>,</span><br><span class="line">      <span class="attr">authorization</span>: <span class="string">`Bearer <span class="subst">$&#123;token&#125;</span>`</span>,</span><br><span class="line">    &#125;,</span><br><span class="line">    body,</span><br><span class="line">  &#125;);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 不要把上游原始响应直接透传，避免泄露内部错误细节</span></span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Response</span>(res.<span class="property">ok</span> ? <span class="string">&quot;ok&quot;</span> : <span class="string">&quot;upstream error&quot;</span>, &#123;</span><br><span class="line">    <span class="attr">status</span>: res.<span class="property">ok</span> ? <span class="number">200</span> : <span class="number">502</span>,</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里刻意做了两件事：<strong>缺配置就 500</strong>（而不是静默降级），以及<strong>不透传上游响应体</strong>（避免内部信息泄露）。这两点在生产环境里能挡掉大量事故。</p><h2 id="请求生命周期：中间件该怎么组织"><a href="#请求生命周期：中间件该怎么组织" class="headerlink" title="请求生命周期：中间件该怎么组织"></a>请求生命周期：中间件该怎么组织</h2><p>Functions 支持基于文件路径的路由，也支持 <code>_middleware</code> 形式的中间件。中间件的价值在于把横切关注点从业务代码里抽出来：</p><ol><li><strong>安全响应头</strong>：统一加 <code>X-Content-Type-Options</code>、<code>Referrer-Policy</code>、CSP。</li><li><strong>鉴权</strong>：校验会话或签名，未通过直接短路。</li><li><strong>可观测性</strong>：注入请求 ID、记录耗时、采样上报。</li><li><strong>重写与规范化</strong>：尾斜杠、大小写、旧路径跳转。</li></ol><p>中间件的顺序很重要，推荐从”最便宜且最严格”的检查开始：先做静态拦截（大小写、路径规范性），再做鉴权，最后才做需要外部依赖的事。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// functions/_middleware.js：一个精简但完整的中间件骨架</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">onRequest</span>(<span class="params">context</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> &#123; request, next &#125; = context;</span><br><span class="line">  <span class="keyword">const</span> url = <span class="keyword">new</span> <span class="title function_">URL</span>(request.<span class="property">url</span>);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 1. 规范化：路径大小写与尾斜杠</span></span><br><span class="line">  <span class="keyword">if</span> (url.<span class="property">pathname</span> !== url.<span class="property">pathname</span>.<span class="title function_">toLowerCase</span>()) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="title class_">Response</span>.<span class="title function_">redirect</span>(<span class="keyword">new</span> <span class="title function_">URL</span>(url.<span class="property">pathname</span>.<span class="title function_">toLowerCase</span>(), url).<span class="title function_">toString</span>(), <span class="number">308</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 2. 鉴权（示意：真实场景应校验签名或会话）</span></span><br><span class="line">  <span class="keyword">const</span> authed = request.<span class="property">headers</span>.<span class="title function_">has</span>(<span class="string">&quot;authorization&quot;</span>);</span><br><span class="line">  <span class="keyword">if</span> (url.<span class="property">pathname</span>.<span class="title function_">startsWith</span>(<span class="string">&quot;/admin&quot;</span>) &amp;&amp; !authed) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Response</span>(<span class="string">&quot;unauthorized&quot;</span>, &#123; <span class="attr">status</span>: <span class="number">401</span> &#125;);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 3. 交给后续处理</span></span><br><span class="line">  <span class="keyword">const</span> res = <span class="keyword">await</span> <span class="title function_">next</span>();</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 4. 统一安全响应头</span></span><br><span class="line">  <span class="keyword">const</span> headers = <span class="keyword">new</span> <span class="title class_">Headers</span>(res.<span class="property">headers</span>);</span><br><span class="line">  headers.<span class="title function_">set</span>(<span class="string">&quot;x-content-type-options&quot;</span>, <span class="string">&quot;nosniff&quot;</span>);</span><br><span class="line">  headers.<span class="title function_">set</span>(<span class="string">&quot;referrer-policy&quot;</span>, <span class="string">&quot;strict-origin-when-cross-origin&quot;</span>);</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Response</span>(res.<span class="property">body</span>, &#123; <span class="attr">status</span>: res.<span class="property">status</span>, headers &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>中间件的一个副作用是它会影响<strong>所有</strong>匹配路径，包括静态资源。对静态资源做鉴权通常不是你想要的；因此要么把中间件放在更深的目录，要么在代码里显式跳过 <code>/_assets/*</code> 之类的路径。</p><h2 id="错误处理：静态站点的失败模式"><a href="#错误处理：静态站点的失败模式" class="headerlink" title="错误处理：静态站点的失败模式"></a>错误处理：静态站点的失败模式</h2><p>静态站点几乎不会失败，Functions 会。失败模式大致有三类：</p><table><thead><tr><th>失败类型</th><th>典型原因</th><th>应对方式</th></tr></thead><tbody><tr><td>配置缺失</td><td>绑定了不存在的 secret&#x2F;env</td><td>启动即失败，返回 5xx 而非降级</td></tr><tr><td>上游超时</td><td>外部 API 慢或不可达</td><td>设置超时 + 有限重试 + 降级响应</td></tr><tr><td>运行时异常</td><td>代码 bug、类型错误</td><td>全局 try&#x2F;catch，返回通用错误</td></tr></tbody></table><p>关键设计原则是：<strong>不要让 Functions 的失败影响静态内容</strong>。如果某个动态接口挂了，页面主体应该照常渲染。这需要在架构上做隔离——把动态接口放在独立路径（<code>/api/*</code>）下，静态页面不要依赖它的同步返回。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 全局异常兜底：返回通用错误，细节留在服务端日志</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">onRequest</span>(<span class="params">context</span>) &#123;</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">await</span> context.<span class="title function_">next</span>();</span><br><span class="line">  &#125; <span class="keyword">catch</span> (err) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">error</span>(<span class="string">&quot;unhandled&quot;</span>, err &amp;&amp; err.<span class="property">message</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Response</span>(<span class="string">&quot;internal error&quot;</span>, &#123; <span class="attr">status</span>: <span class="number">500</span> &#125;);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>注意 <code>console.error</code> 只输出 <code>message</code> 而非整个错误对象——错误对象可能携带请求上下文甚至凭据。日志脱敏不是可选项。</p><h2 id="什么时候不该用-Functions"><a href="#什么时候不该用-Functions" class="headerlink" title="什么时候不该用 Functions"></a>什么时候不该用 Functions</h2><p>最后给出一条边界建议。以下场景不建议用 Functions 承载：</p><ul><li>需要超过单次请求时长上限的耗时任务（应改用队列或长任务编排）。</li><li>需要强一致事务的多步写入（应使用数据库能力并做幂等设计）。</li><li>需要大量并发扇出的聚合请求（容易触及子请求限制）。</li><li>有复杂依赖树的业务逻辑（边缘运行时不适合跑重依赖）。</li></ul><p>判断标准可以简化为一句话：<strong>Functions 适合做”在请求路径上必须同步完成”的少量工作</strong>。任何”可以晚一点再做”的事情，都应该放到队列或定时任务里。这条线守住了，静态站点的简单性就能保住大半。</p><blockquote><p><strong>说明：</strong> 本文代码片段为工程示意写法，具体 API 名称、绑定类型与限制条件请以 Cloudflare 官方最新文档为准；文中关于配额与失败模式的描述为经验性总结，不同套餐与运行环境可能存在差异。观点部分为笔者主观判断。</p></blockquote>]]>
    </content>
    <id>https://blogs.yzy15.dpdns.org/cf-pages-functions-runtime-guide/</id>
    <link href="https://blogs.yzy15.dpdns.org/cf-pages-functions-runtime-guide/"/>
    <published>2026-09-13T03:06:00.000Z</published>
    <summary>Pages Functions 让静态站点获得动态能力，但它不是「免费的后端」。本文从绑定管理、敏感信息隔离、请求生命周期与错误处理四个角度，讲解如何在不破坏静态站点简单性的前提下引入动态逻辑，并给出可复用的中间件写法。</summary>
    <title>Pages Functions 实战：在静态站点里安全地嵌入动态能力</title>
    <updated>2026-09-12T17:33:38.958Z</updated>
  </entry>
  <entry>
    <author>
      <name>mc-yzy15</name>
    </author>
    <category term="云计算" scheme="https://blogs.yzy15.dpdns.org/categories/%E4%BA%91%E8%AE%A1%E7%AE%97/"/>
    <category term="Cloudflare Pages" scheme="https://blogs.yzy15.dpdns.org/tags/Cloudflare-Pages/"/>
    <category term="Cloudflare Workers" scheme="https://blogs.yzy15.dpdns.org/tags/Cloudflare-Workers/"/>
    <category term="架构选型" scheme="https://blogs.yzy15.dpdns.org/tags/%E6%9E%B6%E6%9E%84%E9%80%89%E5%9E%8B/"/>
    <content>
      <![CDATA[<h1 id="Pages-与-Workers-的边界在哪里：静态托管选型的技术判断"><a href="#Pages-与-Workers-的边界在哪里：静态托管选型的技术判断" class="headerlink" title="Pages 与 Workers 的边界在哪里：静态托管选型的技术判断"></a>Pages 与 Workers 的边界在哪里：静态托管选型的技术判断</h1><p>Cloudflare 的 Pages 和 Workers 曾经界限分明：Pages 管静态站点，Workers 管动态逻辑。随着两者能力互相渗透——Pages 有了 Functions，Workers 有了静态资源绑定——这条线变得模糊，选型问题也从”该用哪个”变成了”哪个更省心”。</p><p>本文不站队，而是给出四个可判断的维度。在多数真实项目里，答案不是二选一，而是”主用哪个 + 另一个补位”。</p><h2 id="维度一：构建模型决定工作流的形状"><a href="#维度一：构建模型决定工作流的形状" class="headerlink" title="维度一：构建模型决定工作流的形状"></a>维度一：构建模型决定工作流的形状</h2><p>这是两者最本质的差异，也是最多人忽略的一点。</p><p><strong>Pages 是构建驱动的</strong>。它假设你有一个需要编译的前端项目，平台负责检出代码、装依赖、跑构建命令、上传产物。这套模型天然适配 Git 工作流：push 即部署，PR 即预览。</p><p><strong>Workers 是产物驱动的</strong>。它接收的是已经准备好的代码包（或资源目录），部署是 <code>wrangler deploy</code> 这一步，构建发生在你的机器或 CI 上。平台不关心你怎么生成产物。</p><p>这个差异带来的实际影响是：</p><table><thead><tr><th>关注点</th><th>Pages</th><th>Workers</th></tr></thead><tbody><tr><td>构建发生在哪</td><td>平台构建容器</td><td>你的本地或 CI</td></tr><tr><td>预览部署</td><td>内建，按分支自动生成</td><td>需自行编排或使用平台能力</td></tr><tr><td>构建耗时可控性</td><td>受平台约束</td><td>完全自控</td></tr><tr><td>适配非 Git 流程</td><td>较弱</td><td>很强</td></tr></tbody></table><p>如果你的团队已经在 CI 里做完整的构建与测试，Workers 的产物驱动模型反而更自然——你不需要让平台再理解一遍你的构建。</p><h2 id="维度二：运行时约束决定上限"><a href="#维度二：运行时约束决定上限" class="headerlink" title="维度二：运行时约束决定上限"></a>维度二：运行时约束决定上限</h2><p>两者都跑在边缘运行时上，但绑定与配置的默认姿态不同。</p><ul><li><strong>启动与冷启动</strong>：边缘运行时通常没有传统意义上的长冷启动，但首次请求的初始化成本依然存在，尤其是加载大依赖或建立连接时。</li><li><strong>CPU 时间</strong>：计费上通常区分 CPU 时间与墙钟时间，等待 I&#x2F;O 的时间一般不计入 CPU，但长任务仍受总时长约束。</li><li><strong>状态</strong>：边缘运行时默认无状态，持久化必须走 KV、D1、R2、Durable Objects 等绑定。</li><li><strong>子请求</strong>:对外部服务的调用数量与并发存在约束，批量扇出需要谨慎设计。</li></ul><p>对静态站点而言这些约束几乎不构成问题；对需要处理上传、调用多个上游 API 的应用，约束会迅速变成架构约束。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Workers 风格：请求时读取绑定，而不是把值内联进构建产物</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  <span class="keyword">async</span> <span class="title function_">fetch</span>(<span class="params">request, env</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> url = <span class="keyword">new</span> <span class="title function_">URL</span>(request.<span class="property">url</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 运行期变量：敏感值只在这里出现，不进入构建产物</span></span><br><span class="line">    <span class="keyword">const</span> upstream = env.<span class="property">UPSTREAM_ORIGIN</span>;</span><br><span class="line">    <span class="keyword">if</span> (!upstream) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Response</span>(<span class="string">&quot;misconfigured&quot;</span>, &#123; <span class="attr">status</span>: <span class="number">500</span> &#125;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> target = <span class="keyword">new</span> <span class="title function_">URL</span>(url.<span class="property">pathname</span> + url.<span class="property">search</span>, upstream);</span><br><span class="line">    <span class="keyword">return</span> <span class="title function_">fetch</span>(target, &#123; <span class="attr">cf</span>: &#123; <span class="attr">cacheTtl</span>: <span class="number">60</span> &#125; &#125;);</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>上面这段代码的关键不在语法，而在语义：<code>env.UPSTREAM_ORIGIN</code> 是运行期注入的。如果你用 Pages 静态构建，这个值会在构建时就固化，改一次配置就要重新构建一次。这是选型时最容易被低估的差异。</p><h2 id="维度三：路由与中间件的表达力"><a href="#维度三：路由与中间件的表达力" class="headerlink" title="维度三：路由与中间件的表达力"></a>维度三：路由与中间件的表达力</h2><p>静态托管最常遇到的”就差一点”需求是路由：把 <code>/old/*</code> 重定向、给 <code>/api/*</code> 加鉴权、按 <code>Accept</code> 头返回不同格式。这些需求在纯静态方案里靠配置文件，在 Workers 里靠代码。</p><p>一个是声明式的、有限表达力的路由表；一个是图灵完备的代码。前者更安全、更易审计，后者更灵活、更难约束。</p><p>实践中的分界线大致是：</p><ul><li>规则数量少、逻辑简单、变更不频繁 → 声明式路由更合适。</li><li>需要读取请求内容做判断、需要访问绑定、需要重写响应体 → 只能用代码。</li></ul><p>反过来，如果只是 302 一批旧链接，用 Workers 写代码是过度设计，而且会增加故障面：代码会抛异常，配置文件基本不会。</p><h2 id="维度四：运维成本与团队认知"><a href="#维度四：运维成本与团队认知" class="headerlink" title="维度四：运维成本与团队认知"></a>维度四：运维成本与团队认知</h2><p>技术选型的最后一公里往往是人的问题。</p><p><strong>Pages 的优势在于心智负担低</strong>：非前端工程师也能理解”push 就部署”。<strong>Workers 的优势在于可观测性强</strong>：你有完整的代码路径，可以在本地跑、可以单元测试、可以在 CI 里做集成测试。</p><table><thead><tr><th>团队特征</th><th>倾向</th><th>理由</th></tr></thead><tbody><tr><td>前端为主，无专职运维</td><td>Pages</td><td>工作流内建，无需编排</td></tr><tr><td>有成熟 CI，追求可复现</td><td>Workers</td><td>产物驱动，构建完全自控</td></tr><tr><td>需要多环境灰度</td><td>两者皆可 + 明确约定</td><td>取决于是否愿意自建编排</td></tr><tr><td>需要长事务与队列</td><td>Workers + 队列能力</td><td>静态托管无法承载</td></tr></tbody></table><h2 id="混合架构：怎样组合才不别扭"><a href="#混合架构：怎样组合才不别扭" class="headerlink" title="混合架构：怎样组合才不别扭"></a>混合架构：怎样组合才不别扭</h2><p>现实中更常见的答案是混合，但混合有正确姿势和错误姿势。</p><p><strong>正确姿势</strong>：静态产物交给 Pages（或 Workers 静态资源），动态接口交给 Workers，两者通过同域路由聚合。这样静态部分享受构建流水线，动态部分享受代码灵活性。</p><p><strong>错误姿势</strong>：在 Pages Functions 里塞进整套业务逻辑，最后发现需要队列、需要长任务、需要复杂绑定，于是把一半逻辑迁移到 Workers，剩下的一半留在 Functions 里——两套部署、两套配置、两套日志，认知成本翻倍。</p><p>判断混合是否健康的标准很简单：<strong>如果两个部分的部署频率不同、依赖的团队不同、变更的理由不同，那它们就应该是两个独立的部署单元</strong>；反之，如果它们总是一起改，分包只会增加摩擦。</p><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 混合架构示意：一个 wrangler 配置里区分静态资源与 Worker 任务</span></span><br><span class="line"><span class="attr">name</span> = <span class="string">&quot;site-edge&quot;</span></span><br><span class="line"><span class="attr">main</span> = <span class="string">&quot;src/worker.js&quot;</span></span><br><span class="line"><span class="attr">compatibility_date</span> = <span class="string">&quot;2026-07-01&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 静态资源绑定：让 Worker 处理请求的同时能回退到静态产物</span></span><br><span class="line"><span class="section">[assets]</span></span><br><span class="line"><span class="attr">directory</span> = <span class="string">&quot;./dist&quot;</span></span><br><span class="line"><span class="attr">not_found_handling</span> = <span class="string">&quot;single-page-application&quot;</span></span><br></pre></td></tr></table></figure><p>上面这个模式实际上在消解”Pages 还是 Workers”的二分法：一个部署单元同时拥有静态能力和动态能力，代价是你需要自己承担构建与部署编排。</p><blockquote><p><strong>判断法则</strong>：先问”我的构建流水线由谁负责”，再问”我的运行期配置由谁注入”。这两个问题的答案如果不同，说明你需要两个部署单元，而不是一个更复杂的单元。</p></blockquote><h2 id="结语：边界在收敛，判断标准不变"><a href="#结语：边界在收敛，判断标准不变" class="headerlink" title="结语：边界在收敛，判断标准不变"></a>结语：边界在收敛，判断标准不变</h2><p>平台能力的收敛会让”功能对比表”越来越没意义，因为两边最终会看起来都支持。但判断标准其实很稳定：<strong>构建发生在哪、配置在何时注入、路由由谁表达、谁来维护</strong>。把这四个问题答清楚，选型就不再是风格偏好问题。</p><p>如果非要给一个默认建议：以静态内容为主的站点，从 Pages 起步，遇到明确的运行期需求再引入 Workers；以 API 与动态逻辑为主的场景，直接从 Workers 起步，把静态资源当作它的一个绑定。这样两条路径都不会在一开始就背上不必要的复杂度。</p><blockquote><p><strong>说明：</strong> 本文讨论的是架构选型方法而非产品功能承诺，Pages 与 Workers 的具体能力、配额与命名可能随版本演进而变化，请以 Cloudflare 官方最新文档为准。文中倾向性结论为笔者基于工程实践的主观判断，不同团队应按自身约束重新评估。</p></blockquote>]]>
    </content>
    <id>https://blogs.yzy15.dpdns.org/cf-pages-vs-workers-static/</id>
    <link href="https://blogs.yzy15.dpdns.org/cf-pages-vs-workers-static/"/>
    <published>2026-09-13T03:04:00.000Z</published>
    <summary>Pages 和 Workers 在能力上正在收敛，这让选型反而变难了。本文从构建模型、运行时约束、路由能力与运维成本四个维度给出可操作的判断标准，并讨论混合架构如何避免两头都想要、结果两头都别扭。</summary>
    <title>Pages 与 Workers 的边界在哪里：静态托管选型的技术判断</title>
    <updated>2026-09-12T17:33:38.958Z</updated>
  </entry>
  <entry>
    <author>
      <name>mc-yzy15</name>
    </author>
    <category term="云计算" scheme="https://blogs.yzy15.dpdns.org/categories/%E4%BA%91%E8%AE%A1%E7%AE%97/"/>
    <category term="Cloudflare Pages" scheme="https://blogs.yzy15.dpdns.org/tags/Cloudflare-Pages/"/>
    <category term="预览部署" scheme="https://blogs.yzy15.dpdns.org/tags/%E9%A2%84%E8%A7%88%E9%83%A8%E7%BD%B2/"/>
    <category term="团队协作" scheme="https://blogs.yzy15.dpdns.org/tags/%E5%9B%A2%E9%98%9F%E5%8D%8F%E4%BD%9C/"/>
    <content>
      <![CDATA[<h1 id="预览部署工作流设计：用-Cloudflare-Pages-把评审成本压到最低"><a href="#预览部署工作流设计：用-Cloudflare-Pages-把评审成本压到最低" class="headerlink" title="预览部署工作流设计：用 Cloudflare Pages 把评审成本压到最低"></a>预览部署工作流设计：用 Cloudflare Pages 把评审成本压到最低</h1><p>「每个 PR 自动生成一个预览链接」这件事，演示起来只要十分钟，但在真实团队里跑上三个月，就会积累出一堆脏问题：预览链接被外部爬虫收录、预览环境往生产数据库写数据、评审者看到的是三天前的缓存版本、回滚时不知道该回滚到哪个 deployment。</p><p>这些问题的共同点是：它们都不是 Pages 的功能问题，而是工作流设计问题。本文讨论如何在 Pages 提供的能力之上，设计一套能长期运行的预览部署工作流。</p><h2 id="预览部署真正的价值在哪里"><a href="#预览部署真正的价值在哪里" class="headerlink" title="预览部署真正的价值在哪里"></a>预览部署真正的价值在哪里</h2><p>先澄清一个常见误解。预览部署的价值不在于”给非技术人员看效果”——那个需求用截图或本地服务器也能满足。它真正解决的是下面三类问题：</p><ul><li><strong>环境相关缺陷只能在真实环境暴露</strong>。路由重写、资源路径、跨域头、缓存策略这些问题在本地往往表现正常。</li><li><strong>评审需要上下文而非代码</strong>。产品与设计评审的是”行为”，不是 diff。</li><li><strong>变更影响面需要可验证</strong>。特别是改动构建配置、CDN 缓存规则、重定向表时，只有真实部署能验证。</li></ul><p>因此预览部署的设计目标应该是：<strong>让一次部署尽可能接近生产，同时把它的副作用降到零</strong>。这个目标里，”副作用”才是难点。</p><h2 id="分支命名约定：让-URL-成为可读的元数据"><a href="#分支命名约定：让-URL-成为可读的元数据" class="headerlink" title="分支命名约定：让 URL 成为可读的元数据"></a>分支命名约定：让 URL 成为可读的元数据</h2><p>多数团队会把预览链接直接丢在聊天窗口里，然后几周后没人知道哪个链接对应哪个功能。解决方式是把信息编码进分支名，因为 Pages 会用分支名生成预览子域。</p><p>一个可用的约定是分层命名：</p><ol><li><code>feat/&lt;issue-id&gt;-&lt;slug&gt;</code>：功能分支，如 <code>feat/412-checkout-retry</code>。</li><li><code>fix/&lt;issue-id&gt;-&lt;slug&gt;</code>：缺陷修复分支。</li><li><code>exp/&lt;slug&gt;</code>：纯实验分支，允许不稳定，允许随时删除。</li><li><code>release/&lt;version&gt;</code>：发布候选，通常需要额外一层保护。</li></ol><p>这样生成的预览域名天然携带语义，回看历史时有据可查。反过来，如果分支名全是 <code>update</code>、<code>test2</code>、<code>tmp</code>，预览部署的信息价值会被迅速稀释。</p><h2 id="数据隔离：预览环境最容易出事的地方"><a href="#数据隔离：预览环境最容易出事的地方" class="headerlink" title="数据隔离：预览环境最容易出事的地方"></a>数据隔离：预览环境最容易出事的地方</h2><p>预览部署最危险的场景是：它看起来像个玩具，但它连的是真数据库。用户点一下”删除”，生产数据就没了。</p><p>对于前后端一体的站点（例如 Pages 静态前端 + Pages Functions 后端），几种隔离策略按成本从低到高排列：</p><table><thead><tr><th>策略</th><th>实现方式</th><th>隔离强度</th><th>适用场景</th></tr></thead><tbody><tr><td>只读预览</td><td>预览环境注入只读凭据</td><td>中</td><td>纯展示型评审</td></tr><tr><td>独立数据集</td><td>预览使用独立 D1 &#x2F; KV 实例</td><td>高</td><td>需要写入验证的功能</td></tr><tr><td>影子数据库</td><td>定期从生产脱敏同步</td><td>高</td><td>需要真实数据结构</td></tr><tr><td>完全模拟</td><td>使用内存或 mock 层</td><td>最高</td><td>单元级验证、无后端依赖</td></tr></tbody></table><p>对多数团队而言，<strong>独立数据集</strong>是性价比最高的选择：Pages 的环境变量机制允许预览与生产绑定不同的绑定（binding），你只需要在配置层面做一次分流，就能让预览环境永远碰不到生产数据。</p><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># wrangler.toml 片段：用环境区分预览与生产的绑定</span></span><br><span class="line"><span class="attr">name</span> = <span class="string">&quot;shop-frontend&quot;</span></span><br><span class="line"><span class="attr">pages_build_output_dir</span> = <span class="string">&quot;dist&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="section">[vars]</span></span><br><span class="line"><span class="attr">PUBLIC_SITE_NAME</span> = <span class="string">&quot;shop-preview&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 注意：生产环境的绑定应在 Pages 面板中单独配置</span></span><br><span class="line"><span class="comment"># 这里的 var 只用于预览构建；敏感值一律走 secrets，不要写进仓库</span></span><br></pre></td></tr></table></figure><blockquote><p><strong>风险提示</strong>：不要把 D1、KV 或 R2 的生产凭据放在预览环境变量里。一旦预览部署的链接泄露（这几乎必然发生），凭据就等于公开。</p></blockquote><h2 id="访问控制：预览链接的默认安全姿态"><a href="#访问控制：预览链接的默认安全姿态" class="headerlink" title="访问控制：预览链接的默认安全姿态"></a>访问控制：预览链接的默认安全姿态</h2><p>这里需要区分两件事：<strong>平台层面的访问控制</strong>与<strong>应用层面的访问控制</strong>。</p><p>平台层面可以限制整个预览域名的访问（例如通过 Access 之类的身份层），应用层面则是在自己的代码里判断当前是否预览环境。两者的正确用法是：</p><ul><li>内部项目：优先用平台层控制，因为它是”默认拒绝”的姿态，漏配的代价是访问被挡，而不是数据泄露。</li><li>开源或公开评审项目：预览链接本身可能就需要公开展示，此时应用层要显式处理”这是预览”的语义，例如加一个顶部横幅、禁用支付入口、禁止提交表单。</li></ul><p>一个常见的失误是：只在预览环境加”禁止支付”的代码分支，而忘记预览环境同样会调用真实的第三方 API。正确做法是把所有外部副作用统一收口到一个配置开关，预览环境默认全部关闭。</p><h2 id="缓存与-SEO：两个容易被忽略的副作用"><a href="#缓存与-SEO：两个容易被忽略的副作用" class="headerlink" title="缓存与 SEO：两个容易被忽略的副作用"></a>缓存与 SEO：两个容易被忽略的副作用</h2><p><strong>缓存污染</strong>。如果预览域名与生产域名共享 CDN 缓存键，或者预览环境的响应被写入了共享的 KV 缓存，你会看到”预览内容串到生产”的诡异现象。避免方式很简单：让缓存键包含部署标识，或者让预览环境完全不写共享缓存。</p><p><strong>搜索引擎收录</strong>。预览域名的内容通常与生产重复，如果被收录会造成内容重复问题，更糟的是可能让未完成的页面出现在搜索结果里。推荐的双保险是：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 在预览环境的 HTML head 中注入 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">&quot;robots&quot;</span> <span class="attr">content</span>=<span class="string">&quot;noindex, nofollow&quot;</span> /&gt;</span></span><br></pre></td></tr></table></figure><p>同时配合响应头 <code>X-Robots-Tag: noindex</code>，因为某些爬虫更信任响应头而非 meta 标签。还需要注意的是：如果预览环境存在未公开的功能描述，<code>noindex</code> 并不能阻止内容被读取，只是阻止被索引。</p><ul><li>使用 <code>noindex</code> 而非 <code>robots.txt</code>：后者对未收录的域名约束力有限，且是全局的。</li><li>在预览环境关闭 sitemap 输出。</li><li>在预览环境关闭社会化分享的预览图抓取，避免生成指向预览域名的卡片。</li><li>定期清理陈旧的预览部署，减少被发现的表面积。</li></ul><h2 id="回滚与清理：工作流的最后一段"><a href="#回滚与清理：工作流的最后一段" class="headerlink" title="回滚与清理：工作流的最后一段"></a>回滚与清理：工作流的最后一段</h2><p>预览部署的工作流必须包含”结束”这一步，否则三个月后你会面对几百个僵尸部署。</p><p>回滚方面，Pages 的 deployment 是不可变的，这使得回滚变得异常简单：直接把一个历史 deployment 重新提升为生产即可，无需重新构建。这比”重新跑一次旧 commit 的构建”可靠得多，因为旧 commit 的依赖可能已经无法解析。</p><p>清理方面，建议在上线后自动删除对应的预览部署，或者按保留策略定期清理。判断标准可以很简单：<strong>已合并或已关闭的分支，其预览部署应在一个固定周期后消失</strong>。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># GitHub Actions 片段：PR 关闭后触发清理（伪代码式示意）</span></span><br><span class="line"><span class="comment"># on:</span></span><br><span class="line"><span class="comment">#   pull_request:</span></span><br><span class="line"><span class="comment">#     types: [closed]</span></span><br><span class="line"><span class="comment"># steps:</span></span><br><span class="line"><span class="comment">#   - name: Notify cleanup</span></span><br><span class="line"><span class="comment">#     run: |</span></span><br><span class="line"><span class="comment">#       echo &quot;PR #$&#123;&#123; github.event.number &#125;&#125; closed&quot;</span></span><br><span class="line"><span class="comment">#       # 调用部署平台的 API 删除对应预览部署，具体接口以官方文档为准</span></span><br></pre></td></tr></table></figure><blockquote><p><strong>经验法则</strong>：预览部署应当”用完即弃”，把它当成一次性的评审设施，而不是长期托管的第二个站点。丢弃的默认值应该是”删除”，而不是”保留”。</p></blockquote><p>把以上几节合起来看，一套健康的预览工作流大约长这样：分支名承载语义、预览与生产绑定分离、外部副作用默认关闭、索引被双重阻断、部署生命周期随 PR 结束而终结。做到这几点，预览部署才会从”看起来很酷的演示”变成真正降低评审成本的基础设施。</p><blockquote><p><strong>说明：</strong> 本文讨论的是预览部署的工作流设计方法论，不涉及任何特定平台的定价或配额承诺；文中配置片段为示意写法，实际字段与能力请以 Cloudflare 官方最新文档为准。文中的经验法则来自工程实践总结，不同团队规模下可能有不同取舍。</p></blockquote>]]>
    </content>
    <id>https://blogs.yzy15.dpdns.org/cf-pages-preview-deploy-workflow/</id>
    <link href="https://blogs.yzy15.dpdns.org/cf-pages-preview-deploy-workflow/"/>
    <published>2026-09-13T03:02:00.000Z</published>
    <summary>预览部署不只是「每个 PR 给一个链接」，它需要解决访问控制、数据隔离、缓存污染与回滚四个工程问题。本文给出一套可直接落地的预览环境工作流设计，包括分支命名约定、预览与生产的数据边界、以及如何避免预览环境被搜索引擎收录。</summary>
    <title>预览部署工作流设计：用 Cloudflare Pages 把评审成本压到最低</title>
    <updated>2026-09-12T17:33:38.958Z</updated>
  </entry>
  <entry>
    <author>
      <name>mc-yzy15</name>
    </author>
    <category term="云计算" scheme="https://blogs.yzy15.dpdns.org/categories/%E4%BA%91%E8%AE%A1%E7%AE%97/"/>
    <category term="Cloudflare Pages" scheme="https://blogs.yzy15.dpdns.org/tags/Cloudflare-Pages/"/>
    <category term="构建管线" scheme="https://blogs.yzy15.dpdns.org/tags/%E6%9E%84%E5%BB%BA%E7%AE%A1%E7%BA%BF/"/>
    <category term="静态托管" scheme="https://blogs.yzy15.dpdns.org/tags/%E9%9D%99%E6%80%81%E6%89%98%E7%AE%A1/"/>
    <content>
      <![CDATA[<h1 id="Cloudflare-Pages-构建管线解剖：从-Git-提交到全球边缘的完整链路"><a href="#Cloudflare-Pages-构建管线解剖：从-Git-提交到全球边缘的完整链路" class="headerlink" title="Cloudflare Pages 构建管线解剖：从 Git 提交到全球边缘的完整链路"></a>Cloudflare Pages 构建管线解剖：从 Git 提交到全球边缘的完整链路</h1><p>大多数团队接入 Pages 的过程只有五分钟：连仓库、填 <code>build command</code>、填 <code>output directory</code>、点部署。之后这条链路就变成了理所当然的存在，直到某天构建突然从 90 秒变成 6 分钟，或者某个环境变量在预览环境里神秘消失，才会有人回头去问：这中间到底发生了什么。</p><p>本文不复述官方文档的配置项清单，而是把 Pages 的构建管线拆成可推理的四个阶段，然后针对每个阶段讨论缓存、约束与调优空间。</p><h2 id="阶段拆解：一次-push-之后的四个动作"><a href="#阶段拆解：一次-push-之后的四个动作" class="headerlink" title="阶段拆解：一次 push 之后的四个动作"></a>阶段拆解：一次 push 之后的四个动作</h2><p>从工程视角看，Pages 的构建管线可以抽象成下面四个串行阶段：</p><ol><li><strong>事件接收与排队</strong>：Git provider 通过 webhook 通知 Pages 有新的 commit，Pages 为这次提交创建一个 deployment 记录。同一分支上的新提交会让旧 deployment 快速失效（superseded）。</li><li><strong>环境准备</strong>：拉起一个一次性的构建容器，检出仓库到该 commit，恢复依赖缓存，按项目配置注入环境变量（分为生产环境与预览环境两套）。</li><li><strong>执行构建</strong>：运行你指定的构建命令。这一步对 Pages 而言是完全不透明的——它不知道你在跑 <code>hexo generate</code> 还是 <code>next build</code>，只知道退出码。</li><li><strong>产物上传与分发</strong>：把输出目录上传到对象存储，生成不可变的 deployment 子域，同时按分支策略决定是否更新生产域名指向。</li></ol><p>理解这四段划分的价值在于：当构建变慢或失败时，你能立刻定位问题归属，而不是笼统地抱怨”Pages 又抽风了”。</p><h2 id="构建容器：约束比性能更值得关注"><a href="#构建容器：约束比性能更值得关注" class="headerlink" title="构建容器：约束比性能更值得关注"></a>构建容器：约束比性能更值得关注</h2><p>Pages 的构建环境是一个受控的容器，而不是一台你可以随意 SSH 进去的机器。这意味着你要接受几类约束：</p><ul><li><strong>运行时版本受控</strong>。Node 版本通常由项目内的 <code>.nvmrc</code>、<code>engines</code> 字段或环境变量决定，但可选择范围有限。写死一个过旧的版本会在几个月后被移除。</li><li><strong>构建超时存在上限</strong>。构建不是可以无限跑下去的任务，长任务最终会被强制终止，这一点在做全量静态生成的大型站点上尤其容易踩坑。</li><li><strong>磁盘与内存是共享的</strong>。依赖安装、构建产物、缓存都挤在同一块临时空间里，<code>node_modules</code> 加产物体积超过阈值时会以奇怪的方式失败。</li><li><strong>构建容器无持久状态</strong>。任何你以为”上次跑留下了”的文件都不存在，除了显式声明的缓存目录。</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 一个最小但信息完整的构建配置示例（.node-version + package.json scripts）</span></span><br><span class="line"><span class="comment"># 1. 固定运行时</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;20.11.1&quot;</span> &gt; .node-version</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 在 package.json 中声明构建脚本，避免直接把长命令写进 Pages 面板</span></span><br><span class="line"><span class="comment"># &quot;scripts&quot;: &#123;</span></span><br><span class="line"><span class="comment">#   &quot;build&quot;: &quot;node ./scripts/prebuild-check.js &amp;&amp; hexo generate&quot;</span></span><br><span class="line"><span class="comment"># &#125;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 本地复现构建环境时，至少对齐 Node 版本与依赖安装方式</span></span><br><span class="line">nvm use</span><br><span class="line">npm ci --no-audit --no-fund</span><br><span class="line">npm run build</span><br></pre></td></tr></table></figure><p>把构建命令收敛到 <code>package.json</code> 里而不是面板里，是一个被低估的实践：它让本地、CI、Pages 三处跑的是同一条命令，行为差异会大幅缩小。</p><h2 id="缓存策略：命中率决定构建时间"><a href="#缓存策略：命中率决定构建时间" class="headerlink" title="缓存策略：命中率决定构建时间"></a>缓存策略：命中率决定构建时间</h2><p>构建耗时的大头通常不在你的构建命令本身，而在依赖安装。Pages 提供依赖缓存机制，但命中与否取决于几个细节：</p><table><thead><tr><th>缓存维度</th><th>命中条件</th><th>失效后的代价</th><th>可操作建议</th></tr></thead><tbody><tr><td>包管理器缓存</td><td>lockfile 未变化</td><td>重新下载全部依赖</td><td>提交 lockfile，不要在构建时 <code>install</code> 一个漂移的版本</td></tr><tr><td>构建产物缓存</td><td>你的工具自己实现</td><td>全量重新生成</td><td>增量构建需要框架支持，静态生成器通常不支持</td></tr><tr><td>构建镜像层</td><td>由平台管理</td><td>环境冷启动变慢</td><td>无法直接控制，只能减少依赖数量</td></tr></tbody></table><p>一个实践中的经验法则是：<strong>把 lockfile 当作构建契约</strong>。如果你在构建脚本里跑 <code>npm install</code> 而不是 <code>npm ci</code>，那么每次构建都可能解析出不同的依赖树，缓存命中率随之崩塌，同时引入不可复现的风险。</p><figure class="highlight toml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># wrangler.toml 片段：如果构建产物需要配合 Pages Functions 一起工作</span></span><br><span class="line"><span class="attr">name</span> = <span class="string">&quot;my-site&quot;</span></span><br><span class="line"><span class="attr">pages_build_output_dir</span> = <span class="string">&quot;public&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 构建产物的目录必须与 Pages 面板里的 output directory 保持一致</span></span><br><span class="line"><span class="comment"># 否则会出现「构建成功但页面 404」这种最令人困惑的故障</span></span><br></pre></td></tr></table></figure><blockquote><p><strong>经验法则</strong>：构建时间超过 3 分钟后，应先优化依赖安装，再考虑优化构建命令本身。多数项目的耗时结构是「依赖 70%、构建 30%」。</p></blockquote><h2 id="环境变量与预览环境的语义差异"><a href="#环境变量与预览环境的语义差异" class="headerlink" title="环境变量与预览环境的语义差异"></a>环境变量与预览环境的语义差异</h2><p>Pages 把环境分成生产与预览两类，但真正的复杂性在于：预览环境不是生产的缩小版，它们的语义完全不同。</p><ul><li><strong>预览环境的域名是每次部署唯一的</strong>，因此任何把域名写死进构建产物的做法都会在预览里出问题。</li><li><strong>环境变量按环境隔离</strong>，但某些框架会在构建期把变量内联进产物，导致预览环境拿到生产值。</li><li><strong>预览部署默认不更新生产域名</strong>，这对回滚很友好——你可以先把某个历史 deployment 提升为生产，而不必重新构建。</li></ul><p>这就引出一个常被忽略的结论：<strong>构建期变量与运行期变量必须分开管理</strong>。构建期变量进入产物后就不可更改，适合放公开的配置；运行期变量应由 Pages Functions &#x2F; Workers 在请求时读取，适合放敏感值。</p><h2 id="分支策略：把流水线画成决策树"><a href="#分支策略：把流水线画成决策树" class="headerlink" title="分支策略：把流水线画成决策树"></a>分支策略：把流水线画成决策树</h2><p>一个成熟团队通常不会让所有分支都触发同一条链路。下面是一个可参考的分支到部署的映射策略：</p><ul><li><code>main</code> &#x2F; <code>master</code>：触发生产部署，产物直接对应用户可见。</li><li><code>preview/*</code> 或 PR 分支：触发预览部署，生成独立子域，用于评审。</li><li><code>docs/*</code>：如果只改文档，可以配置为跳过构建或用更轻的命令。</li><li><code>release/*</code>：用于发布候选，通常会额外挂一层灰度域名。</li></ul><p>Pages 允许为不同分支配置不同的构建命令与环境变量，这一点在做「文档站与主站共用仓库」时特别有用——你不需要两个仓库，只需要两条构建路径。</p><h2 id="缩短构建时间的可操作清单"><a href="#缩短构建时间的可操作清单" class="headerlink" title="缩短构建时间的可操作清单"></a>缩短构建时间的可操作清单</h2><p>把前面几节落到操作层面，可以总结成一张清单：</p><ol><li>用 <code>npm ci</code> &#x2F; <code>pnpm install --frozen-lockfile</code> 替代 <code>install</code>，保证可复现并提升缓存命中。</li><li>把长命令收进 <code>package.json</code> 的 script，面板里只留一个短入口。</li><li>用 <code>.node-version</code> 或 <code>engines</code> 显式固定运行时版本。</li><li>清理不必要的构建依赖，把只在本地用到的工具移到 <code>devDependencies</code> 之外或独立脚本。</li><li>对大型站点做产物分片或延迟生成，避免单次构建触及超时上限。</li><li>在 CI 中加一步「产物大小检查」，防止无意中把几百 MB 的资源带进部署。</li></ol><p>最后一点尤其重要：Pages 的部署产物会分发到全球边缘节点，产物体积不仅影响构建上传时间，也影响用户的首字节时间。构建优化与运行时优化在这里是同一件事。</p><blockquote><p><strong>说明：</strong> 本文基于 Cloudflare Pages 公开的构建与部署机制进行工程化解读，具体配额、超时上限与运行时版本以 Cloudflare 官方最新文档为准；文中涉及的耗时比例为经验观察，不作为权威统计数据。观点部分为笔者基于实际项目经验的判断，欢迎按自身场景验证。</p></blockquote>]]>
    </content>
    <id>https://blogs.yzy15.dpdns.org/cf-pages-build-pipeline-anatomy/</id>
    <link href="https://blogs.yzy15.dpdns.org/cf-pages-build-pipeline-anatomy/"/>
    <published>2026-09-13T03:00:00.000Z</published>
    <summary>把 Cloudflare Pages 当成一个黑盒使用的人很多，能讲清楚一次 Git push 之后究竟发生了什么的人很少。本文拆解构建管线的四个阶段、缓存与增量策略、构建镜像与 Node 版本约束，并给出缩短构建耗时的可操作清单，帮助你把发布链路从「能跑」提升到「可预测」。</summary>
    <title>Cloudflare Pages 构建管线解剖：从 Git 提交到全球边缘的完整链路</title>
    <updated>2026-09-12T17:33:38.958Z</updated>
  </entry>
  <entry>
    <author>
      <name>mc-yzy15</name>
    </author>
    <category term="云计算" scheme="https://blogs.yzy15.dpdns.org/categories/%E4%BA%91%E8%AE%A1%E7%AE%97/"/>
    <category term="D1" scheme="https://blogs.yzy15.dpdns.org/tags/D1/"/>
    <category term="Schema迁移" scheme="https://blogs.yzy15.dpdns.org/tags/Schema%E8%BF%81%E7%A7%BB/"/>
    <category term="数据库" scheme="https://blogs.yzy15.dpdns.org/tags/%E6%95%B0%E6%8D%AE%E5%BA%93/"/>
    <category term="发布流程" scheme="https://blogs.yzy15.dpdns.org/tags/%E5%8F%91%E5%B8%83%E6%B5%81%E7%A8%8B/"/>
    <content>
      <![CDATA[<h1 id="边缘-SQLite-的-Schema-迁移：把-DDL-当作可回滚的发布流程"><a href="#边缘-SQLite-的-Schema-迁移：把-DDL-当作可回滚的发布流程" class="headerlink" title="边缘 SQLite 的 Schema 迁移：把 DDL 当作可回滚的发布流程"></a>边缘 SQLite 的 Schema 迁移：把 DDL 当作可回滚的发布流程</h1><p>应用代码可以灰度、可以回滚、可以同时跑两个版本，但数据库 schema 只有一个。在边缘 SQLite 场景下，这个矛盾会被放大：因为边缘应用是就近执行的，多个版本的 Worker 可能同时在线，它们会同时按自己的假设读写同一套表结构。本文讨论的是如何在这种约束下做 schema 迁移，而不是教 ALTER TABLE 的语法。</p><h2 id="SQLite-的-DDL-特性如何约束迁移设计"><a href="#SQLite-的-DDL-特性如何约束迁移设计" class="headerlink" title="SQLite 的 DDL 特性如何约束迁移设计"></a>SQLite 的 DDL 特性如何约束迁移设计</h2><p>SQLite 有一个与其他数据库很不相同的特性：它的 <code>ALTER TABLE</code> 能力非常有限。长期以来它只能做「加列」「重命名表」「重命名列」这类操作，而<strong>改列类型、改约束、删列、调整主键</strong>这些需求，需要通过「建新表 → 复制数据 → 删旧表 → 重命名」这一整套重建流程来完成。这个流程本身是标准做法，但它有一个直接的工程后果：<strong>很多看似微小的 schema 变更，其实是一次数据搬运</strong>。</p><p>数据搬运在我们关心的环境里有三个代价。第一是行读写开销，复制整表意味着读一遍写一遍，这与按行计费的模型直接相关。第二是执行时长，大表的复制可能超出单次请求或单条语句的时长限制。第三是原子性边界，重建过程中应用若还在写旧表，数据就会分叉。</p><p>另一个特性是 SQLite 对表结构变更的元数据操作会短暂锁表。在单机 SQLite 上这通常不是问题，但在托管的多读副本架构下，DDL 需要先在主库执行，再传播到副本，期间不同副本可能处于不同的 schema 版本。这就要求迁移设计必须假设「新旧 schema 会共存一段时间」。</p><p>这两条特性合起来，给出了迁移设计的第一原则：<strong>任何迁移都必须是向前兼容的，即旧代码在新 schema 上仍能正确运行。</strong></p><h2 id="expand-contract：把一次变更拆成三个阶段"><a href="#expand-contract：把一次变更拆成三个阶段" class="headerlink" title="expand-contract：把一次变更拆成三个阶段"></a>expand-contract：把一次变更拆成三个阶段</h2><p>为了让新旧代码与新老 schema 能安全共存，实践中最可靠的模型是 expand-contract，也叫「扩展-收缩」。它把一次 breaking change 拆成三个可独立发布的阶段。</p><ul><li><strong>扩展（expand）</strong>：只做加法。新增可空列、新增表、新增索引。这一步不修改任何既有结构，因此新旧代码都能正常运行。旧代码忽略新列，新代码在新列写入默认值。</li><li><strong>双写与回填（migrate）</strong>：新代码同时写新旧两处表达（新旧列、新旧表），并启动一个幂等的回填任务把历史数据补齐。此时读仍然走旧路径，保证结果稳定。</li><li><strong>收缩（contract）</strong>：等确认所有读路径都已切到新结构、且回填完成之后，再删除旧列或旧表。这一步是不可逆的，要放在所有灰度完成之后单独执行。</li></ul><p>这个模型的关键在于<strong>把不可逆操作推到流程最后</strong>，让前面每一阶段都可以安全回滚。回滚的定义也随之变得清晰：扩展阶段回滚只需取消发布，收缩阶段才需要真正的数据恢复预案。</p><p>举一个具体的例子：把 <code>users.name</code> 拆成 <code>first_name</code> 与 <code>last_name</code>。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 阶段 1：expand —— 只加列，可空，不动旧列</span></span><br><span class="line"><span class="keyword">ALTER TABLE</span> users <span class="keyword">ADD</span> <span class="keyword">COLUMN</span> first_name TEXT;</span><br><span class="line"><span class="keyword">ALTER TABLE</span> users <span class="keyword">ADD</span> <span class="keyword">COLUMN</span> last_name TEXT;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 阶段 3：contract —— 确认无读依赖后，重建表去掉旧列</span></span><br><span class="line"><span class="keyword">CREATE TABLE</span> users_new (</span><br><span class="line">  id <span class="type">INTEGER</span> <span class="keyword">PRIMARY KEY</span>,</span><br><span class="line">  first_name TEXT <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span>,</span><br><span class="line">  last_name TEXT <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span>,</span><br><span class="line">  email TEXT <span class="keyword">UNIQUE</span></span><br><span class="line">);</span><br><span class="line"><span class="keyword">INSERT INTO</span> users_new (id, first_name, last_name, email)</span><br><span class="line">  <span class="keyword">SELECT</span> id,</span><br><span class="line">         <span class="built_in">COALESCE</span>(first_name, name, <span class="string">&#x27;&#x27;</span>),</span><br><span class="line">         <span class="built_in">COALESCE</span>(last_name, <span class="string">&#x27;&#x27;</span>),</span><br><span class="line">         email</span><br><span class="line">  <span class="keyword">FROM</span> users;</span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> users;</span><br><span class="line"><span class="keyword">ALTER TABLE</span> users_new RENAME <span class="keyword">TO</span> users;</span><br></pre></td></tr></table></figure><p>注意阶段 3 里的 <code>COALESCE</code>：它会优先取已经回填好的新列，回退到旧列。这类兜底表达式是让重建过程在「回填尚未 100% 完成」时依然安全的常用手法。</p><h2 id="迁移与代码发布的顺序约束"><a href="#迁移与代码发布的顺序约束" class="headerlink" title="迁移与代码发布的顺序约束"></a>迁移与代码发布的顺序约束</h2><p>有了三阶段模型，还需要明确它与代码发布的相对顺序。核心规则只有一条：<strong>先发布能兼容新旧两种 schema 的代码，再改 schema</strong>。</p><p>具体到两个方向：</p><ul><li><strong>加列</strong>：先发布会写新列、但读旧列的代码，再执行 DDL。因为加列本身向下兼容，这个顺序的风险主要来自「代码先写了新列但列还不存在」，所以更稳妥的顺讯是先 DDL 再发代码——前提是 DDL 纯粹是加法且不阻塞。</li><li><strong>删列</strong>：必须先让所有读路径停止依赖该列，再删除。这要求你能确信没有旧版本 Worker 仍在读取它。</li></ul><p>在边缘环境里，「是否还有旧版本在跑」这个问题并不容易回答。Worker 的旧部署可能在短时间内仍然接收流量，长连接（如 WebSocket）会延长旧版本的生命周期。因此<strong>删列前的观察期应当以小时计，而不是以分钟计</strong>。这是边缘场景相对于传统单体应用的一个重要差异。</p><p>下表对比了不同变更类型在三个阶段中的风险分布，可以作为评审时的速查表。</p><table><thead><tr><th>变更类型</th><th>expand 阶段风险</th><th>migrate 阶段风险</th><th>contract 阶段风险</th><th>备注</th></tr></thead><tbody><tr><td>加可空列</td><td>极低</td><td>无</td><td>无</td><td>最常见也最安全</td></tr><tr><td>加非空列</td><td>低（需默认值）</td><td>无</td><td>无</td><td>默认值需与业务语义一致</td></tr><tr><td>改列类型</td><td>低</td><td>中（回填正确性）</td><td>高（数据不可逆）</td><td>必须走重建表流程</td></tr><tr><td>拆列&#x2F;合列</td><td>低</td><td>中到高</td><td>高</td><td>需双写与回填</td></tr><tr><td>加索引</td><td>低</td><td>无</td><td>无</td><td>注意写放大与存储</td></tr><tr><td>删列</td><td>无</td><td>无</td><td>高</td><td>必须确认无读依赖</td></tr><tr><td>改主键</td><td>极低</td><td>中</td><td>高</td><td>通常等价于重建全表</td></tr></tbody></table><h2 id="回填任务的幂等与断点续跑"><a href="#回填任务的幂等与断点续跑" class="headerlink" title="回填任务的幂等与断点续跑"></a>回填任务的幂等与断点续跑</h2><p>数据回填是整个流程里最容易出事故的环节，因为它要处理的数据量大、执行时间长、还可能中途失败。把它写成一次性脚本是危险的，正确的做法是把它当作一个可重复执行的作业。</p><p>设计要点有四个：</p><ol><li><strong>幂等的更新条件</strong>。回填语句应当只更新「尚未迁移」的行，例如 <code>WHERE first_name IS NULL</code>，这样重复执行不会产生副作用，也不会因为并发而互相覆盖。</li><li><strong>按主键区间分批</strong>。用 <code>WHERE id &gt; ? ORDER BY id LIMIT ?</code> 推进游标，避免 <code>OFFSET</code> 在大表上越来越慢。</li><li><strong>每批独立提交</strong>。批次大小与事务体量限制匹配，单批失败不影响已完成部分。</li><li><strong>可观测的进度</strong>。记录已处理的最大主键，既作为断点，也作为进度指标。</li></ol><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 幂等回填：只处理尚未迁移的行，按主键推进</span></span><br><span class="line"><span class="keyword">UPDATE</span> users</span><br><span class="line"><span class="keyword">SET</span> first_name <span class="operator">=</span> <span class="built_in">COALESCE</span>(first_name, substr(name, <span class="number">1</span>, instr(name <span class="operator">||</span> <span class="string">&#x27; &#x27;</span>, <span class="string">&#x27; &#x27;</span>) <span class="operator">-</span> <span class="number">1</span>)),</span><br><span class="line">    last_name  <span class="operator">=</span> <span class="built_in">COALESCE</span>(last_name, <span class="string">&#x27;&#x27;</span>)</span><br><span class="line"><span class="keyword">WHERE</span> id <span class="operator">&gt;</span> ?</span><br><span class="line">  <span class="keyword">AND</span> id <span class="operator">&lt;=</span> ?</span><br><span class="line">  <span class="keyword">AND</span> first_name <span class="keyword">IS</span> <span class="keyword">NULL</span>;</span><br></pre></td></tr></table></figure><p>这个模式还有一个额外好处：它天然支持在流量低峰期慢速推进，把回填对线上读写的影响摊平。如果你的环境有调度能力，把回填做成定时任务比一次性执行更稳。</p><h2 id="迁移的可观测性与回滚预案"><a href="#迁移的可观测性与回滚预案" class="headerlink" title="迁移的可观测性与回滚预案"></a>迁移的可观测性与回滚预案</h2><p>一个没有被观测的迁移等于一次盲飞。最低限度需要采集的信息包括：当前 schema 版本、已回填行数占总数的比例、迁移期间的错误率与延迟变化、以及新旧读路径的结果差异比例。</p><p>关于最后一项，有一个非常实用的技巧：在双写阶段<strong>同时读新旧两处并比对结果</strong>，把不一致的样本记录下来但不影响返回。这能让你在真正切换读路径之前就发现回填逻辑的缺陷，而不是等用户报错。</p><p>回滚预案则要按阶段区分：</p><ul><li>expand 阶段：直接回滚代码即可，schema 的加法无需撤销。</li><li>migrate 阶段：回滚代码会停止双写，但已回填的数据保留，通常无害；需要确认回滚后的读路径能正确解释这些数据。</li><li>contract 阶段：这是唯一真正不可逆的阶段。执行前应当有一次可验证的数据快照，并演练过从快照恢复的流程。</li></ul><blockquote><p>一条必须写进规范的规则：<strong>收缩阶段永远单独发布，不与任何功能变更混在同一次上线里</strong>。把不可逆操作和功能变更耦合在一起，是排查事故时最让人头疼的组合。</p></blockquote><h2 id="团队实践：把迁移纳入版本控制与流程"><a href="#团队实践：把迁移纳入版本控制与流程" class="headerlink" title="团队实践：把迁移纳入版本控制与流程"></a>团队实践：把迁移纳入版本控制与流程</h2><p>最后是流程层面的约定。成熟的团队会把迁移脚本纳入版本控制，用编号或时间戳排序，并由 CI 在部署前自动执行未应用的迁移。这样做的好处是迁移历史可审计、环境可重建、评审时能看清变更范围。</p><p>配套的约定还包括：</p><ul><li>每条迁移脚本必须写明<strong>对应的回滚方案</strong>，哪怕回滚只是「从快照恢复」。</li><li>迁移脚本不允许包含业务逻辑，只做结构变更与数据搬运。</li><li>迁移前必须评估行触达量，超过一定规模的要走分批与低峰执行。</li><li>所有迁移在预发布环境至少完整跑过一次，包括回滚演练。</li><li>破坏性变更必须在 PR 描述里显式标注，并说明验证方式。</li></ul><p>把这五条固化下来之后，数据库变更就从「上线前夜的手工操作」变成了普通的一次发布。这是边缘数据库项目从原型走向长期维护的必要一步。</p><blockquote><p><strong>说明：</strong> 本文为原创技术分析，讨论 SQLite 与 D1 场景下的 schema 迁移方法论。SQLite 对 DDL 的支持能力与 D1 的执行与配额限制均可能随版本更新而变化，请以官方最新文档为准。文中 SQL 为讲解用途，涉及重建大表时请在实际环境中先验证耗时与锁影响。</p></blockquote>]]>
    </content>
    <id>https://blogs.yzy15.dpdns.org/cf-d1-sqlite-schema-migration/</id>
    <link href="https://blogs.yzy15.dpdns.org/cf-d1-sqlite-schema-migration/"/>
    <published>2026-09-13T02:04:00.000Z</published>
    <summary>讨论在边缘 SQLite（D1）上做 schema 迁移的工程方法，包括 SQLite 的 DDL 限制如何影响迁移设计、expand-contract 三阶段模型的落地步骤、迁移与代码发布的顺序约束、回滚策略与数据回填的幂等写法，帮助团队把数据库变更从手工操作变成可审计的发布流程。</summary>
    <title>边缘 SQLite 的 Schema 迁移：把 DDL 当作可回滚的发布流程</title>
    <updated>2026-09-12T17:33:38.957Z</updated>
  </entry>
  <entry>
    <author>
      <name>mc-yzy15</name>
    </author>
    <category term="开发者生态" scheme="https://blogs.yzy15.dpdns.org/categories/%E5%BC%80%E5%8F%91%E8%80%85%E7%94%9F%E6%80%81/"/>
    <category term="工程实践" scheme="https://blogs.yzy15.dpdns.org/tags/%E5%B7%A5%E7%A8%8B%E5%AE%9E%E8%B7%B5/"/>
    <category term="Vibe Coding" scheme="https://blogs.yzy15.dpdns.org/tags/Vibe-Coding/"/>
    <category term="交互范式" scheme="https://blogs.yzy15.dpdns.org/tags/%E4%BA%A4%E4%BA%92%E8%8C%83%E5%BC%8F/"/>
    <category term="能力分层" scheme="https://blogs.yzy15.dpdns.org/tags/%E8%83%BD%E5%8A%9B%E5%88%86%E5%B1%82/"/>
    <content>
      <![CDATA[<h1 id="从自动补全到意图编程：Vibe-Coding-的光谱与能力分层"><a href="#从自动补全到意图编程：Vibe-Coding-的光谱与能力分层" class="headerlink" title="从自动补全到意图编程：Vibe Coding 的光谱与能力分层"></a>从自动补全到意图编程：Vibe Coding 的光谱与能力分层</h1><p>在上一篇文章里，我们把 Vibe Coding 从「AI 辅助编程」「低代码」「提示词生成应用」这几个概念里切了出来。但切开之后还有一件事没做：<strong>Vibe Coding 自己内部也不是一个东西。</strong></p><p>让模型补全一行 if 判断，和让一个 agent 自己读仓库、改五个文件、跑测试、失败了自己重试——这两件事的差别，比「Vibe Coding 和低代码的差别」还要大。把它们统称为「用 AI 写代码」，就等于把自行车和货轮统称为「交通工具」。</p><p>这篇文章试图建立一个可用的分层模型。它不追求术语精确，追求的是<strong>让团队在讨论工具选型时，能指着某一层说「我们要的是这个，不是那个」。</strong></p><h2 id="两条轴：控制权与验证强度，以及由此得出的五个层级"><a href="#两条轴：控制权与验证强度，以及由此得出的五个层级" class="headerlink" title="两条轴：控制权与验证强度，以及由此得出的五个层级"></a>两条轴：控制权与验证强度，以及由此得出的五个层级</h2><p>分层之前先确定坐标。观察大量实际工作流后，笔者发现区分度最高的两个变量是：</p><ul><li><strong>控制权（Control）</strong>：在一轮修改中，谁决定「改哪里、改成什么」。人逐步确认是最保守的一端，模型自主决策是另一端。</li><li><strong>验证强度（Verification）</strong>：改动是否被自动化手段拦截。没有检查是最弱的一端，编译器 + 类型系统 + 测试 + 静态分析全绿是最强的一端。</li></ul><p>关键在于，<strong>这两条轴是独立的</strong>。很多人以为「模型越自主，验证必然越弱」，其实不然：一个被约束在「改完必须让 pnpm test 通过、否则回滚」循环里的 agent，其验证强度远高于一个人肉复制粘贴模型输出、然后直接 git commit 的开发者。</p><p>反过来也成立：人手动改一行代码，验证强度也可能是零。控制权和验证强度是两个旋钮，可以分开拧。</p><p>理解了这一点，就能避免一个常见错误——用「谁写的代码」来判断风险。风险取决于验证，不取决于作者是人还是模型。</p><p>把它们组合起来，就得到下面这个按控制权从人到模型、验证强度从弱到强综合排序的分层，每一层都对应一种已经被广泛使用的交互形态。</p><table><thead><tr><th>层级</th><th>名称</th><th>典型交互</th><th>控制权</th><th>验证强度</th><th>人的核心动作</th></tr></thead><tbody><tr><td>L0</td><td>自动补全</td><td>行内 Tab 接受&#x2F;拒绝</td><td>人</td><td>沿用原流程</td><td>判断这行对不对</td></tr><tr><td>L1</td><td>对话式生成</td><td>选中代码，让模型改写，人工应用</td><td>人</td><td>沿用原流程</td><td>审查 diff</td></tr><tr><td>L2</td><td>带护栏的生成</td><td>模型改代码，自动跑 lint&#x2F;测试，失败回灌</td><td>混合</td><td>中到强</td><td>定义护栏、看结果</td></tr><tr><td>L3</td><td>任务级 Agent</td><td>给一个任务，agent 自行多文件修改并验证</td><td>模型</td><td>强（依赖护栏）</td><td>写任务描述、验收益</td></tr><tr><td>L4</td><td>意图编程</td><td>描述目标，系统持续演化产物并自证</td><td>模型</td><td>取决于系统设计</td><td>定义目标与验收标准</td></tr></tbody></table><p>需要强调：<strong>层级越高不代表越先进，只代表让渡的控制权越多。</strong> L4 不是 L0 的进化终点，它们是不同工具，服务不同问题。</p><h2 id="L0-L1：把机械劳动还给人脑之外"><a href="#L0-L1：把机械劳动还给人脑之外" class="headerlink" title="L0-L1：把机械劳动还给人脑之外"></a>L0-L1：把机械劳动还给人脑之外</h2><p>L0 和 L1 是当今覆盖人数最多的两层，也是最没有争议的两层。</p><p>L0 的形态是补全。它的价值不在「生成得多聪明」，而在<strong>摩擦极低</strong>：一次按键，一个 Tab，接受或忽略，全程不打断思考。这一层的正确使用方式是「把它当成一个打字速度更快的自己」，而不是「当成一个会思考的同事」。</p><p>L1 的形态是对话式改写：选中一段代码，说「把这个回调改成 async&#x2F;await，顺便处理错误」，然后审查 diff。这一层已经能显著改变工作节奏，因为它处理的是<strong>有语义的整块逻辑</strong>。但它的关键约束没变：<strong>人必须读 diff</strong>。跳过 diff 的人，其实已经越级到了 L2 的风险区间，却还在享受 L1 的心理安全感。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># L1 使用者的典型日常——改完立刻看 diff，再看类型检查</span></span><br><span class="line">git diff --<span class="built_in">stat</span></span><br><span class="line">git diff -- src/services/payment.ts</span><br><span class="line">pnpm tsc --noEmit          <span class="comment"># 类型是第一道最便宜的护栏</span></span><br><span class="line">pnpm <span class="built_in">test</span> -- --changed     <span class="comment"># 只跑受影响的测试</span></span><br></pre></td></tr></table></figure><p>这两层的隐性成本很低，唯一需要注意的是<strong>注意力债务</strong>：当补全过于流畅，人会逐渐降低审阅标准，把「看起来对」当成「是对的」。对抗手段很简单——把检查交给命令，而不是交给眼睛。</p><h2 id="L2：真正开始有价值的分水岭"><a href="#L2：真正开始有价值的分水岭" class="headerlink" title="L2：真正开始有价值的分水岭"></a>L2：真正开始有价值的分水岭</h2><p>L2 是笔者认为当前性价比最高的一层，也是被讨论得最少的一层。</p><p>它的定义是：模型直接修改工作区文件，但每一次修改后系统自动执行一组检查，检查结果被回灌给模型，模型据此修正，直到通过或者超出重试上限。人做的事从「读每一行代码」变成「定义护栏 + 判断业务语义」。</p><p>这一层的价值来自一个被低估的事实：<strong>大部分 AI 生成的代码缺陷是机械性的、可被机器发现的</strong>。类型不匹配、未处理的 Promise、导入路径错误、格式违规——这些占失败原因的绝大多数，而它们全都能被现有工具零成本捕获。一旦把这些交给自动循环，人就只剩下真需要人判断的部分：这个逻辑符合业务吗？这个抽象合理吗？</p><p>一个最小可用的护栏脚本大致长这样：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// package.json 中的检查聚合脚本（示意，命令名以本地配置为准）</span></span><br><span class="line"><span class="keyword">const</span> checks = [</span><br><span class="line">  [<span class="string">&#x27;typecheck&#x27;</span>, <span class="string">&#x27;tsc --noEmit&#x27;</span>],</span><br><span class="line">  [<span class="string">&#x27;lint&#x27;</span>, <span class="string">&#x27;eslint . --max-warnings=0&#x27;</span>],</span><br><span class="line">  [<span class="string">&#x27;test&#x27;</span>, <span class="string">&#x27;vitest run --changed&#x27;</span>],</span><br><span class="line">];</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">const</span> [name, cmd] <span class="keyword">of</span> checks) &#123;</span><br><span class="line">  <span class="keyword">const</span> r = <span class="title function_">run</span>(cmd);</span><br><span class="line">  <span class="keyword">if</span> (r.<span class="property">code</span> !== <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">error</span>(<span class="string">`[gate:<span class="subst">$&#123;name&#125;</span>] failed\n<span class="subst">$&#123;r.stdout&#125;</span>`</span>);</span><br><span class="line">    process.<span class="title function_">exit</span>(<span class="number">1</span>); <span class="comment">// 交给循环，而不是交给人</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>L2 的升级条件是<strong>你真的配置了护栏</strong>。没有护栏的 L2 会退化成「模型自动改一堆文件，人一脸茫然地 review 五百行 diff」——那比 L1 更差，因为决策颗粒度变粗了。</p><h2 id="L3：任务级-Agent-的收益与陷阱"><a href="#L3：任务级-Agent-的收益与陷阱" class="headerlink" title="L3：任务级 Agent 的收益与陷阱"></a>L3：任务级 Agent 的收益与陷阱</h2><p>L3 是近两年变化最快的层。交互形态从「改这段代码」变成「把这个需求做掉」：agent 自行搜索相关文件、规划修改、执行、验证、必要时回滚，结束时交付一个可评审的变更集。</p><p>它真正的收益不在速度，而在<strong>并行度</strong>。一个人可以同时推进多个 L3 任务，只要这些任务的边界清晰、文件不重叠。这让「一个人做一个小团队的事」第一次变得在工程上说得通，而不只是营销话术。</p><p>它的陷阱也集中在三处：</p><ul><li><strong>任务描述质量决定一切</strong>。L3 不是「随便说说」，好的任务描述包含目标、边界、验收标准、禁止事项，写起来接近写一份小型的 RFC。很多人抱怨 agent 不靠谱，其实是任务描述只有一句话。</li><li><strong>上下文污染</strong>。agent 读到的旧代码、过期的注释、废弃的模块，会直接影响它的方案选择。仓库越混乱，L3 产出越随机。</li><li><strong>错误的自信</strong>。agent 在无法完成任务时，倾向于交付一个「看起来完成」的版本，而不是明确说「我做不到」。所以验收标准必须由人预先定义，而不是事后从产物反推。</li></ul><p>笔者的经验法则是：<strong>L3 适合「边界清晰、验收可自动化」的任务</strong>，例如补测试、批量迁移 API、修复类型错误、给模块补齐日志。不适合「需要判断产品取舍」的任务。</p><h2 id="L4：意图编程的边界在哪里"><a href="#L4：意图编程的边界在哪里" class="headerlink" title="L4：意图编程的边界在哪里"></a>L4：意图编程的边界在哪里</h2><p>L4 是标题里那个「意图编程」。它的想象图景是：你描述目标，系统自己规划、实现、验证、迭代，你只负责定义什么叫做「好」。</p><p>必须诚实地讲，<strong>L4 目前只有非常窄的适用范围</strong>。它在以下条件同时成立时才可能工作：</p><ol><li>目标是<strong>可机器验证的</strong>（有测试、有明确的输入输出、有形式化约束）；</li><li>反馈循环足够快（几秒到几分钟，而不是几小时）；</li><li>搜索空间有界（技术栈固定、依赖可控、接口有限）。</li></ol><p>一旦缺少其中任意一条，L4 就会退化成一个成本更高、可控性更差的 L3。这也是为什么「一句话生成一个完整产品」在今天仍然是演示大于生产：演示的生命周期是十分钟，生产系统的生命周期是三年，而后者需要的是可维护性，不是生成速度。</p><p>对绝大多数团队，务实的做法是<strong>把 L4 当方向，把 L2&#x2F;L3 当抓手</strong>：先让验证自动化，再让模型自主。顺序反了会很痛苦——先放权再补护栏，等于在没有安全带的时候上高速。</p><h2 id="决策建议：怎么选你的层级"><a href="#决策建议：怎么选你的层级" class="headerlink" title="决策建议：怎么选你的层级"></a>决策建议：怎么选你的层级</h2><p>把上面的分析收敛成一张决策路径，可以直接用于团队讨论：</p><ul><li><strong>产物是临时的、失败了无后果</strong>？跳到 L3 甚至 L4，不要犹豫，这是这些层级最有价值的场景。</li><li><strong>产物要长期维护</strong>？起点是 L0&#x2F;L1，先把补全和对话式改写用熟，成本几乎为零。</li><li><strong>仓库有测试但覆盖率不高</strong>？优先投入 L2，先补护栏再提自动化程度，这是投入产出比最高的动作。</li><li><strong>任务可并行、边界清晰</strong>？上 L3，但要为每个任务写清验收标准。</li><li><strong>目标无法机器验证</strong>？停在 L2&#x2F;L3，不要指望 L4，人工判断这一环无法被跳过。</li><li><strong>团队禁止读代码的人工智能产物合入主干</strong>？那就把这条规则写进规范，而不是靠个人自觉。</li></ul><p>一句话版本：<strong>顺着「验证先于自主」的顺序往上爬，而不是顺着「模型能力强弱」往上换工具。</strong></p><h2 id="结论：分层是为了分权，不是为了排名"><a href="#结论：分层是为了分权，不是为了排名" class="headerlink" title="结论：分层是为了分权，不是为了排名"></a>结论：分层是为了分权，不是为了排名</h2><p>把 Vibe Coding 拆成 L0 到 L4，最大的作用不是排序，而是<strong>让「谁能改、谁验证」这件事变成显式选择</strong>。</p><p>L0 和 L1 几乎不需要治理，因为控制权还在人手里。真正需要提前设计的是 L2 及以上的护栏形态：跑哪些检查、失败几次放弃、谁最终签字、变更集多大算可评审。这些问题没有标准答案，但每个团队都应该有答案。</p><p>最后提醒一句：层级不是身份。同一个人在同一个下午，写业务逻辑时用 L1，写一次性脚本时用 L4，这是非常健康的用法。危险的是用同一套心智模型对待所有代码——那才是把工具用坏的方式。</p><blockquote><p><strong>说明：</strong> 本文的 L0-L4 分层为笔者自建的分析框架，用于工程讨论与工具选型，并非行业标准或任何厂商的官方定义。文中涉及的命令、包名与写法均为主流常见形式，具体行为以各工具官方最新文档为准；未引用任何未公开的统计数据。关于「L4 适用范围仍然很窄」的判断属笔者主观评估，会随工具与验证技术进步而变化，请结合自身项目风险承受能力判断。</p></blockquote>]]>
    </content>
    <id>https://blogs.yzy15.dpdns.org/vibe-probe-spectrum/</id>
    <link href="https://blogs.yzy15.dpdns.org/vibe-probe-spectrum/"/>
    <published>2026-09-13T02:03:00.000Z</published>
    <summary>把「AI 写代码」当成一个整体来讨论，是多数团队做错技术决策的起点。本文用控制权与验证强度两条轴，把它拆成 L0 到 L4 五个层级，说明每一层的适用边界、隐性成本与升级条件，并给出可落地的选择决策树。</summary>
    <title>从自动补全到意图编程：Vibe Coding 的光谱与能力分层</title>
    <updated>2026-09-12T17:33:38.964Z</updated>
  </entry>
  <entry>
    <author>
      <name>mc-yzy15</name>
    </author>
    <category term="云计算" scheme="https://blogs.yzy15.dpdns.org/categories/%E4%BA%91%E8%AE%A1%E7%AE%97/"/>
    <category term="云计算" scheme="https://blogs.yzy15.dpdns.org/tags/%E4%BA%91%E8%AE%A1%E7%AE%97/"/>
    <category term="D1" scheme="https://blogs.yzy15.dpdns.org/tags/D1/"/>
    <category term="成本优化" scheme="https://blogs.yzy15.dpdns.org/tags/%E6%88%90%E6%9C%AC%E4%BC%98%E5%8C%96/"/>
    <category term="配额" scheme="https://blogs.yzy15.dpdns.org/tags/%E9%85%8D%E9%A2%9D/"/>
    <content>
      <![CDATA[<h1 id="D1-成本与配额模型拆解：行读写、存储与按量计费的隐藏成本"><a href="#D1-成本与配额模型拆解：行读写、存储与按量计费的隐藏成本" class="headerlink" title="D1 成本与配额模型拆解：行读写、存储与按量计费的隐藏成本"></a>D1 成本与配额模型拆解：行读写、存储与按量计费的隐藏成本</h1><p>在传统关系型数据库里，估算成本主要看实例规格和并发连接数，SQL 写得好不好通常不影响账单。到了边缘托管的 SQLite 方案上，这套直觉会失效：D1 的计费核心是<strong>行读写次数</strong>与<strong>存储量</strong>，而不是实例规格。这意味着一次慢查询和一次快查询可能同价，而一次被索引优化掉的扫描和一次全表扫描可能差出好几个数量级。理解这个模型，是控制成本的前提。</p><h2 id="计费维度到底看什么"><a href="#计费维度到底看什么" class="headerlink" title="计费维度到底看什么"></a>计费维度到底看什么</h2><p>按公开的计费口径，D1 的费用主要由几块构成：读操作触及的行数、写操作触及的行数、存储的数据量与备份占用、以及超出免费额度后的按量费用。不同套餐在额度、并发和可用能力上有差别，具体数值以官方定价页为准。本文关心的是这些维度背后的行为机制，而不是记住一串会变的数字。</p><p>需要特别理解的一点是「行」这个计量单位。一条 <code>SELECT</code> 如果通过索引精确定位到一行，成本约等于一次行读；如果它退化成全表扫描，扫过的每一行都可能计入读行数。所以在 D1 上，<strong>索引不是性能优化，而是成本优化</strong>。这跟传统数据库里「索引主要为了快」的心智模型有本质区别。</p><p>写操作同理。单行 <code>INSERT</code> 计一次写；如果表上有触发器或者外键级联，间接影响的行也可能被计入。批量插入 N 行，写行数通常是 N 而不是 1。这些规则看起来直白，但在嵌套结构、级联删除和 upsert 场景里很容易被低估。</p><h2 id="查询次数与行数的分离：一个反直觉的例子"><a href="#查询次数与行数的分离：一个反直觉的例子" class="headerlink" title="查询次数与行数的分离：一个反直觉的例子"></a>查询次数与行数的分离：一个反直觉的例子</h2><p>看下面两个语义等价、成本却可能差出几十倍的写法。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 写法 A：先查作者，再逐条查评论（N+1 模式）</span></span><br><span class="line"><span class="keyword">SELECT</span> id, name <span class="keyword">FROM</span> users <span class="keyword">WHERE</span> id <span class="operator">=</span> ?;          <span class="comment">-- 约 1 行读</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> comments <span class="keyword">WHERE</span> post_id <span class="operator">=</span> ?;         <span class="comment">-- 约 M 行读</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 写法 B：一次 JOIN 完成，且利用索引把扫描范围收窄</span></span><br><span class="line"><span class="keyword">SELECT</span> c.id, c.body, u.name</span><br><span class="line"><span class="keyword">FROM</span> comments c</span><br><span class="line"><span class="keyword">JOIN</span> users u <span class="keyword">ON</span> u.id <span class="operator">=</span> c.user_id</span><br><span class="line"><span class="keyword">WHERE</span> c.post_id <span class="operator">=</span> ?</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> c.created_at <span class="keyword">DESC</span></span><br><span class="line">LIMIT <span class="number">20</span>;</span><br></pre></td></tr></table></figure><p>写法 A 在应用层循环执行时，每次循环都要重新走一遍网络与查询解析，如果循环里还带上了表扫描，读行数会随评论数量线性增长。写法 B 的读行数上界主要由参与结果集的行数决定，配合 <code>comments(post_id, created_at)</code> 这样的复合索引，扫描范围可以被压到接近 LIMIT 的大小。</p><p>这不是说 JOIN 永远更便宜。当结果集很大、或者连接条件无法走索引时，一次 JOIN 扫描的行数可能比多次点查更大。真正的原则是：<strong>先算清楚每种写法的行触达量，再决定形态</strong>，而不是背「JOIN 更快」或者「点查更省」这类口号。</p><h2 id="索引的双面性：省读行，涨写成本"><a href="#索引的双面性：省读行，涨写成本" class="headerlink" title="索引的双面性：省读行，涨写成本"></a>索引的双面性：省读行，涨写成本</h2><p>索引把读成本压下去的同时，会抬高写成本。每插入一行，所有相关索引都需要维护，这会体现为额外的写操作开销，同时占用存储。在 D1 这种按行计费的模型里，索引的净收益取决于读写比。</p><p>判断一个索引是否值得，可以用下面这张表做快速对照。它把几类常见索引按收益与代价分档，方便在评审时快速决策。</p><table><thead><tr><th>索引类型</th><th>读收益</th><th>写代价</th><th>适用判断</th></tr></thead><tbody><tr><td>高频等值查询列</td><td>高，可直接定位行</td><td>低，单列维护</td><td>几乎总是值得</td></tr><tr><td>复合索引（前导列高频）</td><td>高，可覆盖范围扫描</td><td>中，列多则维护成本上升</td><td>有明确查询模式时值得</td></tr><tr><td>低区分度列（如布尔位）</td><td>低，选择性差</td><td>低但无收益</td><td>通常不值得，删掉</td></tr><tr><td>纯写表的索引</td><td>无读需求</td><td>高，纯增负担</td><td>审计类表应尽量少建</td></tr><tr><td>覆盖索引</td><td>高，可避免回表</td><td>中到高，存储占用大</td><td>热点查询值得</td></tr></tbody></table><p>一个常被忽略的点是<strong>低区分度索引</strong>。给 <code>status</code> 这种只有几个取值的列单独建索引，优化器很可能根本不选它，但每次写入都要维护它。这类索引是纯成本，审计时应该优先清理。</p><h2 id="批量写入与小事务的取舍"><a href="#批量写入与小事务的取舍" class="headerlink" title="批量写入与小事务的取舍"></a>批量写入与小事务的取舍</h2><p>写成本与「写行数」挂钩，这让批量写入看起来是省钱利器——一次请求提交 1000 行，比 1000 次请求各提交 1 行省掉了大量网络往返和请求开销。这个方向是对的，但有两个边界必须注意。</p><p>第一是<strong>事务体量上限</strong>。单次事务过大可能触及服务端的限制，具体阈值以官方文档为准。稳妥的做法是按几百到一千行分批，而不是一次灌入几万行。</p><p>第二是<strong>失败代价</strong>。一次提交 1000 行的事务失败，重试意味着全部 1000 行的写开销重新计一遍（在服务端已经部分写入的情况下尤其如此）。所以分批大小需要与幂等设计配合，让重试不会产生重复数据。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 分批 upsert，配合幂等键避免重试产生重复写</span></span><br><span class="line"><span class="keyword">const</span> <span class="variable constant_">BATCH</span> = <span class="number">500</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">upsertMany</span>(<span class="params">db, rows</span>) &#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; rows.<span class="property">length</span>; i += <span class="variable constant_">BATCH</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> chunk = rows.<span class="title function_">slice</span>(i, i + <span class="variable constant_">BATCH</span>);</span><br><span class="line">    <span class="keyword">const</span> placeholders = chunk.<span class="title function_">map</span>(<span class="function">() =&gt;</span> <span class="string">&quot;(?, ?, ?)&quot;</span>).<span class="title function_">join</span>(<span class="string">&quot;, &quot;</span>);</span><br><span class="line">    <span class="keyword">const</span> values = chunk.<span class="title function_">flatMap</span>(<span class="function">(<span class="params">r</span>) =&gt;</span> [r.<span class="property">id</span>, r.<span class="property">name</span>, r.<span class="property">updatedAt</span>]);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">await</span> db</span><br><span class="line">      .<span class="title function_">prepare</span>(</span><br><span class="line">        <span class="string">`INSERT INTO items (id, name, updated_at)</span></span><br><span class="line"><span class="string">         VALUES <span class="subst">$&#123;placeholders&#125;</span></span></span><br><span class="line"><span class="string">         ON CONFLICT(id) DO UPDATE SET</span></span><br><span class="line"><span class="string">           name = excluded.name,</span></span><br><span class="line"><span class="string">           updated_at = excluded.updated_at`</span></span><br><span class="line">      )</span><br><span class="line">      .<span class="title function_">bind</span>(...values)</span><br><span class="line">      .<span class="title function_">run</span>();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>ON CONFLICT DO UPDATE</code> 在这个场景里很实用：它把「先查是否存在再决定插入还是更新」的两步逻辑压成一步，省掉了那一次判定用的读行。这是一个典型的「用写法换成本」的例子。</p><h2 id="存储、备份与那些不显眼的费用"><a href="#存储、备份与那些不显眼的费用" class="headerlink" title="存储、备份与那些不显眼的费用"></a>存储、备份与那些不显眼的费用</h2><p>除了行读写，还有几项容易被漏算的成本：</p><ul><li><strong>存储量</strong>：按实际数据体积计费。文本表长期只增不删，体积会缓慢膨胀，需要归档策略。</li><li><strong>备份与时间点恢复</strong>：保留策略越长，占用与成本越高。多数团队并不需要很长的保留窗口。</li><li><strong>导出与迁移流量</strong>：把数据导出到对象存储做分析时会产生传输与请求开销，规划时要一并算进去。</li><li><strong>闲置额度</strong>：套餐内额度不会结转，低峰期省钱、高峰期超支是常态，预算应按峰值估算。</li></ul><blockquote><p>一条实用经验：先用「每千次请求的预估行触达量」乘以峰值 QPS，估算月度成本量级，再和免费额度对照。先把量级算对，比精确到分钱更重要——量级错了，优化方向一定是错的。</p></blockquote><h2 id="把成本压下来的惯用手法"><a href="#把成本压下来的惯用手法" class="headerlink" title="把成本压下来的惯用手法"></a>把成本压下来的惯用手法</h2><p>下面这些手法在多数边缘数据库项目里都适用，按收益从高到低排列：</p><ol><li><strong>给热点查询建对索引</strong>，尤其是带 <code>LIMIT</code> 的分页查询，让扫描范围与返回行数同阶。</li><li><strong>消灭 N+1</strong>，把循环里的点查合并成一次批量查询或一次 JOIN。</li><li><strong>用 upsert 替代「查后写」</strong>，省掉判定所需的读行。</li><li><strong>读走缓存</strong>，把可容忍陈旧的读取放到 KV 或 Cache API 上，让 D1 只承接需要一致性的查询。</li><li><strong>写入分片批量化</strong>，按几百行一批提交，同时保证幂等。</li><li><strong>定期清理低区分度索引</strong>，把纯写负担去掉。</li><li><strong>给只增表加归档</strong>，把历史数据搬到对象存储，保持主表体积可控。</li></ol><p>这七条里，前三条通常能解决大部分超支问题，后四条属于长期治理。团队的评审清单里应该固化前三条，把它们当作代码合并的硬要求。</p><h2 id="成本模型对架构的反向塑造"><a href="#成本模型对架构的反向塑造" class="headerlink" title="成本模型对架构的反向塑造"></a>成本模型对架构的反向塑造</h2><p>理解 D1 的计费方式之后，一些架构选择会变得显而易见。比如：</p><ul><li><strong>聚合统计不放在 D1 上实时算</strong>。全表聚合意味着读行数等于表大小，次数一多账单会失控。更好的做法是周期性把结果物化到小表，或者干脆放进 KV 做读缓存。</li><li><strong>日志与审计数据不进主库</strong>。这类数据只需要写不需要就地查，放到对象存储更合适，D1 只保留必要索引。</li><li><strong>把「读多写少」和「写多读少」拆开</strong>。前者适合放 D1 加缓存，后者也许更适合直接落对象存储再离线分析。</li></ul><p>换句话说，成本模型不只是财务问题，它会一路影响你的表设计、查询写法和模块拆分。越早把这个模型引入架构评审，后期的返工越少。</p><blockquote><p><strong>说明：</strong> 本文为原创技术分析，探讨的是 D1 计费与配额模型背后的机制与工程取舍。文中未给出精确价格与配额数值，因为这类数字会随套餐与官方政策调整，请以 Cloudflare 官方定价与配额文档为准。示例 SQL 与代码为讲解用途，生产使用前请核对当前语法与 API。</p></blockquote>]]>
    </content>
    <id>https://blogs.yzy15.dpdns.org/cf-d1-sqlite-quota-cost-model/</id>
    <link href="https://blogs.yzy15.dpdns.org/cf-d1-sqlite-quota-cost-model/"/>
    <published>2026-09-13T02:02:00.000Z</published>
    <summary>从计费维度出发拆解 Cloudflare D1 的成本结构，讲清「行读写次数」而非「查询次数」计费如何改变架构设计、索引与外键如何间接放大账单、批量写入与大事务的取舍，以及如何在保持正确性的前提下把单位请求成本压下来，适合正在做边缘数据库预算评估的团队参考。</summary>
    <title>D1 成本与配额模型拆解：行读写、存储与按量计费的隐藏成本</title>
    <updated>2026-09-12T17:33:38.957Z</updated>
  </entry>
  <entry>
    <author>
      <name>mc-yzy15</name>
    </author>
    <category term="云计算" scheme="https://blogs.yzy15.dpdns.org/categories/%E4%BA%91%E8%AE%A1%E7%AE%97/"/>
    <category term="D1" scheme="https://blogs.yzy15.dpdns.org/tags/D1/"/>
    <category term="SQLite" scheme="https://blogs.yzy15.dpdns.org/tags/SQLite/"/>
    <category term="数据一致性" scheme="https://blogs.yzy15.dpdns.org/tags/%E6%95%B0%E6%8D%AE%E4%B8%80%E8%87%B4%E6%80%A7/"/>
    <category term="边缘计算" scheme="https://blogs.yzy15.dpdns.org/tags/%E8%BE%B9%E7%BC%98%E8%AE%A1%E7%AE%97/"/>
    <content>
      <![CDATA[<h1 id="D1-与-SQLite-的读副本一致性模型：延迟、顺序与故障恢复"><a href="#D1-与-SQLite-的读副本一致性模型：延迟、顺序与故障恢复" class="headerlink" title="D1 与 SQLite 的读副本一致性模型：延迟、顺序与故障恢复"></a>D1 与 SQLite 的读副本一致性模型：延迟、顺序与故障恢复</h1><p>把数据库搬到边缘，第一个被现实击碎的美好幻想往往不是性能，而是「为什么我刚刚写入的数据查不到了」。这不是 D1 的 bug，而是分布式系统里读副本一致性模型在所有边缘数据库上的必然表现。本文不谈怎么建表，只谈一件事：当一次写操作返回成功之后，世界各地的读者究竟在多长时间里、在多远的距离上，能看到一个「什么样」的数据版本。</p><h2 id="为什么边缘读会「看不到」刚写的行"><a href="#为什么边缘读会「看不到」刚写的行" class="headerlink" title="为什么边缘读会「看不到」刚写的行"></a>为什么边缘读会「看不到」刚写的行</h2><p>D1 的架构本质上是「一个主库 + 一组读副本」，主库负责所有写入，读副本分布在离用户更近的位置负责读取。这个模型决定了写入必须先落到主库并被确认提交，随后才通过复制通道投递到各副本。任何读副本上的查询，返回的都是<strong>某个已经复制完成的快照</strong>，而不是主库此刻的真实状态。</p><p>因此「写后立即读」在边缘会遇到一个明确的窗口期：写请求的响应在网络里返回给客户端的同一时刻，复制流可能还停在离主库最近的节点上。用户刷新页面，请求被路由到一个尚未追上进度的副本，于是看到了旧值。这个窗口通常以毫秒到百毫秒量级计，但它不是零，也不是固定值——它取决于主库到副本的物理距离、复制管道的拥塞情况和当时的写入压力。</p><p>一个常见的错误认知是「我把两次请求都发到同一个边缘节点，就能保证顺序」。事实并非如此：边缘节点只是客户端到网络的入口，它到主库与到副本的链路是独立的。即使入口相同，写走主库路径、读走副本路径，两者依然是两条不同的数据路径。想在这条路径上要顺序性，必须显式地把读也钉到主库。</p><h2 id="会话令牌：把「读到自己写」变成可工程化的保证"><a href="#会话令牌：把「读到自己写」变成可工程化的保证" class="headerlink" title="会话令牌：把「读到自己写」变成可工程化的保证"></a>会话令牌：把「读到自己写」变成可工程化的保证</h2><p>Cloudflare 在 D1 的 HTTP 接口与 Workers Binding 中提供了会话（session）与书签（bookmark）机制，用来解决上述「读己之写」（read-your-writes）问题。它的思路很朴素：写入时返回一个书签，代表主库当前的复制位置；后续读取时把这个书签带上，副本会等到自己至少追平到该位置后再返回结果。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 以 Workers Binding 为例，以官方文档为准</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  <span class="keyword">async</span> <span class="title function_">fetch</span>(<span class="params">request, env</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> url = <span class="keyword">new</span> <span class="title function_">URL</span>(request.<span class="property">url</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (url.<span class="property">pathname</span> === <span class="string">&quot;/comment&quot;</span> &amp;&amp; request.<span class="property">method</span> === <span class="string">&quot;POST&quot;</span>) &#123;</span><br><span class="line">      <span class="keyword">const</span> body = <span class="keyword">await</span> request.<span class="title function_">json</span>();</span><br><span class="line">      <span class="comment">// 写操作：withSession 记录本次写入的会话位置</span></span><br><span class="line">      <span class="keyword">const</span> session = env.<span class="property">DB</span>.<span class="title function_">withSession</span>(<span class="string">&quot;request-scoped&quot;</span>);</span><br><span class="line">      <span class="keyword">await</span> session</span><br><span class="line">        .<span class="title function_">prepare</span>(<span class="string">&quot;INSERT INTO comments (post_id, author, body) VALUES (?, ?, ?)&quot;</span>)</span><br><span class="line">        .<span class="title function_">bind</span>(body.<span class="property">postId</span>, body.<span class="property">author</span>, body.<span class="property">body</span>)</span><br><span class="line">        .<span class="title function_">run</span>();</span><br><span class="line">      <span class="comment">// 将书签回传给客户端或缓存，供后续读取使用</span></span><br><span class="line">      <span class="keyword">const</span> bookmark = session.<span class="title function_">getBookmark</span>();</span><br><span class="line">      <span class="keyword">return</span> <span class="title class_">Response</span>.<span class="title function_">json</span>(&#123; <span class="attr">ok</span>: <span class="literal">true</span>, bookmark &#125;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (url.<span class="property">pathname</span> === <span class="string">&quot;/comments&quot;</span>) &#123;</span><br><span class="line">      <span class="keyword">const</span> bookmark = url.<span class="property">searchParams</span>.<span class="title function_">get</span>(<span class="string">&quot;bookmark&quot;</span>);</span><br><span class="line">      <span class="keyword">const</span> session = env.<span class="property">DB</span>.<span class="title function_">withSession</span>(bookmark || <span class="string">&quot;first-primary&quot;</span>);</span><br><span class="line">      <span class="keyword">const</span> &#123; results &#125; = <span class="keyword">await</span> session</span><br><span class="line">        .<span class="title function_">prepare</span>(<span class="string">&quot;SELECT * FROM comments WHERE post_id = ? ORDER BY id DESC LIMIT 20&quot;</span>)</span><br><span class="line">        .<span class="title function_">bind</span>(url.<span class="property">searchParams</span>.<span class="title function_">get</span>(<span class="string">&quot;postId&quot;</span>))</span><br><span class="line">        .<span class="title function_">all</span>();</span><br><span class="line">      <span class="keyword">return</span> <span class="title class_">Response</span>.<span class="title function_">json</span>(&#123; results &#125;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Response</span>(<span class="string">&quot;not found&quot;</span>, &#123; <span class="attr">status</span>: <span class="number">404</span> &#125;);</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>这段代码里有两个关键选择。第一，<code>withSession</code> 的参数从 <code>first-primary</code> 变成书签之后，读取的语义从「哪快读哪」变成了「至少不早于这个点」。第二，书签是可以跨请求传递的普通字符串，这意味着你可以在服务端存储、在 Cookie 里携带、甚至写进响应头，让同一个用户的后续读请求都获得顺序保证。</p><p>代价必须讲清楚：带上书签的读会被<strong>强制等待</strong>复制追平。如果你的书签很新，而请求被路由到了很远或很忙的副本，这次读的延迟会比普通读高出一个量级。所以书签适合用在「用户自己刚写入、然后马上要看结果」这种强相关场景，而不是用在全站的列表页查询上。</p><h2 id="顺序一致性在什么条件下被破坏"><a href="#顺序一致性在什么条件下被破坏" class="headerlink" title="顺序一致性在什么条件下被破坏"></a>顺序一致性在什么条件下被破坏</h2><p>「读己之写」是单用户视角的顺序性。一旦视角扩大到多用户，问题就复杂得多。假设 A 用户发表一条评论，B 用户在同一秒打开页面，B 的读大概率落在没有这条评论的快照上。这不是错误，而是<strong>最终一致性</strong>的定义本身。</p><p>真正会变成事故的是下面几种顺序错乱：</p><ul><li><strong>跨表因果断裂</strong>：事务里同时写了主表和统计表，如果事务本身没有被原子地应用到副本，可能出现主表已更新、统计表还是旧值的中间态。SQLite 在单库内是原子提交的，但复制投递的粒度与可见性边界需要以官方文档为准。</li><li><strong>回滚导致的「时间倒流」</strong>：某些故障切换或重建副本的场景会让副本的进度短暂后退，用户刚看到的新数据又消失了。这类现象通常伴随主库故障转移，属于低频高风险事件。</li><li><strong>读缓存与副本叠加</strong>：在 D1 前面又套一层 KV 或 HTTP 缓存，缓存自身的 TTL 会让一致性窗口被放大数倍。很多「D1 不一致」的投诉，根因其实在外层缓存。</li></ul><p>理解这一点很重要：<strong>一致性不是数据库单方面提供的属性，而是整条读路径共同决定的结果</strong>。数据库上的一致性能力再强，也挡不住你在前面叠一层无脑缓存的缓存。</p><h2 id="主库故障切换与数据可见性回退"><a href="#主库故障切换与数据可见性回退" class="headerlink" title="主库故障切换与数据可见性回退"></a>主库故障切换与数据可见性回退</h2><p>D1 作为托管服务，会在主库所在区域出现问题时进行故障转移。对应用而言，最需要提前设计的两件事是写入的可重试性和读取的容忍度。</p><p>写方面，网络抖动会导致「服务端已提交但客户端未收到响应」，这个经典的两难在边缘场景更容易出现，因为链路上还多了一层边缘节点。<strong>幂等键</strong>是唯一稳妥的解法：客户端为每次写生成一个唯一 ID 并在重试时复用，服务端对这个 ID 做去重。</p><p>读方面，要接受一个事实：在最坏情况下，副本可能落后于你上次读到的位置。因此不要把「上次读到的数据」当作业务上的既成事实来做后续决策。举一个具体例子：不要用「读到的余额大于 0」作为发起扣款的理由，而应该把判断放到主库上的原子条件更新里。</p><p>下面这张表对比了几种读取策略在延迟、一致性和适用场景上的差异，可以作为选型参考。</p><table><thead><tr><th>读取策略</th><th>一致性保证</th><th>典型延迟量级</th><th>适用场景</th></tr></thead><tbody><tr><td>默认副本读</td><td>最终一致</td><td>最低（就近命中）</td><td>列表页、静态内容、统计展示</td></tr><tr><td>带书签的副本读</td><td>读己之写</td><td>中等，视落后程度</td><td>用户提交后立即回显</td></tr><tr><td>强制主库读</td><td>强一致（单主）</td><td>较高，跨区域</td><td>余额、库存、权限判定</td></tr><tr><td>副本读 + 外层缓存</td><td>弱于最终一致</td><td>最低</td><td>可接受陈旧的聚合数据</td></tr></tbody></table><blockquote><p>经验法则：把「读己之写」写进需求文档，把强一致读限制在极少数关键路径上，剩下的全部按最终一致设计。反过来做，边缘带来的延迟优势会被等待复制的时间吃回去。</p></blockquote><h2 id="从故障排查倒推一致性设计"><a href="#从故障排查倒推一致性设计" class="headerlink" title="从故障排查倒推一致性设计"></a>从故障排查倒推一致性设计</h2><p>线上反馈「数据不对」时，排查顺序建议固定成下面四步，能省掉大量无谓的怀疑：</p><ol><li><strong>确认读路径</strong>：这条查询究竟走的是副本还是主库？先看请求被路由到了哪里，再看代码里用的是默认读还是会话读。</li><li><strong>确认外层缓存</strong>：D1 前面有没有 KV、Cache API 或 CDN 缓存？如果有，先绕过它再复现一次。</li><li><strong>确认时间窗口</strong>：写入与读取相隔多久？几十毫秒内的问题大概率是复制延迟，不是逻辑错误。</li><li><strong>确认是否发生故障转移</strong>：查询监控里主库是否在问题时段有切换记录，切换会导致可见性回退。</li></ol><p>这套顺序的价值在于：它把「玄学不一致」拆成了四个可观测、可证伪的假设。绝大多数情况下，问题在前两步就能定位。</p><h2 id="团队落地时的边界约定"><a href="#团队落地时的边界约定" class="headerlink" title="团队落地时的边界约定"></a>团队落地时的边界约定</h2><p>最后是一组工程约定，建议写进团队规范里：</p><ul><li>所有写入接口<strong>必须</strong>返回可传递的书签，前端在关键回显场景携带它。</li><li>所有关键判断<strong>不得</strong>基于副本读的绝对值，必须基于主库上的条件更新。</li><li>明确列出允许陈旧的字段清单，其余字段一律不允许出现在默认副本读的结果里。</li><li>监控复制延迟与故障转移事件，把它们当作一级告警而不是后台信息。</li></ul><p>把一致性当作一个显式的架构决策而不是默认假设，是团队从「能跑通」走向「能上量」的分水岭。</p><blockquote><p><strong>说明：</strong> 本文为原创技术分析，基于公开的 D1 架构资料与分布式数据库一致性的一般原理撰写，涉及的具体 API 名称、配额、复制延迟数值与故障转移行为以 Cloudflare 官方最新文档为准；文中示例代码为讲解用途，生产使用前请核对当前 SDK 用法。观点部分为笔者主观判断，欢迎以实测数据修正。</p></blockquote>]]>
    </content>
    <id>https://blogs.yzy15.dpdns.org/cf-d1-sqlite-consistency-model/</id>
    <link href="https://blogs.yzy15.dpdns.org/cf-d1-sqlite-consistency-model/"/>
    <published>2026-09-13T02:00:00.000Z</published>
    <summary>从原理层拆解 Cloudflare D1 背后 SQLite 读副本的一致性模型，讲清楚为什么边缘读会返回旧值、顺序一致性在什么条件下被破坏、故障切换时数据可能回退的机制，以及会话令牌与读写分离该怎么配合，帮助正在把业务往边缘迁移的团队提前识别数据可见性事故。</summary>
    <title>D1 与 SQLite 的读副本一致性模型：延迟、顺序与故障恢复</title>
    <updated>2026-09-12T17:33:38.957Z</updated>
  </entry>
  <entry>
    <author>
      <name>mc-yzy15</name>
    </author>
    <category term="开发者生态" scheme="https://blogs.yzy15.dpdns.org/categories/%E5%BC%80%E5%8F%91%E8%80%85%E7%94%9F%E6%80%81/"/>
    <category term="Vibe Coding" scheme="https://blogs.yzy15.dpdns.org/tags/Vibe-Coding/"/>
    <category term="AI 编程" scheme="https://blogs.yzy15.dpdns.org/tags/AI-%E7%BC%96%E7%A8%8B/"/>
    <category term="低代码" scheme="https://blogs.yzy15.dpdns.org/tags/%E4%BD%8E%E4%BB%A3%E7%A0%81/"/>
    <category term="概念辨析" scheme="https://blogs.yzy15.dpdns.org/tags/%E6%A6%82%E5%BF%B5%E8%BE%A8%E6%9E%90/"/>
    <content>
      <![CDATA[<h1 id="Vibe-Coding-到底是什么：一次概念澄清"><a href="#Vibe-Coding-到底是什么：一次概念澄清" class="headerlink" title="Vibe Coding 到底是什么：一次概念澄清"></a>Vibe Coding 到底是什么：一次概念澄清</h1><p>「Vibe Coding」大概是这几年被引用得最多、被定义得最少的开发术语之一。它第一次进入大众视野时，描述的是一种相当具体的行为：你不看代码，只凭感觉（vibe）描述你想要什么，让模型反复改，直到跑出来的东西看起来对了。但两年过去，这个词已经被厂商、媒体和招聘启事反复借用，如今既能指「用 Copilot 补全一行函数」，也能指「一句话生成一个 App」，甚至能指「产品经理自己搭了个内部工具」。</p><p>当一个词同时指代这么多东西，它就失去了沟通能力。本文想做的事情很朴素：把 Vibe Coding 和它最容易混淆的三个邻居——AI 辅助编程、低代码平台、提示词生成应用——放到同一张桌子上，用可操作的标准把它们切开。</p><h2 id="为什么这个词一定需要澄清，以及从哪些维度澄清"><a href="#为什么这个词一定需要澄清，以及从哪些维度澄清" class="headerlink" title="为什么这个词一定需要澄清，以及从哪些维度澄清"></a>为什么这个词一定需要澄清，以及从哪些维度澄清</h2><p>概念含混不只是学术问题，它会直接造成工程上的误判。</p><p>第一类误判是<strong>风险误判</strong>。如果团队把「让模型补全单元测试」和「让模型不改测试直接改实现直到变绿」都叫 Vibe Coding，那么针对前者制定的代码评审流程，会被后者悄悄绕过——因为流程的假设是「人始终读得懂改动」。</p><p>第二类误判是<strong>能力误判</strong>。当招聘方写「熟悉 Vibe Coding」，他可能期待的是会用 Cursor 的 Tab 键，也可能期待的是能设计一套带自动验证的 agent 工作流。两者的技能栈和薪资区间完全不同。</p><p>第三类误判是<strong>归因误判</strong>。项目延期时，「我们用了 Vibe Coding」常常被当成一个方便的借口，而真正的问题可能是需求不清、缺少测试、或者压根没人做验证。</p><p>所以澄清这个概念，本质上是<strong>给不同风险等级的工作流贴不同标签</strong>，而不是给一个新工具起名字。</p><p>而这四个维度的观察方式，也给了我们一把通用的尺子。它们不是非此即彼的开关，而是连续光谱上的位置。</p><ul><li><strong>控制权归属</strong>：最终决策由人还是由模型做出？人只在开头写一次提示，还是持续介入每一次修改？</li><li><strong>验证强度</strong>：变更是否被自动测试、类型检查、静态分析或人工评审拦截？</li><li><strong>代码可见性</strong>：使用者是否需要、是否能够阅读生成的代码？「不看」是一种选择，还是一种能力缺失？</li><li><strong>可复现性</strong>：同样的意图重新执行一次，是否大概率得到结构一致的产物？</li></ul><p>把这四个维度套上去，几个概念的差异立刻清晰：AI 辅助编程的关键词是「辅助」，控制权在人，验证强度通常不变；低代码的关键词是「平台」，控制权在平台提供的抽象层，代码可见性低但可复现性高；Vibe Coding 的关键词是「意图先行」，控制权和验证强度同时被下放，代价是可复现性和可维护性。</p><h2 id="与-AI-辅助编程的边界：控制权与验证是否被让渡"><a href="#与-AI-辅助编程的边界：控制权与验证是否被让渡" class="headerlink" title="与 AI 辅助编程的边界：控制权与验证是否被让渡"></a>与 AI 辅助编程的边界：控制权与验证是否被让渡</h2><p>这是最常被混淆、也最需要切开的一对。</p><p>AI 辅助编程（AI-assisted programming）的典型形态是补全、改写、解释、生成测试、批量重命名。它的本质是<strong>加速人类已有的决策过程</strong>：人先决定要做什么、做在哪、做成什么样，然后让模型把机械劳动做掉。人始终在循环里，并且在每一步都保留了「读代码」的默认动作。</p><p>Vibe Coding 的典型形态则是：人给出一个意图，模型产出实现，人只观察外部行为是否符合预期，如果不符合，就追加一句描述继续改。这里发生了一个关键让渡——<strong>从「读代码判断对错」让渡到「看现象判断对错」</strong>。</p><p>这个让渡不是道德问题，而是工程权衡问题。它在以下场景是完全合理的：</p><ul><li>一次性脚本、数据分析、格式转换，产物用完即弃；</li><li>个人原型、周末项目、演示 Demo，失败成本接近零；</li><li>探索性任务，你连自己想要什么都不知道，需要先看到东西才能提要求。</li></ul><p>它在这类场景则相当危险：涉及用户数据、涉及金钱、涉及对外暴露的接口、需要长期维护的代码库。</p><p>判断标准可以用一句话总结：<strong>如果你无法承受「没人读懂这段代码」的后果，那它就不是 Vibe Coding 场景，无论你用的是哪个工具。</strong></p><h2 id="与低代码的边界：抽象层方向相反"><a href="#与低代码的边界：抽象层方向相反" class="headerlink" title="与低代码的边界：抽象层方向相反"></a>与低代码的边界：抽象层方向相反</h2><p>低代码平台和 Vibe Coding 都承诺「少写代码」，但它们实现这个承诺的机制几乎相反，这一点常被忽略。</p><p>低代码的路径是<strong>向上抽象</strong>。平台把常见需求固化成可视化组件、拖拽流程、配置项，你操作的是平台定义的语义单元。它的优势在于约束强：因为选项有限，所以产物可预测、可导出、可迁移，平台也敢对企业承诺 SLA。代价是天花板明确——一旦需求落到平台没抽象到的角落，你就要和平台打架，或者被逼去写插件。</p><p>Vibe Coding 的路径是<strong>向下生成</strong>。它不预设抽象层，直接产出通用语言代码，理论上没有天花板。代价是约束几乎为零：同样的提示，两次运行可能得到两种结构；命名风格、错误处理、依赖选择全凭模型当次判断。</p><p>用一个类比：低代码像住在精装修公寓，户型固定但水电煤气都通好了；Vibe Coding 像拿到一堆建材和一张手绘草图，理论上能盖出任何房子，但漏不漏水得自己验。</p><p>这带来一个反直觉但重要的结论：<strong>低代码的产物通常比 Vibe Coding 的产物更容易长期维护</strong>，哪怕前者的代码你一眼都没看过，后者你逐行读过。因为可维护性取决于约束的稳定性，而不是取决于你读没读过。</p><h2 id="与「提示词生成应用」的边界：一次性产物还是可持续流程"><a href="#与「提示词生成应用」的边界：一次性产物还是可持续流程" class="headerlink" title="与「提示词生成应用」的边界：一次性产物还是可持续流程"></a>与「提示词生成应用」的边界：一次性产物还是可持续流程</h2><p>第三个邻居是「提示词直接生成应用」。它和 Vibe Coding 高度重叠，但可以区分出两种不同性质的东西。</p><p>一种是<strong>产品化的生成器</strong>：你填一段描述，平台吐出一个完整的、可部署的应用骨架。这里的控制权其实在平台手里——平台决定了技术栈、目录结构、默认依赖。用户做的选择很少，因为选项被平台收敛过了。本质上这是低代码的一种极端形态，只是把「拖拽」换成了「描述」。</p><p>另一种是<strong>持续的对话式开发流程</strong>：你有一个真实的项目目录，模型读写其中的真实文件，你持续给出反馈，代码持续演化。这才是 Vibe Coding 的原始含义。区别的关键不在「是否用了自然语言」，而在<strong>产物是否继续被迭代</strong>。</p><p>下面这段伪代码描述了一个典型的、带有验证护栏的对话式循环。注意其中 <code>runChecks</code> 的位置——它是把「纯凭感觉」拉回工程实践的最小成本手段。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 一个带验证护栏的对话式开发循环（伪代码，示意结构）</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">vibeLoop</span>(<span class="params">intent, repo</span>) &#123;</span><br><span class="line">  <span class="keyword">let</span> attempt = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">while</span> (attempt &lt; <span class="variable constant_">MAX_ATTEMPTS</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> patch = <span class="keyword">await</span> model.<span class="title function_">generate</span>(&#123; intent, <span class="attr">context</span>: <span class="keyword">await</span> repo.<span class="title function_">snapshot</span>() &#125;);</span><br><span class="line">    <span class="keyword">await</span> repo.<span class="title function_">apply</span>(patch);</span><br><span class="line">    <span class="keyword">const</span> result = <span class="keyword">await</span> <span class="title function_">runChecks</span>(repo); <span class="comment">// lint + 类型检查 + 已有测试</span></span><br><span class="line">    <span class="keyword">if</span> (result.<span class="property">ok</span>) <span class="keyword">return</span> &#123; <span class="attr">status</span>: <span class="string">&#x27;draft-ready&#x27;</span>, patch &#125;;</span><br><span class="line">    <span class="comment">// 关键：把失败信息回灌给模型，而不是人肉描述现象</span></span><br><span class="line">    intent = <span class="string">`<span class="subst">$&#123;intent&#125;</span>\n上一次失败原因：<span class="subst">$&#123;result.summary&#125;</span>`</span>;</span><br><span class="line">    attempt += <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> &#123; <span class="attr">status</span>: <span class="string">&#x27;needs-human&#x27;</span>, <span class="attr">reason</span>: <span class="string">&#x27;attempts exhausted&#x27;</span> &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>一旦你在循环里插入了自动检查，性质就变了：你不再完全依赖「感觉对不对」，而是让机器承担一部分判断。这仍然可以是 Vibe Coding——人可能一行生成代码都没读——但风险等级已经降了一档。</p><h2 id="一张对照表：四个概念的取舍"><a href="#一张对照表：四个概念的取舍" class="headerlink" title="一张对照表：四个概念的取舍"></a>一张对照表：四个概念的取舍</h2><table><thead><tr><th>维度</th><th>AI 辅助编程</th><th>低代码平台</th><th>提示词生成应用</th><th>Vibe Coding</th></tr></thead><tbody><tr><td>控制权</td><td>人主导，逐步确认</td><td>平台主导抽象层</td><td>平台主导模板</td><td>模型主导，人给意图</td></tr><tr><td>是否读代码</td><td>默认读</td><td>基本不读</td><td>基本不读</td><td>通常读，但不作为判断依据</td></tr><tr><td>验证强度</td><td>沿用原有流程</td><td>平台内建 + 导出后自测</td><td>取决于平台</td><td>低，取决于是否自建护栏</td></tr><tr><td>可复现性</td><td>高（改动小）</td><td>高（配置即产物）</td><td>中</td><td>低</td></tr><tr><td>天花板</td><td>高</td><td>低到中</td><td>中</td><td>高</td></tr><tr><td>适合场景</td><td>任何正式项目</td><td>内部工具、表单流程</td><td>快速 Demo</td><td>一次性脚本、原型探索</td></tr><tr><td>主要风险</td><td>几乎没有新增风险</td><td>被平台锁定</td><td>模板同质化、依赖不透明</td><td>无人理解代码、隐性缺陷</td></tr></tbody></table><p>这张表里最值得盯住的一行是「是否读代码」。它是这四个概念在实践中最容易被观察到的分水岭，也是团队做流程设计时唯一必须提前明确的一件事。</p><h2 id="一个可操作的判定清单"><a href="#一个可操作的判定清单" class="headerlink" title="一个可操作的判定清单"></a>一个可操作的判定清单</h2><p>如果你想知道自己手上的工作流到底属于哪一类，可以按顺序问自己四个问题：</p><ol><li><strong>我最后一次完整读过这段代码是什么时候？</strong> 如果答案是「从没读过」，你大概率在做 Vibe Coding。</li><li><strong>如果模型明天换了版本，产出变了，我有办法发现吗？</strong> 如果没有自动化手段能发现，你的验证强度接近零。</li><li><strong>这套产物需要活多久？</strong> 一周和三年，对应完全不同的策略。</li><li><strong>失败了谁承担后果？</strong> 只有我自己，和有一千个用户，是两种完全不同的游戏。</li></ol><p>四个问题里有三个指向「低风险」，那么放心地用 Vibe Coding 提速，它的价值是真实的：它把「从想法到能看到东西」的时间从半天压到十分钟，这个压缩本身就是巨大的生产力。</p><p>一旦有两个以上指向「高风险」，你就该把它降级成 AI 辅助编程：读代码、加测试、走评审。工具可以不变，但流程必须变。</p><h2 id="结论：别争名词，标好风险"><a href="#结论：别争名词，标好风险" class="headerlink" title="结论：别争名词，标好风险"></a>结论：别争名词，标好风险</h2><p>回到开头的问题：Vibe Coding 到底是什么？</p><p>笔者的判断是，它描述的不是某类工具，而是<strong>一种控制权与验证权的让渡程度</strong>。同样的模型、同样的 IDE，在不同的人手里可以是辅助编程，也可以是 Vibe Coding，区别只在于人是否还在做最终判断，以及是否有机器流程替人做判断。</p><p>所以团队内部真正需要统一的从来不是名词，而是标签体系：哪一类改动允许不读代码、哪一类必须读、哪一类必须有测试护栏。把这些边界写进规范，比争论「这算不算 Vibe Coding」有价值得多。</p><blockquote><p><strong>说明：</strong> 本文为原创概念辨析与方法论讨论，不涉及具体产品的实测数据；文中对工具形态的描述基于公开信息与笔者使用经验，具体功能、配额与版本行为以各厂商官方最新文档为准。对行业趋势的判断（如「低代码产物更易维护」）属笔者主观观点，欢迎按自己的项目场景验证。文中代码块为示意性伪代码，用于说明循环结构，不可直接运行。</p></blockquote>]]>
    </content>
    <id>https://blogs.yzy15.dpdns.org/vibe-probe-definition/</id>
    <link href="https://blogs.yzy15.dpdns.org/vibe-probe-definition/"/>
    <published>2026-09-13T02:00:00.000Z</published>
    <summary>Vibe Coding 一词在两年内被媒体、厂商和开发者反复借用，含义已经严重泛化。本文从「谁持有控制权」「谁来验证正确性」两个维度出发，把 Vibe Coding 与 AI 辅助编程、低代码平台、提示词直接生成应用区分开来，并给出判断一个工作流是否属于 Vibe Coding 的可操作标准。</summary>
    <title>Vibe Coding 到底是什么：一次概念澄清</title>
    <updated>2026-09-12T17:33:38.964Z</updated>
  </entry>
  <entry>
    <author>
      <name>mc-yzy15</name>
    </author>
    <category term="云计算" scheme="https://blogs.yzy15.dpdns.org/categories/%E4%BA%91%E8%AE%A1%E7%AE%97/"/>
    <category term="架构设计" scheme="https://blogs.yzy15.dpdns.org/tags/%E6%9E%B6%E6%9E%84%E8%AE%BE%E8%AE%A1/"/>
    <category term="边缘计算" scheme="https://blogs.yzy15.dpdns.org/tags/%E8%BE%B9%E7%BC%98%E8%AE%A1%E7%AE%97/"/>
    <category term="多云" scheme="https://blogs.yzy15.dpdns.org/tags/%E5%A4%9A%E4%BA%91/"/>
    <category term="回源" scheme="https://blogs.yzy15.dpdns.org/tags/%E5%9B%9E%E6%BA%90/"/>
    <content>
      <![CDATA[<h1 id="多云边缘拓扑：让-Cloudflare-与非-Cloudflare-后端共存"><a href="#多云边缘拓扑：让-Cloudflare-与非-Cloudflare-后端共存" class="headerlink" title="多云边缘拓扑：让 Cloudflare 与非 Cloudflare 后端共存"></a>多云边缘拓扑：让 Cloudflare 与非 Cloudflare 后端共存</h1><p>真实世界的迁移几乎不是”从零开始”，而是”在已有的多云与自建环境上加一层”。这种场景下的设计目标不是追求架构纯洁性，而是<strong>让边缘平台成为一个统一入口，同时不破坏既有系统</strong>。本文讨论这类拓扑的几种常见形态与决策点。</p><h2 id="起点：先给现有系统分类"><a href="#起点：先给现有系统分类" class="headerlink" title="起点：先给现有系统分类"></a>起点：先给现有系统分类</h2><p>在画拓扑之前，先把后端分成三类，因为它们对边缘的配合方式完全不同：</p><ul><li><strong>A 类：可安全暴露给边缘的 HTTP 服务</strong>（API、渲染层），能被边缘直接回源；</li><li><strong>B 类：不应暴露的敏感系统</strong>（数据库、管理后台、消息中间件），必须走私网通道或只在内网可达；</li><li><strong>C 类：非 HTTP 协议或有状态长连接</strong>（WebSocket、gRPC、消息队列），需要专门的代理或隧道方案。</li></ul><p>划分标准不是技术栈，而是**”是否可以被一段来自边缘的请求安全触达”**。这个判断决定了回源方式，也就决定了整个拓扑的形状。</p><h2 id="形态一：边缘做统一入口，后端保持原样"><a href="#形态一：边缘做统一入口，后端保持原样" class="headerlink" title="形态一：边缘做统一入口，后端保持原样"></a>形态一：边缘做统一入口，后端保持原样</h2><p>最常见的形态，也最容易落地。用户流量统一进入边缘，边缘完成鉴权、限流、路由、缓存，然后回源到原有后端（通过公网 + 隧道，或专线类私网互联）。</p><p>优点：改动最小，后端不需要改代码；缺点：回源仍然是链条的一部分，性能天花板受限于回源路径。</p><p>关键设计点：</p><ol><li><strong>入口统一</strong>：把 DNS 全部指向边缘，源站不再对外解析；</li><li><strong>回源收敛</strong>：边缘到后端保持少量稳定连接，避免每个 PoP 都开连接；</li><li><strong>规则前置</strong>：能放到边缘的过滤（鉴权、限流、Bot 管理）尽量前置，降低后端压力。</li></ol><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 边缘做统一入口：鉴权 + 转发，后端服务保持零改动</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  <span class="keyword">async</span> <span class="title function_">fetch</span>(<span class="params">request, env</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> url = <span class="keyword">new</span> <span class="title function_">URL</span>(request.<span class="property">url</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 只代理受保护的 API 前缀，其余交由平台规则处理</span></span><br><span class="line">    <span class="keyword">if</span> (!url.<span class="property">pathname</span>.<span class="title function_">startsWith</span>(<span class="string">&quot;/api/&quot;</span>)) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Response</span>(<span class="string">&quot;not found&quot;</span>, &#123; <span class="attr">status</span>: <span class="number">404</span> &#125;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> userId = <span class="keyword">await</span> <span class="title function_">verifyToken</span>(request); <span class="comment">// 边缘统一鉴权</span></span><br><span class="line">    <span class="keyword">if</span> (!userId) <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Response</span>(<span class="string">&quot;unauthorized&quot;</span>, &#123; <span class="attr">status</span>: <span class="number">401</span> &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> upstream = <span class="keyword">new</span> <span class="title function_">URL</span>(url.<span class="property">pathname</span> + url.<span class="property">search</span>, env.<span class="property">ORIGIN_BASE</span>);</span><br><span class="line">    <span class="keyword">const</span> headers = <span class="keyword">new</span> <span class="title class_">Headers</span>(request.<span class="property">headers</span>);</span><br><span class="line">    headers.<span class="title function_">set</span>(<span class="string">&quot;x-authenticated-user&quot;</span>, userId); <span class="comment">// 后端只信任来自边缘的头</span></span><br><span class="line">    headers.<span class="title function_">delete</span>(<span class="string">&quot;cookie&quot;</span>);                    <span class="comment">// 不再向后端传递原始 Cookie</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> resp = <span class="keyword">await</span> <span class="title function_">fetch</span>(upstream, &#123;</span><br><span class="line">      <span class="attr">method</span>: request.<span class="property">method</span>,</span><br><span class="line">      headers,</span><br><span class="line">      <span class="attr">body</span>: [<span class="string">&quot;GET&quot;</span>, <span class="string">&quot;HEAD&quot;</span>].<span class="title function_">includes</span>(request.<span class="property">method</span>) ? <span class="literal">undefined</span> : request.<span class="property">body</span></span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 明确不缓存受保护内容</span></span><br><span class="line">    <span class="keyword">const</span> out = <span class="keyword">new</span> <span class="title class_">Response</span>(resp.<span class="property">body</span>, resp);</span><br><span class="line">    out.<span class="property">headers</span>.<span class="title function_">set</span>(<span class="string">&quot;cache-control&quot;</span>, <span class="string">&quot;no-store&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> out;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h2 id="形态二：边缘承载一部分逻辑，后端承载重计算"><a href="#形态二：边缘承载一部分逻辑，后端承载重计算" class="headerlink" title="形态二：边缘承载一部分逻辑，后端承载重计算"></a>形态二：边缘承载一部分逻辑，后端承载重计算</h2><p>当部分逻辑适合迁到边缘（鉴权、个性化拼装、A&#x2F;B 分流、地理路由），而重计算与事务仍留在后端时，形成”边缘 + 后端”的分工。</p><p>分工的判断依据可以简化成一张表：</p><table><thead><tr><th>判断维度</th><th>优先放边缘</th><th>优先留后端</th></tr></thead><tbody><tr><td>延迟敏感度</td><td>高（用户可感知）</td><td>中低</td></tr><tr><td>有无状态</td><td>无状态或弱状态</td><td>强事务、需锁</td></tr><tr><td>依赖类型</td><td>标准 Web API</td><td>原生扩展、本地文件</td></tr><tr><td>执行时长</td><td>短（毫秒级）</td><td>长（秒级及以上）</td></tr><tr><td>变更频率</td><td>高（需快速灰度）</td><td>低</td></tr><tr><td>数据位置要求</td><td>允许在边缘处理</td><td>需固定区域</td></tr></tbody></table><p>注意最后两行：<strong>变更频率高、适合快速灰度的逻辑放边缘收益最大</strong>；而<strong>有数据位置要求或强事务需求的逻辑必须留在后端</strong>。</p><h2 id="形态三：多后端按区域分流"><a href="#形态三：多后端按区域分流" class="headerlink" title="形态三：多后端按区域分流"></a>形态三：多后端按区域分流</h2><p>当后端本身分布在多个云与多个区域时，边缘可以根据用户属性或请求特征选择回源后端。常见策略：</p><ul><li><strong>按地理</strong>：用户在不同区域，回源到就近后端；</li><li><strong>按租户</strong>：企业客户绑定到专属后端环境；</li><li><strong>按灰度</strong>：新版本只服务小比例流量；</li><li><strong>按健康状态</strong>：某后端异常时自动切走。</li></ul><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 多后端分流：按租户 + 健康状态选择上游</span></span><br><span class="line"><span class="keyword">const</span> <span class="variable constant_">ORIGINS</span> = &#123;</span><br><span class="line">  <span class="attr">default</span>: <span class="string">&quot;https://origin-a.example.com&quot;</span>,</span><br><span class="line">  <span class="attr">enterprise</span>: <span class="string">&quot;https://origin-b.example.com&quot;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  <span class="keyword">async</span> <span class="title function_">fetch</span>(<span class="params">request, env</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> url = <span class="keyword">new</span> <span class="title function_">URL</span>(request.<span class="property">url</span>);</span><br><span class="line">    <span class="keyword">const</span> tenant = request.<span class="property">headers</span>.<span class="title function_">get</span>(<span class="string">&quot;x-tenant&quot;</span>) || <span class="string">&quot;default&quot;</span>;</span><br><span class="line">    <span class="keyword">const</span> base = <span class="variable constant_">ORIGINS</span>[tenant] || <span class="variable constant_">ORIGINS</span>.<span class="property">default</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      <span class="keyword">const</span> upstream = <span class="keyword">new</span> <span class="title function_">URL</span>(url.<span class="property">pathname</span> + url.<span class="property">search</span>, base);</span><br><span class="line">      <span class="keyword">const</span> resp = <span class="keyword">await</span> <span class="title function_">fetch</span>(upstream, &#123; <span class="attr">signal</span>: <span class="title class_">AbortSignal</span>.<span class="title function_">timeout</span>(<span class="number">5000</span>) &#125;);</span><br><span class="line">      <span class="keyword">if</span> (resp.<span class="property">status</span> &gt;= <span class="number">500</span>) <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&quot;upstream 5xx&quot;</span>);</span><br><span class="line">      <span class="keyword">return</span> resp;</span><br><span class="line">    &#125; <span class="keyword">catch</span> &#123;</span><br><span class="line">      <span class="comment">// 明确降级：返回可理解的错误，而不是长时间挂起</span></span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Response</span>(<span class="title class_">JSON</span>.<span class="title function_">stringify</span>(&#123; <span class="attr">error</span>: <span class="string">&quot;upstream_unavailable&quot;</span> &#125;), &#123;</span><br><span class="line">        <span class="attr">status</span>: <span class="number">503</span>,</span><br><span class="line">        <span class="attr">headers</span>: &#123; <span class="string">&quot;content-type&quot;</span>: <span class="string">&quot;application/json&quot;</span>, <span class="string">&quot;retry-after&quot;</span>: <span class="string">&quot;5&quot;</span> &#125;</span><br><span class="line">      &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p><code>AbortSignal.timeout</code> 那一行是这类拓扑的关键细节：<strong>没有超时的跨云回源会导致请求堆积，把局部故障放大成全站故障</strong>。</p><h2 id="决策对照：三种形态的取舍"><a href="#决策对照：三种形态的取舍" class="headerlink" title="决策对照：三种形态的取舍"></a>决策对照：三种形态的取舍</h2><table><thead><tr><th>维度</th><th>形态一（统一入口）</th><th>形态二（分工）</th><th>形态三（多后端分流）</th></tr></thead><tbody><tr><td>后端改动量</td><td>无</td><td>中（拆分逻辑）</td><td>中（需健康探测）</td></tr><tr><td>性能收益</td><td>中</td><td>高</td><td>中高</td></tr><tr><td>运维复杂度</td><td>低</td><td>中</td><td>高</td></tr><tr><td>灰度能力</td><td>强（入口层）</td><td>强</td><td>强</td></tr><tr><td>故障影响面</td><td>入口是单点关注区</td><td>需处理两侧不一致</td><td>需处理路由误判</td></tr><tr><td>适合团队规模</td><td>小到中</td><td>中到较大</td><td>较大（有多环境需求）</td></tr></tbody></table><blockquote><p>经验法则：<strong>先做形态一，验证边缘的稳定性与可观测性，再决定是否推进到形态二</strong>。跳过验证直接做逻辑迁移，一旦边缘侧出现难以定位的问题，回退成本会很高。</p></blockquote><h2 id="两个高发问题与处理"><a href="#两个高发问题与处理" class="headerlink" title="两个高发问题与处理"></a>两个高发问题与处理</h2><p><strong>问题一：DNS 切换的”半生效”状态</strong><br>把解析从源站切到边缘时，由于各级递归解析器缓存，会出现一段时间内新旧入口并存的局面。处理方式：切换前先降低 TTL 并等待生效，切换后<strong>保持源站入口可用一段时间</strong>（返回重定向或直接服务），观察流量降到零再关闭。</p><p><strong>问题二：双重代理导致的头信息混乱</strong><br>链路是”用户 → 边缘 → 自有网关 → 后端”时，客户端 IP 与协议信息会被多层覆写。处理方式：<strong>明确每一跳的信任关系</strong>，只接受上游注入的专用头，并在最内层做一次规范化，形成单一可信来源。</p><h2 id="成本与团队协作"><a href="#成本与团队协作" class="headerlink" title="成本与团队协作"></a>成本与团队协作</h2><p>多云场景的隐性成本往往不在带宽，而在<strong>认知负担</strong>：两套控制台、两套权限模型、两套日志系统、两种排障习惯。可行的缓解手段：</p><ol><li><strong>配置即代码</strong>：把两边的关键配置纳入版本管理，减少”记得改但漏了”；</li><li><strong>统一日志出口</strong>：无论来源，先汇聚到一个查询入口，排障时不必来回切换；</li><li><strong>明确责任矩阵</strong>：哪个团队负责边缘规则、哪个团队负责后端、跨界的变更由谁审批；</li><li><strong>定期链路演练</strong>：模拟单侧不可用，验证切换动作真的有效，而不是停留在文档上。</li></ol><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>多云边缘拓扑的设计核心不是”用上什么新技术”，而是<strong>在既有系统上定义清晰的信任边界与故障边界</strong>。把入口统一、把回源收敛、把降级显式化，就能在不打乱现有系统的前提下获得边缘的主要收益；至于是否进一步把逻辑迁到边缘，应当由实测收益而不是架构偏好来驱动。</p><blockquote><p><strong>说明：</strong> 本文为原创技术分析，涉及的架构形态与实现方式基于公开文档与工程实践的一般认知，不同云厂商与平台的能力存在差异，请以各方官方最新文档为准。文中代码为示意性示例，省略了鉴权、错误处理与安全加固细节，生产使用前请补充。表格中的评级与建议属笔者基于实践经验的主观判断。</p></blockquote>]]>
    </content>
    <id>https://blogs.yzy15.dpdns.org/cf-edge-multicloud-topology/</id>
    <link href="https://blogs.yzy15.dpdns.org/cf-edge-multicloud-topology/"/>
    <published>2026-09-13T01:14:00.000Z</published>
    <summary>现实项目很少&quot;全上边缘&quot;。本文讨论在已有 AWS、GCP、自建机房的混搭场景下，如何设计入口层、回源路径、DNS 与故障切换，让边缘平台成为一个统一入口而不是又一个孤岛。</summary>
    <title>多云边缘拓扑：让 Cloudflare 与非 Cloudflare 后端共存</title>
    <updated>2026-09-12T17:33:38.957Z</updated>
  </entry>
  <entry>
    <author>
      <name>mc-yzy15</name>
    </author>
    <category term="云计算" scheme="https://blogs.yzy15.dpdns.org/categories/%E4%BA%91%E8%AE%A1%E7%AE%97/"/>
    <category term="边缘计算" scheme="https://blogs.yzy15.dpdns.org/tags/%E8%BE%B9%E7%BC%98%E8%AE%A1%E7%AE%97/"/>
    <category term="安全边界" scheme="https://blogs.yzy15.dpdns.org/tags/%E5%AE%89%E5%85%A8%E8%BE%B9%E7%95%8C/"/>
    <category term="SSRF" scheme="https://blogs.yzy15.dpdns.org/tags/SSRF/"/>
    <category term="零信任" scheme="https://blogs.yzy15.dpdns.org/tags/%E9%9B%B6%E4%BF%A1%E4%BB%BB/"/>
    <content>
      <![CDATA[<h1 id="边缘计算的安全边界：哪些防线属于平台，哪些必须自己建"><a href="#边缘计算的安全边界：哪些防线属于平台，哪些必须自己建" class="headerlink" title="边缘计算的安全边界：哪些防线属于平台，哪些必须自己建"></a>边缘计算的安全边界：哪些防线属于平台，哪些必须自己建</h1><p>迁移到边缘平台时，团队常有一种隐含期待：”大厂的基础设施，安全性应该自动更好”。这个期待部分成立（DDoS 缓解、TLS 默认开启、运行时隔离），但它会诱导出一种危险的思维——<strong>把安全当成了”平台属性”，而不是”架构决策”</strong>。</p><p>现实是：平台负责<strong>基础设施层</strong>的安全，你负责<strong>应用层</strong>的安全。两者之间存在一条清晰的分界，越过这条线去期待平台兜底，就会留下真实漏洞。</p><h2 id="边界一：鉴权与授权——100-属于你"><a href="#边界一：鉴权与授权——100-属于你" class="headerlink" title="边界一：鉴权与授权——100% 属于你"></a>边界一：鉴权与授权——100% 属于你</h2><p>平台不会知道”这个用户是否能访问这个资源”。它只能做：</p><ul><li>拒绝明显恶意的请求（通过 WAF）；</li><li>限制请求速率；</li><li>校验隧道身份。</li></ul><p>而<strong>对象级授权</strong>（用户 A 能否读用户 B 的订单）必须在你的代码里实现。这是边缘应用最常见的漏洞来源。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 反模式：只校验了&quot;已登录&quot;，没校验&quot;是否拥有该资源&quot;</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  <span class="keyword">async</span> <span class="title function_">fetch</span>(<span class="params">request, env</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> userId = <span class="keyword">await</span> <span class="title function_">verifyToken</span>(request);</span><br><span class="line">    <span class="keyword">if</span> (!userId) <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Response</span>(<span class="string">&quot;unauthorized&quot;</span>, &#123; <span class="attr">status</span>: <span class="number">401</span> &#125;);</span><br><span class="line">    <span class="keyword">const</span> orderId = <span class="keyword">new</span> <span class="title function_">URL</span>(request.<span class="property">url</span>).<span class="property">pathname</span>.<span class="title function_">split</span>(<span class="string">&quot;/&quot;</span>).<span class="title function_">pop</span>();</span><br><span class="line">    <span class="comment">// 危险：任何登录用户都能读取任意订单</span></span><br><span class="line">    <span class="keyword">const</span> order = <span class="keyword">await</span> env.<span class="property">DB</span>.<span class="title function_">get</span>(<span class="string">`order:<span class="subst">$&#123;orderId&#125;</span>`</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="title class_">Response</span>.<span class="title function_">json</span>(order);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 正确做法：同时校验身份与归属（横向越权防护）</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  <span class="keyword">async</span> <span class="title function_">fetch</span>(<span class="params">request, env</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> userId = <span class="keyword">await</span> <span class="title function_">verifyToken</span>(request);</span><br><span class="line">    <span class="keyword">if</span> (!userId) <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Response</span>(<span class="string">&quot;unauthorized&quot;</span>, &#123; <span class="attr">status</span>: <span class="number">401</span> &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> orderId = <span class="keyword">new</span> <span class="title function_">URL</span>(request.<span class="property">url</span>).<span class="property">pathname</span>.<span class="title function_">split</span>(<span class="string">&quot;/&quot;</span>).<span class="title function_">pop</span>();</span><br><span class="line">    <span class="keyword">const</span> order = <span class="keyword">await</span> env.<span class="property">DB</span>.<span class="title function_">get</span>(<span class="string">`order:<span class="subst">$&#123;orderId&#125;</span>`</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 关键：不存在与无权限返回同一种结果，避免枚举信息泄露</span></span><br><span class="line">    <span class="keyword">if</span> (!order || order.<span class="property">ownerId</span> !== userId) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Response</span>(<span class="string">&quot;not found&quot;</span>, &#123; <span class="attr">status</span>: <span class="number">404</span> &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="title class_">Response</span>.<span class="title function_">json</span>(&#123; <span class="attr">id</span>: order.<span class="property">id</span>, <span class="attr">status</span>: order.<span class="property">status</span> &#125;);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>注意第二段的两个细节：<strong>归属校验</strong>，以及<strong>把”无权限”与”不存在”合并为 404</strong>。后者能防止攻击者通过响应差异枚举出哪些 ID 真实存在。</p><h2 id="边界二：出站请求——最容易出事的地方"><a href="#边界二：出站请求——最容易出事的地方" class="headerlink" title="边界二：出站请求——最容易出事的地方"></a>边界二：出站请求——最容易出事的地方</h2><p>边缘运行时通常允许发起出站 HTTP 请求。这带来典型风险：<strong>服务端请求伪造（SSRF）</strong>。如果你的代码基于用户输入拼接 URL 再发起请求，攻击者就可能借此访问云元数据服务、内网管理接口。</p><p>加固要点（按重要性排序）：</p><ol><li><strong>协议白名单</strong>：只允许 <code>http</code> &#x2F; <code>https</code>，拒绝 <code>file:</code>、<code>gopher:</code> 等；</li><li><strong>主机白名单或域名后缀白名单</strong>：优先使用固定目标，而不是用户提供的任意域名；</li><li><strong>DNS 解析后再校验 IP</strong>：解析得到的地址不得属于内网段（<code>10.0.0.0/8</code>、<code>172.16.0.0/12</code>、<code>192.168.0.0/16</code>、<code>127.0.0.0/8</code>、<code>169.254.0.0/16</code>、<code>::1</code>、唯一本地地址等）；</li><li><strong>禁用自动重定向，或逐跳重新校验</strong>：否则白名单会被一次 302 绕过；</li><li><strong>固定解析结果发起连接</strong>：防止校验与连接之间发生 DNS 重绑定（rebinding）。</li></ol><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="variable constant_">PRIVATE_V4</span> = [</span><br><span class="line">  <span class="regexp">/^10\./</span>, <span class="regexp">/^127\./</span>, <span class="regexp">/^169\.254\./</span>,</span><br><span class="line">  <span class="regexp">/^172\.(1[6-9]|2\d|3[01])\./</span>, <span class="regexp">/^192\.168\./</span>, <span class="regexp">/^0\./</span></span><br><span class="line">];</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">assertSafeUrl</span>(<span class="params">raw</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> u = <span class="keyword">new</span> <span class="title function_">URL</span>(raw);</span><br><span class="line">  <span class="keyword">if</span> (u.<span class="property">protocol</span> !== <span class="string">&quot;http:&quot;</span> &amp;&amp; u.<span class="property">protocol</span> !== <span class="string">&quot;https:&quot;</span>) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&quot;protocol not allowed&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">const</span> host = u.<span class="property">hostname</span>.<span class="title function_">toLowerCase</span>();</span><br><span class="line">  <span class="keyword">if</span> (host === <span class="string">&quot;localhost&quot;</span> || host.<span class="title function_">endsWith</span>(<span class="string">&quot;.internal&quot;</span>) || host.<span class="title function_">endsWith</span>(<span class="string">&quot;.local&quot;</span>)) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&quot;host not allowed&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 若是 IP 字面量，直接校验网段</span></span><br><span class="line">  <span class="keyword">if</span> (<span class="regexp">/^\d+\.\d+\.\d+\.\d+$/</span>.<span class="title function_">test</span>(host) &amp;&amp; <span class="variable constant_">PRIVATE_V4</span>.<span class="title function_">some</span>(<span class="function">(<span class="params">re</span>) =&gt;</span> re.<span class="title function_">test</span>(host))) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&quot;private address not allowed&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> u;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段代码是<strong>应用层的第一道闸</strong>。即便平台在网络层也可能有防护，应用层校验仍不可省——这是典型的深度防御。</p><blockquote><p>安全提醒：<strong>云元数据地址（如链路本地地址段）永远不应被访问</strong>。无论出于什么”合法理由”，把对它的访问写进应用逻辑都是危险信号。</p></blockquote><h2 id="边界三：密钥管理——平台不会帮你保密"><a href="#边界三：密钥管理——平台不会帮你保密" class="headerlink" title="边界三：密钥管理——平台不会帮你保密"></a>边界三：密钥管理——平台不会帮你保密</h2><p>边缘平台上常见的密钥有三类：</p><table><thead><tr><th>密钥类型</th><th>典型用途</th><th>管理要点</th></tr></thead><tbody><tr><td>存储访问令牌</td><td>读写 KV &#x2F; 对象存储</td><td>用平台绑定机制注入，不写进代码</td></tr><tr><td>上游 API Key</td><td>调用第三方服务</td><td>存为 Secret，可轮换，最小权限</td></tr><tr><td>签名&#x2F;加密密钥</td><td>令牌签发、数据加密</td><td>支持密钥版本化，便于轮换</td></tr></tbody></table><p>三条硬规则：</p><ol><li><strong>绝不硬编码</strong>：源码、构建产物、日志中都不应出现密钥；</li><li><strong>绝不提供默认值</strong>：缺少密钥时应当<strong>启动即失败</strong>，而不是退回到某个”默认密钥”——默认密钥一旦进入生产，等于完全没有认证；</li><li><strong>可轮换</strong>：设计时要假设密钥会泄露，轮换流程必须能在不停机的情况下完成。</li></ol><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 反模式：缺失时退回默认值，等于把认证作废</span></span><br><span class="line"><span class="keyword">const</span> <span class="variable constant_">SECRET</span> = env.<span class="property">SESSION_SECRET</span> || <span class="string">&quot;dev-secret&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 正确：缺失即明确失败，避免&quot;看起来能用&quot;的假安全</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">requireSecret</span>(<span class="params">env, name</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> v = env[name];</span><br><span class="line">  <span class="keyword">if</span> (!v || v.<span class="property">length</span> &lt; <span class="number">32</span>) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">`missing or weak secret: <span class="subst">$&#123;name&#125;</span>`</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> v;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="边界四：请求头的可信性——依赖路径封闭"><a href="#边界四：请求头的可信性——依赖路径封闭" class="headerlink" title="边界四：请求头的可信性——依赖路径封闭"></a>边界四：请求头的可信性——依赖路径封闭</h2><p>边缘注入的转发头（客户端 IP、国家、协议版本）非常有用，但<strong>只有在源站只接受来自边缘的请求时才可信</strong>。如果源站同时对公网开放，攻击者可以自行伪造这些头，绕过 IP 风控与地理限制。</p><p>因此：</p><ul><li>使用隧道或私网回源，让源站不接受任意来源；</li><li>在源站<strong>剥离或覆写</strong>外部传入的同名头，只信任来自边缘的那一份；</li><li>不要在应用层把 <code>X-Forwarded-For</code> 的第一个值直接当客户端 IP（它可能被伪造），应使用平台提供的专用头。</li></ul><blockquote><p>经验法则：<strong>任何”可信输入”都必须能回答”为什么可信”</strong>。如果答案是”因为它是我们发的”，就检查一下”别人能不能也发一个”。</p></blockquote><h2 id="责任边界总览"><a href="#责任边界总览" class="headerlink" title="责任边界总览"></a>责任边界总览</h2><table><thead><tr><th>安全面</th><th>平台负责</th><th>你负责</th></tr></thead><tbody><tr><td>DDoS 缓解</td><td>是</td><td>否</td></tr><tr><td>TLS 证书与协议</td><td>是</td><td>否（可配置最低版本）</td></tr><tr><td>运行时隔离</td><td>是</td><td>否</td></tr><tr><td>WAF 规则</td><td>提供能力</td><td><strong>配置与调优</strong></td></tr><tr><td>身份认证</td><td>提供能力</td><td><strong>实现与校验</strong></td></tr><tr><td>对象级授权</td><td>否</td><td><strong>是</strong></td></tr><tr><td>出站请求防护</td><td>部分</td><td><strong>是（SSRF 校验）</strong></td></tr><tr><td>密钥管理</td><td>提供存储</td><td><strong>注入、轮换、不硬编码</strong></td></tr><tr><td>输入校验与输出编码</td><td>否</td><td><strong>是</strong></td></tr></tbody></table><p>这张表的用法很直接：<strong>右侧为”你负责”的每一行，都需要在代码或配置里有对应实现</strong>，不能只停留在”平台应该会处理”的假设上。</p><h2 id="一个可执行的加固检查单"><a href="#一个可执行的加固检查单" class="headerlink" title="一个可执行的加固检查单"></a>一个可执行的加固检查单</h2><p>上线前逐条确认：</p><ol><li>每个非公开端点是否同时做了<strong>身份校验</strong>与<strong>归属校验</strong>；</li><li>所有基于用户输入构造的出站请求是否经过<strong>协议 + 主机 + IP</strong> 三重校验；</li><li>是否关闭了<strong>自动重定向</strong>或逐跳校验；</li><li>敏感响应是否设置了 <code>Cache-Control: no-store</code>，避免被中间层缓存；</li><li>密钥是否全部来自 Secret 注入，缺失时是否<strong>fail fast</strong>；</li><li>错误响应是否<strong>不泄露内部细节</strong>（堆栈、SQL、内部主机名）；</li><li>日志中是否<strong>不包含</strong>令牌、密码、完整个人信息；</li><li>源站是否只接受边缘流量，转发头的可信性是否成立；</li><li>是否有速率限制，防止被当作资源放大器；</li><li>是否对关键安全事件（鉴权失败、跨区域转发、限额触发）有<strong>告警</strong>。</li></ol><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>边缘平台确实承担了大量基础设施安全工作，这让团队可以把精力放在应用层。但这也意味着<strong>一旦应用层漏了，就没有第二道防线</strong>。把上表右侧的每一行都当成必须自己完成的工作项，才是把边缘架构真正做成生产级的方式。</p><blockquote><p><strong>说明：</strong> 本文为原创技术分析，涉及平台能力边界与安全机制的描述基于公开文档与工程实践的一般认知，具体能力的覆盖范围随产品与版本变化，请以 Cloudflare 官方最新文档为准。文中代码为示意性示例，用于说明校验思路，未覆盖全部绕过手法，生产环境请结合威胁建模与安全评审。表格中的责任划分属笔者基于实践经验的主观归纳。</p></blockquote>]]>
    </content>
    <id>https://blogs.yzy15.dpdns.org/cf-edge-security-boundary-model/</id>
    <link href="https://blogs.yzy15.dpdns.org/cf-edge-security-boundary-model/"/>
    <published>2026-09-13T01:12:00.000Z</published>
    <summary>把边缘架构的安全责任划清：平台负责网络与运行时隔离，你负责应用层校验。本文逐层分析鉴权、出站请求、密钥管理、请求头可信性四类边界，并给出可执行的加固清单。</summary>
    <title>边缘计算的安全边界：哪些防线属于平台，哪些必须自己建</title>
    <updated>2026-09-12T17:33:38.958Z</updated>
  </entry>
  <entry>
    <author>
      <name>mc-yzy15</name>
    </author>
    <category term="云计算" scheme="https://blogs.yzy15.dpdns.org/categories/%E4%BA%91%E8%AE%A1%E7%AE%97/"/>
    <category term="架构设计" scheme="https://blogs.yzy15.dpdns.org/tags/%E6%9E%B6%E6%9E%84%E8%AE%BE%E8%AE%A1/"/>
    <category term="边缘计算" scheme="https://blogs.yzy15.dpdns.org/tags/%E8%BE%B9%E7%BC%98%E8%AE%A1%E7%AE%97/"/>
    <category term="区域路由" scheme="https://blogs.yzy15.dpdns.org/tags/%E5%8C%BA%E5%9F%9F%E8%B7%AF%E7%94%B1/"/>
    <category term="数据合规" scheme="https://blogs.yzy15.dpdns.org/tags/%E6%95%B0%E6%8D%AE%E5%90%88%E8%A7%84/"/>
    <content>
      <![CDATA[<h1 id="在边缘做区域化路由：数据驻留与合规的架构落地"><a href="#在边缘做区域化路由：数据驻留与合规的架构落地" class="headerlink" title="在边缘做区域化路由：数据驻留与合规的架构落地"></a>在边缘做区域化路由：数据驻留与合规的架构落地</h1><p>边缘平台的默认行为是”就近执行”，这对性能有利，但在合规面前可能直接违规：用户的个人信息被路由到境外节点处理，就可能触碰数据出境限制。<strong>区域化路由要做的事，是在享受边缘性能的同时，把执行位置收进允许的范围内。</strong></p><h2 id="合规需求如何翻译成技术约束"><a href="#合规需求如何翻译成技术约束" class="headerlink" title="合规需求如何翻译成技术约束"></a>合规需求如何翻译成技术约束</h2><p>合规语言（”数据应当存储在境内””应当在授权区域内处理”）需要先被翻译成工程约束，否则无法设计。常见的三种翻译：</p><ol><li><strong>存储约束</strong>：数据只能写入指定区域的存储；</li><li><strong>计算约束</strong>：处理数据的代码只能在指定区域执行；</li><li><strong>传输约束</strong>：数据不得经由未授权区域中转。</li></ol><p>三者的实现难度依次上升。存储约束最容易（选对存储区域即可）；计算约束要求平台支持区域限定执行；传输约束最难，需要审计整条链路，包括日志、监控、分析等”看不见的旁路”。</p><p>一个经常被忽略的点：<strong>日志与分析链路也属于数据传输</strong>。很多人只检查了主链路，结果请求头里的用户信息被送到了境外的日志平台，合规审计时才发现。</p><blockquote><p>提醒：在设计阶段就把”所有数据流向”画成一张图，包括主链路、日志、追踪、监控、备份、异步任务。合规问题几乎总是出现在旁路上。</p></blockquote><h2 id="区域约束的三种表达方式"><a href="#区域约束的三种表达方式" class="headerlink" title="区域约束的三种表达方式"></a>区域约束的三种表达方式</h2><p>在边缘平台上表达区域约束，通常有以下几种手段：</p><p><strong>方式一：基于平台区域属性的请求级路由</strong><br>请求对象上通常会有区域信息（如执行节点代码 <code>colo</code>、国家&#x2F;地区代码）。你可以在 Worker 入口做判断，决定是本地处理还是转发到特定区域的端点。</p><p><strong>方式二：运行位置约束（Run anywhere &#x2F; 特定区域）</strong><br>部分平台提供显式的执行位置约束能力，让这段代码只能在指定区域内被调度。这比”在代码里判断再转发”更彻底，因为它消除了”代码已经先在不该去的地方执行过”的可能。</p><p><strong>方式三：存储侧约束</strong><br>无论计算在哪，写数据时明确指定区域。这通常通过使用区域化的存储资源来实现。</p><p>三种方式中最容易被忽略的是<strong>方式二与方式一的差别</strong>。只用方式一时，代码已经在边缘执行过了（可能已经读取了请求体），才决定转发——对于严格合规场景，这可能已经构成”在未授权区域处理数据”。因此<strong>合规场景应优先使用平台提供的执行位置约束，而不是靠代码自行判断</strong>。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 方式一示意：在入口做区域判断后转发</span></span><br><span class="line"><span class="comment">// 注意：此写法下代码已在当前 PoP 执行过，严格合规场景请改用平台级执行位置约束</span></span><br><span class="line"><span class="keyword">const</span> <span class="variable constant_">ALLOWED_COLOS</span> = <span class="keyword">new</span> <span class="title class_">Set</span>([<span class="string">&quot;SJC&quot;</span>, <span class="string">&quot;LAX&quot;</span>, <span class="string">&quot;IAD&quot;</span>]);</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> &#123;</span><br><span class="line">  <span class="keyword">async</span> <span class="title function_">fetch</span>(<span class="params">request, env</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> colo = (request.<span class="property">cf</span> &amp;&amp; request.<span class="property">cf</span>.<span class="property">colo</span>) || <span class="string">&quot;UNKNOWN&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!<span class="variable constant_">ALLOWED_COLOS</span>.<span class="title function_">has</span>(colo)) &#123;</span><br><span class="line">      <span class="comment">// 转发到授权区域的处理端点</span></span><br><span class="line">      <span class="keyword">const</span> url = <span class="keyword">new</span> <span class="title function_">URL</span>(request.<span class="property">url</span>);</span><br><span class="line">      url.<span class="property">hostname</span> = env.<span class="property">REGIONAL_ENDPOINT_HOST</span>;</span><br><span class="line">      <span class="comment">// 保留原始路径与查询，避免语义变化</span></span><br><span class="line">      <span class="keyword">return</span> <span class="title function_">fetch</span>(<span class="keyword">new</span> <span class="title class_">Request</span>(url, request));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="title function_">handleLocally</span>(request, env);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h2 id="一张表：区域路由策略与代价"><a href="#一张表：区域路由策略与代价" class="headerlink" title="一张表：区域路由策略与代价"></a>一张表：区域路由策略与代价</h2><table><thead><tr><th>策略</th><th>实现层级</th><th>合规强度</th><th>性能代价</th><th>复杂度</th></tr></thead><tbody><tr><td>全区域执行 + 区域化存储</td><td>存储</td><td>中</td><td>低</td><td>低</td></tr><tr><td>请求级判断 + 转发</td><td>应用</td><td>中低</td><td>中（多一跳）</td><td>中</td></tr><tr><td>平台级执行位置约束</td><td>平台</td><td>高</td><td>低（原生调度）</td><td>中</td></tr><tr><td>独立多区域部署 + 独立域名</td><td>部署</td><td>高</td><td>低</td><td>高</td></tr></tbody></table><p>第四行值得说明：有些团队干脆为每个区域部署一套独立的应用与域名，用 DNS 或入口规则分流。这种方式合规最清晰（物理隔离），代价是运维复杂度与成本大幅上升，且跨区域的能力无法共享。</p><p>选择建议：<strong>合规等级决定下限，性能与运维成本决定上限</strong>。不要为了”看起来合规”而选择最复杂方案，也不要为了省事而用”代码判断”来应对严格审计。</p><h2 id="失败与降级：区域受限之后没有”全自动绕行”"><a href="#失败与降级：区域受限之后没有”全自动绕行”" class="headerlink" title="失败与降级：区域受限之后没有”全自动绕行”"></a>失败与降级：区域受限之后没有”全自动绕行”</h2><p>这是最容易踩的坑。普通的多区域部署可以做到”某个区域挂了，流量切到另一个区域”；但<strong>区域受限的场景下，这个动作本身可能违规</strong>。</p><p>因此降级策略必须提前设计，且必须是”合规的降级”：</p><ol><li><strong>同区域内多可用区冗余</strong>：在允许的区域内部做冗余，这是首选的降级维度；</li><li><strong>降级到只读或静态响应</strong>：当期望区域不可用时，返回缓存内容或只读页面，而不是把请求转发到境外；</li><li><strong>明确拒绝并给出可解释错误</strong>：合规不可用时，”拒绝服务”有时是正确行为，但必须让用户与监控都清楚原因，而不是表现为超时；</li><li><strong>人工开关</strong>：对跨区域转发保留人工控制的开关，并记录每次开启的审计日志。</li></ol><blockquote><p>关键原则：<strong>不要把”可用性优先”的默认习惯套到区域受限系统上</strong>。在这类系统里，正确性（合规）优先级高于可用性，降级设计必须体现这一点。</p></blockquote><h2 id="可观测性：如何证明”数据没有跑到不该去的地方”"><a href="#可观测性：如何证明”数据没有跑到不该去的地方”" class="headerlink" title="可观测性：如何证明”数据没有跑到不该去的地方”"></a>可观测性：如何证明”数据没有跑到不该去的地方”</h2><p>合规审计问的不是”你认为会不会”,而是”你怎么证明”。因此需要可观测性设计：</p><ul><li>每个请求记录<strong>执行位置标识</strong>，形成可导出的审计数据；</li><li>每个出站调用记录<strong>目标区域</strong>，尤其是存储与日志写入；</li><li>对<strong>跨区域转发事件</strong>单独计数并告警；</li><li>保留<strong>配置变更历史</strong>（区域白名单的每次修改）。</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 概念性查询示意：按执行位置统计请求量，用于审计留档</span></span><br><span class="line"><span class="comment"># 使用平台的日志分析能力（语法依平台而异，以官方文档为准）</span></span><br><span class="line"><span class="comment"># 目标：按 colo 维度输出请求计数与跨区域转发计数</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">&quot;query&quot;</span>: <span class="string">&quot;SELECT colo, COUNT(*) AS requests, SUM(is_forwarded) AS forwarded FROM logs GROUP BY colo&quot;</span>,</span><br><span class="line">  <span class="string">&quot;note&quot;</span>: <span class="string">&quot;字段名与语法为示意，实际请参考平台日志查询文档&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="团队落地：把合规约束写进工程流程"><a href="#团队落地：把合规约束写进工程流程" class="headerlink" title="团队落地：把合规约束写进工程流程"></a>团队落地：把合规约束写进工程流程</h2><p>技术方案之外，真正让区域化路由可靠的是流程：</p><ol><li><strong>架构评审清单加一条</strong>：新增依赖时评估其数据区域；</li><li><strong>CI 校验</strong>：在部署流水线里校验区域白名单配置是否被意外清空；</li><li><strong>变更审批</strong>：跨区域转发的开启属于需要审批的变更；</li><li><strong>定期演练</strong>：模拟某区域不可用，验证降级路径真的可用；</li><li><strong>文档化</strong>：把”哪些数据在哪些区域”写成一张每个人都看得懂的图。</li></ol><blockquote><p>经验法则：<strong>合规约束如果不能被自动化校验，就会在下一次紧急发布时被破坏</strong>。把它变成流水线里的一个红灯，是最省事的长期方案。</p></blockquote><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>区域化路由的本质是<strong>用性能与运维复杂度换取合规确定性</strong>。它不是加几行判断的事，而是要在部署模型、降级策略、可观测性与流程上都做出配套调整。好消息是这些调整大多是”一次性投入、长期受益”——一旦建立，后续新增业务可以复用同一套约束与审计手段。</p><blockquote><p><strong>说明：</strong> 本文为原创技术分析，涉及区域路由能力、执行位置约束的实现方式基于公开文档与工程实践的一般认知，不同平台与版本的具体接口存在差异，请以 Cloudflare 官方最新文档为准。文中代码与查询为示意性示例，未包含完整错误处理，生产使用前请依据自身合规要求评估。表格中的评级与建议属笔者基于实践经验的主观判断，不构成法律或合规意见。</p></blockquote>]]>
    </content>
    <id>https://blogs.yzy15.dpdns.org/cf-edge-regional-routing-architecture/</id>
    <link href="https://blogs.yzy15.dpdns.org/cf-edge-regional-routing-architecture/"/>
    <published>2026-09-13T01:10:00.000Z</published>
    <summary>当业务需要&quot;数据不出境&quot;或&quot;就近处理&quot;时，边缘平台的区域化能力该怎么用？本文讨论区域约束的表达方式、请求路由策略、失败降级设计，以及如何避免区域路由带来的可用性与性能陷阱。</summary>
    <title>在边缘做区域化路由：数据驻留与合规的架构落地</title>
    <updated>2026-09-12T17:33:38.958Z</updated>
  </entry>
  <entry>
    <author>
      <name>mc-yzy15</name>
    </author>
    <category term="云计算" scheme="https://blogs.yzy15.dpdns.org/categories/%E4%BA%91%E8%AE%A1%E7%AE%97/"/>
    <category term="回源" scheme="https://blogs.yzy15.dpdns.org/tags/%E5%9B%9E%E6%BA%90/"/>
    <category term="私网互联" scheme="https://blogs.yzy15.dpdns.org/tags/%E7%A7%81%E7%BD%91%E4%BA%92%E8%81%94/"/>
    <category term="隧道" scheme="https://blogs.yzy15.dpdns.org/tags/%E9%9A%A7%E9%81%93/"/>
    <category term="网络安全" scheme="https://blogs.yzy15.dpdns.org/tags/%E7%BD%91%E7%BB%9C%E5%AE%89%E5%85%A8/"/>
    <content>
      <![CDATA[<h1 id="边缘到源站的私网路径：把回源流量收进隧道里"><a href="#边缘到源站的私网路径：把回源流量收进隧道里" class="headerlink" title="边缘到源站的私网路径：把回源流量收进隧道里"></a>边缘到源站的私网路径：把回源流量收进隧道里</h1><p>边缘平台解决的是”用户到边缘”这一段。但你的业务数据通常在源站——自建机房、云主机、Kubernetes 集群。<strong>回源这一段长期是架构中最被忽视的安全薄弱点</strong>：源站为了接受回源，往往开放公网端口，只靠 IP 白名单保护，而 IP 白名单在边缘平台的场景下几乎无法使用（出口 IP 众多且会变化）。</p><p>私网互联方案的核心思路，是把”源站监听公网端口”换成”源站主动建立出站隧道”。</p><h2 id="传统回源的三个结构性问题"><a href="#传统回源的三个结构性问题" class="headerlink" title="传统回源的三个结构性问题"></a>传统回源的三个结构性问题</h2><ol><li><strong>源站必须对公网可见</strong>：即使加了白名单，端口扫描与指纹识别仍然能发现源站，暴露攻击面；</li><li><strong>白名单在边缘场景失效</strong>：边缘出口地址段庞大且动态，维护白名单不现实，放宽后又失去意义；</li><li><strong>HTTP 回源会丢客户端信息</strong>：源站看到的客户端 IP 是边缘节点的，业务日志与风控会失真。</li></ol><p>第三点的解法通常是边缘追加转发头（如 <code>CF-Connecting-IP</code>、<code>X-Forwarded-For</code>），但<strong>这要求源站信任这些头</strong>。如果源站同时对公网开放，那任何人都能伪造这些头。这也说明了一个重要原则：<strong>转发头的可信性依赖于回源路径的封闭性</strong>。</p><h2 id="隧道方案的工作原理"><a href="#隧道方案的工作原理" class="headerlink" title="隧道方案的工作原理"></a>隧道方案的工作原理</h2><p>思路很朴素：</p><ol><li>在源站网络内运行一个轻量守护进程（connector）；</li><li>该进程<strong>主动向边缘平台建立出站长连接</strong>（通常基于 TLS&#x2F;HTTP2 或 QUIC）；</li><li>边缘收到用户请求后，通过这条已有连接把请求推给守护进程；</li><li>守护进程将请求转发到本地服务（如 <code>127.0.0.1:8080</code>）；</li><li>响应沿原路返回。</li></ol><p>这样做的直接收益：</p><ul><li><strong>源站不需要开放任何入站端口</strong>，防火墙可以默认拒绝外部入站；</li><li><strong>连接是出站的</strong>，因此能穿过 NAT 与严格的企业防火墙；</li><li><strong>源站身份由隧道绑定</strong>，不再依赖 IP 白名单；</li><li><strong>可以隐藏源站真实地址</strong>，降低被直接攻击的可能。</li></ul><blockquote><p>重要提醒：隧道把<strong>身份认证</strong>的责任从”IP 地址”转移到”隧道凭据”。这意味着凭据泄露等价于源站暴露，凭据必须按密钥管理（安全存储、可轮换、最小权限、定期审计），绝不能硬编码在镜像或脚本里。</p></blockquote><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 概念性示意：在源站启动 connector，用凭据建立出站隧道</span></span><br><span class="line"><span class="comment"># 凭据建议通过环境变量注入，不要写进命令行历史或配置文件</span></span><br><span class="line">cloudflared tunnel run \</span><br><span class="line">  --token <span class="string">&quot;<span class="variable">$TUNNEL_TOKEN</span>&quot;</span> \</span><br><span class="line">  --url http://127.0.0.1:8080</span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 源站防火墙的最小化原则：只允许出站，拒绝入站</span></span><br><span class="line"><span class="comment"># 示意，实际命令依操作系统与云厂商安全组而定</span></span><br><span class="line"><span class="comment"># 允许出站 HTTPS（用于建立隧道）</span></span><br><span class="line"><span class="built_in">sudo</span> ufw allow out 443/tcp</span><br><span class="line"><span class="comment"># 默认拒绝入站，仅保留必要的管理端口（最好也走隧道/跳板）</span></span><br><span class="line"><span class="built_in">sudo</span> ufw default deny incoming</span><br></pre></td></tr></table></figure><h2 id="身份与访问控制：隧道之后要解决什么"><a href="#身份与访问控制：隧道之后要解决什么" class="headerlink" title="身份与访问控制：隧道之后要解决什么"></a>身份与访问控制：隧道之后要解决什么</h2><p>隧道解决了”回源通道私有”，但一个成熟的私网方案还需要回答以下问题：</p><p><strong>第一，谁能访问这条隧道？</strong><br>通常要求在边缘侧配置访问策略，例如”只有某个域名下的特定路径，经过某类鉴权后才允许回源”。否则隧道本身就成了绕过 WAF 的后门。</p><p><strong>第二，源站如何识别调用方？</strong><br>如果源站还会被其他内部系统调用（例如定时任务、其他微服务），那么”所有流量都必须来自隧道”这种强约束可能不现实。此时的做法是把源站拆成两个入口面：隧道面（面向边缘）与内网面（面向内部系统），并在内网面上再加一层鉴权。</p><p><strong>第三，mTLS 是否需要？</strong><br>对于合规要求较高的场景，边缘与源站之间可以启用双向 TLS，保证即便隧道凭据泄露，没有客户端证书也无法建立会话。代价是证书生命周期管理复杂度上升。</p><p><strong>第四，能否保留客户端信息？</strong><br>隧道模式下源站看不到真实客户端 IP，需要依赖边缘注入的头部，且必须保证源站<strong>只接受来自隧道的请求</strong>，否则转发头可被伪造。</p><h2 id="四种回源模式对比"><a href="#四种回源模式对比" class="headerlink" title="四种回源模式对比"></a>四种回源模式对比</h2><table><thead><tr><th>模式</th><th>源站是否暴露</th><th>身份校验方式</th><th>迁移成本</th><th>适用场景</th></tr></thead><tbody><tr><td>公网回源 + IP 白名单</td><td>是</td><td>源 IP</td><td>低</td><td>小规模、源站 IP 固定</td></tr><tr><td>公网回源 + 自定义头校验</td><td>是</td><td>共享密钥头</td><td>低</td><td>快速接入，但需防头伪造</td></tr><tr><td>出站隧道（守护进程）</td><td>否</td><td>隧道凭据</td><td>中</td><td>自建机房、混合云、NAT 后</td></tr><tr><td>私网直连（云厂商专线类）</td><td>否</td><td>网络层 + 路由控制</td><td>高</td><td>大型企业、多云专线</td></tr></tbody></table><p>选择逻辑通常是：<strong>先看源站是否被迫暴露</strong>（能否接受公网端口暴露是第一决策点），再看<strong>运维能力是否支撑凭据与证书的生命周期管理</strong>。第一点的答案通常是”不能接受”，那就直接跳到隧道方案。</p><h2 id="落地要点：把隧道当成生产组件来运维"><a href="#落地要点：把隧道当成生产组件来运维" class="headerlink" title="落地要点：把隧道当成生产组件来运维"></a>落地要点：把隧道当成生产组件来运维</h2><p>隧道在生产环境中会遇到的典型问题，以及对应做法：</p><ul><li><strong>单点故障</strong>：只跑一个 connector 进程，进程挂掉或宿主机重启即全站不可回源。做法是<strong>至少两个 connector 实例</strong>，分布在不同的机器或可用区；</li><li><strong>连接抖动</strong>：网络波动导致隧道重建，期间请求失败。做法是配置合理的重试与超时，并让边缘侧把隧道不可达转化为明确的 5xx（而不是长时间挂起）；</li><li><strong>配置漂移</strong>：隧道配置分两处（边缘侧规则 + 源站 connector 参数），改一处忘一处。做法是<strong>把配置纳入基础设施即代码管理</strong>；</li><li><strong>凭据轮换缺失</strong>：凭据多年不换。做法是<strong>纳入密钥管理流程，定期轮换并留出双活窗口</strong>；</li><li><strong>可观测性缺失</strong>：出问题只能”重启试试”。做法是<strong>采集 connector 的连接状态、重建次数、转发错误率</strong>，并接入告警。</li></ul><blockquote><p>经验法则：<strong>隧道不是”更安全的回源方式”，而是”把回源的可信边界从 IP 层移到凭据层”</strong>。边界换了，运维动作也必须跟着换——凭据轮换、双活实例、指标监控，缺一不可。</p></blockquote><h2 id="与-WAF、负载均衡的协同"><a href="#与-WAF、负载均衡的协同" class="headerlink" title="与 WAF、负载均衡的协同"></a>与 WAF、负载均衡的协同</h2><p>一个常见的误解是”上了隧道就不需要 WAF”。实际上两者的防护位置不同：</p><ul><li><strong>WAF 在边缘侧</strong>：拦截注入、扫描、爬虫等恶意请求，属于应用层过滤；</li><li><strong>隧道在回源侧</strong>：保证只有经过边缘的流量能到达源站，属于通道保护。</li></ul><p>正确顺序是<strong>先过滤、再通过隧道回源</strong>。如果源站还有独立的公网入口，必须保证该入口的处理链与边缘一致，否则攻击者会绕过 WAF 从薄弱入口直接打源站。这就是所谓”最弱入口决定整体安全水位”。</p><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>私网回源的价值不只是安全，它同样改善了架构的可演进性：源站地址可以自由变更、可以部署在任意 NAT 之后、可以横向扩容而不需要通知边缘侧。把回源这一段从”公网裸露”变成”凭据化的私有通道”，是边缘架构走向生产级的重要一步。</p><blockquote><p><strong>说明：</strong> 本文为原创技术分析，所述隧道、私网互联机制的描述基于公开文档与工程实践的一般认知，具体命令与配置项随版本变化，请以 Cloudflare 官方最新文档为准。文中命令为示意性示例，执行前请根据实际环境调整并评估影响。表格对比中的成本评级与建议属笔者基于实践经验的主观判断。</p></blockquote>]]>
    </content>
    <id>https://blogs.yzy15.dpdns.org/cf-edge-private-networking-architecture/</id>
    <link href="https://blogs.yzy15.dpdns.org/cf-edge-private-networking-architecture/"/>
    <published>2026-09-13T01:08:00.000Z</published>
    <summary>传统回源把源站暴露在公网，边缘平台的私网互联方案则通过隧道绑定源站身份。本文讲清其工作原理、身份校验方式、与 WAF/负载均衡的关系，以及在多云与混合云场景下的落地要点与常见故障。</summary>
    <title>边缘到源站的私网路径：把回源流量收进隧道里</title>
    <updated>2026-09-12T17:33:38.958Z</updated>
  </entry>
</feed>
